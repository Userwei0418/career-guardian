# 变更日志

## 2026-07-11 ~ 2026-07-12 全面重构

### Phase 1: 前端修复
- 修复"启动失败"假阳性问题：改为 async 启动 + Toast 通知
- Pipeline 仪表盘 UI：三步骤向导 + 实时进度

### Phase 2: 数据库重构
- 新增 6 张表替代 INI 配置：
  - `crawl_companies` (816家公司已迁移)
  - `crawl_company_urls` (1467个URL)
  - `crawl_tasks`
  - `crawl_company_status`
  - `crawl_pipeline_runs`
  - `crawl_data_lineage`
- 迁移脚本：`backend/db/migrate_ini_to_db.py`

### Phase 3: 爬虫服务重构
- TaskManager 类（数据库持久化 + 线程池）
- 支持按公司粒度抓取：`--company-ids`, `--all` 参数
- Pipeline 编排器：`crawler/pipeline.py`
- 按公司抓取 + asyncio 异步执行

### Phase 4: 后端API增强
- 新增爬虫管理代理端点
- 新增数据处理端点：`/api/process/*`, `/api/ingest/*`
- WebSocket 实时推送：`/ws/crawl-status`
- 文件浏览 API（数据库版）

### Phase 5: 前端重构
- `/admin/crawler` - Pipeline 仪表盘
- `/admin/companies/crawl` - 公司管理
- `/admin/process` - 处理进度
- `/admin/files` - 文件浏览
- Toast 通知组件

### Phase 6: 质量反馈
- `backend/ingest_cjob.py`: run_ingest() 返回结构化结果
- 入库报告：新增/重复/失败/质量分布

---

## 技术决策记录

| 决策 | 原因 |
|------|------|
| INI 保留为配置源 | 迁移脚本导入DB，向后兼容 |
| WebSocket 而非 SSE | 双向通信需求 |
| 公司级而非配置文件级抓取 | 更细粒度控制 |
| Pipeline 表记录每步骤 | 数据血缘追踪 |

## 待完善事项

- Phase 6: Ingest 结果反馈 + 血缘追踪完善
- API 访问速率限制
- 配置外部化（CSV路径/DB连接 -> 环境变量）
- Prometheus + Grafana 监控
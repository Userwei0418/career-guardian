# 部署指南

## 本地开发部署

### 前置依赖

- Python 3.10+
- Node.js 18+
- MySQL 8.0
- Redis 7

### 快速启动

```bash
# 一键启动所有服务（4个窗口）
start.bat
```

### 手动分步启动

```bash
# 1. 后端 API (port 8000)
cd backend/api
python main.py

# 2. 爬虫管理 (port 8001)
cd services
python crawler_service.py

# 3. 系统监控 (port 8002)
cd services
python monitor_service.py

# 4. 前端 (port 3000)
cd frontend
npm run dev
```

## 环境配置

### backend/.env

```
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=zhaogebanshang

REDIS_HOST=localhost
REDIS_PORT=6379

CACHE_TTL=300
WARM_INTERVAL_MINUTES=30
```

### frontend/.env.local

```
NEXT_PUBLIC_API_URL=http://localhost:8000
```

## 数据库初始化

```mysql
-- 创建数据库
CREATE DATABASE zhaogebanshang CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 初始化表结构 (执行 db/init.sql)
-- 导入CSV数据
python import_csv_data.py

-- 构建向量索引
POST /api/analysis/match/build-index?sample_size=10000
```

## 生产部署 (宝塔面板)

### 架构说明

- **前端**: 宝塔 Node 项目管理器 -> `npm start` (port 3000)
- **后端**: 宝塔 Python 项目管理器 -> `main.py` (port 8000)
- **Nginx**: 反向代理 `/` -> 前端, `/api/` -> 后端

### 部署步骤

1. 本地 `npm run build` 后上传 `.next` + `public` + `package.json`
2. 上传 `api/` 目录到服务器
3. 配置 Nginx 反向代理
4. 启动服务

### Nginx 配置示例

```nginx
location ^~ /api/ {
    proxy_pass http://127.0.0.1:8000/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

location / {
    proxy_pass http://127.0.0.1:3000/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```

## 数据备份

```bash
# 导出
mysqldump -h 127.0.0.1 -P 3306 -u root -p zhaogebanshang > backup.sql

# 导入
mysql -h 127.0.0.1 -P 3306 -u root -p zhaogebanshang < backup.sql
```

## 常见问题

| 问题 | 解决方案 |
|------|----------|
| 端口占用 | `netstat -ano \| findstr :8000` |
| 数据库连接失败 | 检查 .env 中 DB_HOST 和防火墙 |
| 前端跨域 | 确认 next.config.js 代理配置 |
| 缓存预热失败 | 检查 Redis 是否正常运行 |
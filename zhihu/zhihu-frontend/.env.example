# 浏览器默认通过 Next 同源 /api 访问后端。
GUARDIAN_API_INTERNAL_URL=http://127.0.0.1:8000

# 只有在 Web 与 API 必须由浏览器跨域直连时才设置。
# NEXT_PUBLIC_API_URL=https://api.example.com/api

# 生产环境若 WebSocket 不与页面同域，可单独指定公开的职护实时代理地址。
# 浏览器仍只连接职护后端，不直接获得第三方服务长期密钥。
# NEXT_PUBLIC_GUARDIAN_WS_URL=wss://api.example.com

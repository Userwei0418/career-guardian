# 职护当前进度与新会话交接

> 文档状态：当前唯一进度入口
> 最后更新：2026-08-25
> 当前状态：2026-08-25 经用户明确授权，后端实际 `.env` 指向的 `localhost:3306/zhihu` 已从 `20260825_0056` 升级到代码唯一 head `20260825_0061`。`alembic current --verbose` 与直接查询均确认 `0061 (head)`，实际库现为 89 张表、18 张 `growth_%` 表。`0057` 增加收支问询幂等字段，`0058`—`0060` 分别建立当下工作 6 表、过去资产 5 表、未来方向 4 表，`0061` 建立沟通草稿、跨守护交接和成长问询 3 表。市场数据的 core/raw/staging 是另一条迁移线，同日最后核对均为 `20260819_0018`。管理员 99 条活动流水、账本修订号 213、系统收支分类 21 个、知识 43 篇/发布 39 篇是迁移前的同日历史快照，不是本次全量复查结果。本轮未重启服务，也未触发真实外部模型。
> 说明：正式运行只支持 MySQL，Alembic 迁移是数据库结构的唯一版本线。SQLite 只允许少量不启动应用、不作为数据库集成证据的隔离纯单元测试引擎；应用配置、开发运行、CI 数据库集成和正式验收均不得使用 SQLite。代码、迁移、测试、HTTP/API、登录态浏览器验收和用户最终业务验收必须分开表述。收支工作台现已形成“两个录入入口 → 可信账本摘要 → 消费/收入多维分析 → AI 流式问询 → 用户知识”的主路径，长列表和低频工具移至独立页面或抽屉；腾讯云 `GeneralAccurateOCR`、完整行切片、跨片确定性去重、坐标配行、来源框选、候选合并和人工门禁已进入代码基线。当前 admin 收支数据均为用户授权的测试数据，后续验收可再次清理；不能把本快照或开发侧回归写成整个收支守护已经最终验收。
> 历史口径：本文件按日期保留了旧迁移头、旧批次和旧数据量，它们只说明当时发生过什么。凡旧章节出现“当前”“正式库当前”等字样，均以章节日期为作用域；判断此刻状态时，以本页顶部、当前迁移文件和只读运行查询为准。

## 0. 当前紧急任务结果与生产前边界

2026-08-18 已确认根因不只是“列表选择器失效”，而是采集链路缺少对入口有效性、详情导航、完整渲染 HTML、详情正文和失败事实的统一管理。本轮已完成：

1. 列表岗位只是发现索引。适配器现在会打开独立详情页、从卡片 ID 构造受限同域详情 URL，或在没有 href 时按可见标题点击。它保留完整渲染 HTML、可读正文、抓取模式、命中选择器、导航策略和警告，然后再进入程序标准化、按需 AI 语义拆分和质量门。
2. 任务不再把“页面打开但没有详情”写成成功。失败现在区分 `source_entry_invalid`、`list_parse_failed`、`detail_navigation_failed`和 `detail_content_failed`；零详情证据的批次直接失败，空职责/要求记录不会被质量门放行。
3. 58 同城旧入口 `https://campus.58.com/campus/jobs` 已 404，目录已改为新的 `campus-recruitment` / `social-recruitment` hash 路由，用 `#/job/` 语义链接识别详情。入口 404 不会由 AI 猜测一个新 URL；系统会记录入口失效并要求管理员核对新入口。
4. 健合 HotJob 卡片不再当成完整岗位；兼容解析器提取 `postId`，声明式同域模板打开 `posDetail.html?postId=...&postType=society`后抓正文。
5. 管理员任务详情会显示 Raw HTML 字符/字节数、正文长度、抓取模式、详情策略、命中选择器和警告；完整 HTML/正文只能由管理员按需打开，HTML 在 `<pre>` 中转义显示，不执行脚本或 iframe。任务时间线可展开 JSON 上下文；失败日志保留错误类型、类别、消息和恢复建议。
6. AI 调用明细保留调用时间、主体、能力类型、页面/功能点、供应商/模型、状态、耗时、用量、成本和错误类型；“采集解析规则自动修复”使用系统主体。不保存 Prompt、页面完整 DOM、简历、Offer、JD 或模型回复正文。
7. 参考 `qzclawler` 后已将公开学校配置和安全兼容解析器包装为职护自有资产：308 个学校配置、667 个公告来源、85 个解析器。学校公告已接入与企业渠道相同的 Raw、标准化、质量门和 Core 岗位主库；导入不会批量启用来源，且只有能取得明确招聘单位和完整岗位事实的记录才晋级，不确定记录继续隔离。
8. 原爬虫逐详情使用 3～10 秒随机间隔。职护现已恢复来源级节奏，并允许管理员在每次采集前填写随机等待最小/最大秒数。单次覆盖写入 `crawl_tasks.run_options`，不会污染渠道默认配置；渠道最小请求间隔仍是不可被单次任务降低的安全下限。`collection_snapshot` 日志记录配置范围、实际等待次数和累计等待毫秒数；页面渲染等待与请求节奏分开记录。
9. 管理员在每次采集前可选择渠道默认/无头/可见浏览器、渠道默认/全量/增量、最大页数、最大记录数和随机等待范围。运行时把请求值、生效值、分页动作、加载批次、停止原因和详情完成度统一保存在任务及事件日志中。
10. 站点明确显示“暂无职位”等空状态时，任务记为成功的零结果并写 `source_empty_confirmed`，不会误报解析规则失效，也不会生成无意义的 AI 修复候选。
11. `DataError / Data too long for column / raw_records` 被归类为 `storage_schema`，恢复动作是检查 MySQL 迁移，不再增加解析策略失败次数或错误触发 AI 规则修复。
12. 管理后台已改为可收起的左侧导航；企业渠道与学校公告共用卡片层级、来源状态筛选和采集任务区。筛选项包括全部、已启用、待启用、被弃用、待审查（有问题）。
13. 采集弹窗会直接显示来源默认最大页数、最大记录数和详情随机等待范围。管理员可终止排队或运行中的任务；操作使用站内二次确认，不再弹浏览器原生 `confirm`。请求人与原因写入 `task_cancel_requested`，执行器在页面等待、翻页、详情等待和逐条存储边界响应后写 `task_cancelled`，已经写入 Raw 的记录不会因主动终止回滚。
14. 管理后台左侧导航在桌面端固定于视口，不随内容滚动；公司和学校的来源状态筛选均收进各自列表标题栏下拉框。公司与学校行内的状态操作统一为单一下拉框“启用 / 暂停 / 弃用”，弃用仍经站内二次确认并以 MySQL 治理状态持久化，不删除渠道配置、Raw、岗位主库或审计历史，可由管理员重新启用。
15. 学校来源卡片标题使用单行截断并保留原文悬停提示，长校名不会再挤压来源状态、健康状态和采集操作区。
16. 管理后台新增公司管理和职位管理：使用正式 MySQL 的 Core 公司/岗位主库，支持新增、查看、编辑、软删除、搜索、状态筛选、排序、服务端分页和每页条数选择；职位手工发布仍受正文完整性约束。
17. 公司/职位 CRUD 均写入独立管理员审计表，保留实体、动作、操作者、变更前后快照和时间。职位删除会明确记录为“删除”，旧版曾把软删除记成“编辑”的历史事件也会按状态变化正确显示；页面展示实体名称、状态变化、记录编号和操作者。
18. 管理导航按业务归属调整为“数据采集 → 数据准入 → 审查规则”，审查规则位于第三个数据治理模块之下；展开侧栏时职位筛选区恢复紧凑单行布局，缩小控件高度、间距和列宽，不再出现大块两行表单或竖排按钮。
19. 解析修复低成功率的主要程序缺陷已修复：过去 AI 候选的 `item_selectors` 只用于等待页面，真实岗位提取仍走旧解析器；现在已经审批生效的声明式选择器会进入真实重复卡片提取，生成标题、链接、岗位 ID、地点和发布时间，再沿同一详情抓取链路运行。候选仍须受限回放、Canary 和人工审批，不承诺任意新站点都能自动修复。
20. 采集任务已持久化六阶段进度：校验入口、发现岗位、抓取详情、写入 Raw、标准化与准入、完成。任务列表只显示单条进度条；详情展示已加载页数、发现总量、详情成功/失败/剩余、Raw 写入/重复/失败、晋级/隔离和总耗时。旧任务只能回填终态，新任务才有逐阶段实时指标。
21. 数据准入页面为每个硬条件、权重和数值设置补充悬停问号说明，保存草稿、影响预检、发布新标准三个按钮保持同一行。策略发布严格向前生效：只约束发布后新进入的数据，存量岗位不会自动重跑、降级或消失，并继续保留原认证版本和审计记录。
22. 正式 MySQL 已应用采集进度迁移并重新导入企业、学校目录；前端生产构建通过，浏览器已验证准入问号、向前生效说明、三按钮单行、任务列表进度条和任务详情六阶段结构。清场后重新产生的验收数据属于新一轮现场结果，不应再引用 0.1 节的清场瞬时值作为当前计数。
23. 数据准入“发布新标准”已移除浏览器原生确认框，改为站内主题弹窗；弹窗明确显示待发布策略、预检样本、预计通过/隔离和“仅约束未来新数据”的生效边界。没有草稿及影响预检时发布按钮保持禁用，不为界面验收伪造正式策略草稿。
24. 学校公告不再受历史 `raw_only` 兼容标记阻断。正式 MySQL 任务 #69（浙江师范大学职业发展与就业服务中心）已有 400 条 Raw 经统一标准化与质量门重放，392 条晋级 Core、8 条按质量规则隔离；任务详情逐条建立 Raw→Core 血缘并显示“查看主库岗位”。市场洞察已刷新，统一岗位总量从 133,309 更新为 133,701。
25. 管理后台新增真正独立的“学校管理”，不再复制“数据采集 → 学校公告”页面。正式 MySQL `market_raw.recruitment_schools` 一行维护一个学校/就业服务机构主体，通过 `data_sources.school_id` 关联一个或多个公告入口；学校管理负责主体 CRUD、搜索、状态筛选、排序、分页和独立审计，公告网址、抓取规则、来源启停和采集任务仍只在数据采集模块维护。正式目录已归并为 308 个学校主体并关联 667 个公告来源，未批量改变来源启用状态。
26. 学校主体接口已用正式数据核验：可分别检索到“东北大学学生指导服务中心”和“湖北大学学生发展与就业指导中心”，并返回所归属学校及公告来源数量。学校晋级岗位仍沿统一 Raw、质量门和 Core 链路进入职位管理、机会守护、仪表盘和岗位检索；学校主体管理与岗位生态接入是两个独立但关联的职责。
27. 采集与监控文档已按当前实现同步：`docs/data/recruitment-collection-pipeline.md` 继续作为企业/学校统一端到端链路，新增 `docs/data/collection-observability-and-operations.md` 统一说明任务状态、六阶段进度、运行参数、事件日志、人工终止、Raw 证据、渠道健康、恢复动作、AI 调用日志和运维验收；`collection-rule-self-healing.md` 明确只有规则类故障才生成受限 AI 候选，空来源、入口失效、存储迁移、限流/封禁和质量门问题不误触发规则修复。
28. 个性化职业形象已有三个可回退提交基线：`267e6ab1`（后端任务与存储）、`5b9105eb`（前端职业形象体验）、`e5278eb5`（调用加固）。此后围绕共享 AI Key、配置精简、提示词、超时恢复和首页重构的改动仍全部留在工作区。2026-08-19 只读核验确认正式 MySQL 已是 `20260819_0021`，两个历史版本的四个供应商任务都返回 `completed`，横图/方图均已入库；但两个迁移文件仍未跟踪，活动任务恢复/幂等没有真实浏览器验收。
29. 今日首页未提交 WIP 已按确认后的信息结构重构：桌面 Hero 固定 560px，长 `ActionItem.title` 不再直接撑高按钮和首屏；右图只常驻 TODAY、优先关注和职业形象角标，生成逻辑及生成/资料入口按需展开。下方用当前接口增加岗位市场、成长、Offer/权益/收入信号，并把原 `/journey` 的六阶段长纵向时间线迁入 `/today`：六个阶段全部展开，文章取 `/knowledge/` 当前已发布内容，工具只链接现有可用页面，文章数与工具数分开显示，不冒充完成进度或里程碑。原 `/journey` 用户页面和导航入口已移除，后端 `/api/journey`、`JourneyNode` 与历史迁移暂留为兼容层。没有复用原型中的 `31,386`、`+9`、`¥7,500` 等示例值。右侧图片槽实测桌面约 `595×558`、手机 `348×368`，当前读取既有 `1024×1024` 方图并铺满卡片。桌面和手机均无横向溢出；角标展开、长时间线、阶段锚点和知识文章抽屉已在登录态浏览器中验证。本轮没有创建图片任务、载入脱敏案例、迁移数据库或启停服务。

### 0.1 正式 MySQL 清场结果

本节记录的是清场完成当时的可恢复验收基线，不是当前实时统计。清场之后管理员已再次发起采集并产生新的任务、Raw 和 Core 记录；当前数量与启用状态必须以管理后台和正式 MySQL 为准。

- 清理边界：北京时间 `2026-08-17 00:00:00` 起；执行前无排队、运行或终止中的任务。
- 已删除：54 个采集任务、21 个采集批次、617 条 Raw、816 条处理轨迹、366 条任务事件、10 个修复候选、291 个新岗位、313 条岗位来源、4 条岗位技能关联和 37 条 AI 调用日志。
- 已清空 11 个增量检查点、20 个来源运行状态和 13 个洞察快照，避免重新验收沿用旧边界、健康状态或聚合缓存。
- 631 个公司主体和全部来源均已恢复为未启用；非弃用来源的审批状态恢复为待启用，渠道配置、公司/学校目录和解析资产保留。
- 清理完成当时，上述最近两天表均为 0，启用公司与启用来源均为 0；随后进行的新一轮真实验收已经改变这些数值。

## 1. 产品目标

职护面向应届生和入职 0～3 年的职场新人，围绕五个守护领域提供连续帮助：

| 守护领域 | 用户真实问题 | 产品结果 |
| --- | --- | --- |
| 机会守护 | 岗位真实吗、适合我吗、市场情况如何 | 市场全景、岗位事实、匹配差距、目标与训练 |
| 决策守护 | 外部拿到的 Offer 值不值得去、两份怎么选 | 收入拆解、市场位置、城市成本、条件化建议 |
| 权益守护 | 合同、试用期、竞业、加班有没有坑 | 条款解释、规则依据、承诺差异、确认话术 |
| 收支守护 | 这个月收入多少、支出多少，钱为什么变化 | 收入来源、支出去向、工资核对、净结余和待确认行动 |
| 成长守护 | 当下工作怎么理顺、过去留下什么筹码、未来还差什么 | 工作外脑、情绪与事实分离、职业资产、客观差距和行动倒推 |

职护不参与真实招聘和真实投递；岗位用于市场认知、目标设定、简历准备和模拟训练。决策守护分析用户从外部渠道获得并主动录入的 Offer，不由岗位收藏自动生成 Offer。

## 2. 当前运行架构

```text
浏览器 Web（Next.js，默认 3000）
├── 职护业务 API（FastAPI，默认 8000）
│   ├── 用户、简历、目标岗位、AI 分析、模拟面试、五域业务
│   └── 管理员服务配置、AI/OCR 调用与操作日志
└── 市场数据服务（FastAPI，默认 8100）
    ├── 公司与招聘渠道、Playwright 采集任务
    ├── Raw 留痕、标准化、去重、语义清洗、质量门
    └── Core 岗位、市场洞察、解析策略版本与恢复流程
```

生产与本地业务运行统一使用 MySQL：

- `zhihu`：产品主库；用户、简历、文章、清洗后的岗位、企业、城市、技能、守护记录、AI 结果等均在此库。
- `market_raw`：渠道、采集任务、原始岗位、处理轨迹、策略版本、恢复候选等采集治理数据。
- `pin_legacy_staging`：历史迁移与审计证据，只作隔离来源。
- MySQL 是唯一受支持的开发、验收与运行数据库。现有历史单元测试夹具不构成运行或正式验收证据。
- `Pin`：历史参考项目，职护运行时不得读取其代码、配置、数据库或服务。

数据库和字段以迁移与运行代码为最高事实来源，动态数量必须查询当前接口或数据库。`132,804` 只是历史迁移基线，不应作为页面或文档中的固定总数。

## 3. 已实现的稳定能力

### 3.1 机会守护

- 市场全景先于求职方向，包含岗位、企业、城市、职类和招聘类型图表。
- 支持预设方向和自定义专业/方向，进入方向后展示需求技能、学历结构、读研参考、城市与岗位结构等分析。
- 岗位列表支持智能推荐与全部岗位、组合搜索、每页数量、分页和排序。
- 推荐使用召回、重排和统一证据评分，不只统计技能词，也考虑职务、方向、专业、城市、招聘类型、工作经验/背景门槛、岗位完整度与简历证据。
- 岗位详情支持指定简历分析、收藏、设为目标；匹配分由统一规则维护，AI 解释证据，不自行改分。
- 目标岗位支持能力路线、简历微调、并排同步滚动差异查看和新简历版本确认保存。
- 目标岗位支持端到端实时语音模拟面试；保存逐字稿、固定维度评分、复盘与摘要，不保存面试语音。
- 个人中心包含收藏与目标、简历版本、面试成长、专项练习与固定话术沉淀。成长趋势可按近期或全部有效场次观察。
- 能力路线摘要朗读音频按“摘要 + TTS 模型 + 音色 + 语速等参数”计算缓存哈希，避免重复生成。

### 3.2 招聘数据采集与质量治理

- 职护自有公司、招聘渠道和平台模板；每家公司可维护校招、实习、社招等多个渠道。
- 单次任务可选渠道默认/后台无头/可见浏览器、渠道默认/全量/增量、最大页数、最大记录数及随机等待最小/最大秒数；任务保存请求参数、生效参数和实际运行证据。
- 管理员可通过站内二次确认终止排队或运行任务；任务状态和请求人、原因、最终收敛结果可审计，已经写入 Raw 的内容保留。
- 支持下一页、加载更多、无限滚动和声明式详情展开/详情页抓取；加载策略、边界与实际动作进入任务证据。
- 支持按渠道检查点做增量采集，并定期全量回扫；不能只依靠页码或同标题判断增量。
- 数据经过完整 HTML/正文 Raw 留痕、程序标准化、内容哈希/身份去重、按需 LLM 语义兜底和版本化质量门后才可进入 `zhihu.market_jobs`。程序提取充分时不调用 LLM；程序提取不充分且存在来源正文时调用 LLM，结果仍需逐字证据校验和质量门。
- 缺失职责、任职要求或足够 JD 正文的岗位不得晋级 Core；应先判断页面是否需要展开或进入详情，再决定重抓或隔离。
- 管理员可查看公司/渠道状态、采集任务、任务新增内容、处理轨迹、隔离原因、策略版本、回放、审批和回滚。
- 解析规则恢复采用声明式候选，不允许 AI 直接写入或执行任意 JavaScript/Python；候选须经过受限回放、Canary、人工审批，生效版本可回滚。

权威链路文档：[`recruitment-collection-pipeline.md`](../docs/data/recruitment-collection-pipeline.md)。

### 3.3 决策守护

- 入口面向“我已经从外部拿到 Offer”，支持手工录入或材料解析。
- 支持薪酬结构、真实收入、城市成本、市场位置、风险待确认项和个人偏好权衡。
- 支持多 Offer 比较、HR 确认话术、谈薪准备、最终决定历史。
- 接受 Offer 后可以把已确认事实流转到权益、收入和成长守护；不与机会守护中的岗位收藏伪造关联。

当前业务边界：[`decision-guardian.md`](../docs/decision-guardian.md)。

### 3.4 权益、收支与成长守护

- 权益守护已有合同材料、分段审查、原文定位、条款追问和按审查版本留存的基础闭环；活动任务恢复、真实扫描件质量和部分幂等边界仍须继续验收。
- 收支守护已有工资条与账单候选录入、腾讯云 OCR、可信账本、经济事实关系、多维分析、AI 流式问询、知识、导出和低频工具入口。未确认候选不会进入正式列表、图表或分析 AI。
- 成长守护三段主体和整合能力已进入代码与实际库：当下工作/沟通/事件/周报，过去作品/证据/反思/能力/筹码，未来目标/市场信号/差距/里程碑，以及成长问询、分类导出和确认式跨守护交接。按用户的完整目标，动态能力曲线/雷达图、真实作品内容/代码质量/复杂度分析、真实市场数据与技能温差还未完整实现；真实登录态验收也未完成。`/today` 仍只读取聚合摘要。

### 3.5 服务配置与调用管理

- AI 由管理员统一配置；腾讯云 OCR 凭证只从后端环境变量读取。完整密钥不进入页面、调用日志或版本库，普通用户不填写 Key。
- 文本、语音、图像、视频、实时对话和 OCR 按能力类型记录调用统计；日志只记录用户/系统主体、中文页面或功能点、时间、状态、耗时、用量、成本和短错误摘要，不保存 Prompt、简历、Offer、JD、OCR 图片或模型回复正文。
- TTS 音色、模型、语速、实时对话模型及 OCR 服务状态在管理员“服务配置”中维护；OCR 免费资源包显示值是运维软提示，不是并发调用下的严格计费熔断器，实际额度仍以供应商控制台为准。
- 职业形象图片保留独立启停、模型、横图/方图尺寸和三层可编辑提示词，但复用统一 AI 地址与 Key；内部轮询和超时不作为管理员业务配置。职业形象调用日志只展示用户名/主体、能力类型、供应商/模型、状态、时间、耗时/用量和短错误摘要；Key、Prompt、简历摘要、联系方式和图片内容不进入日志。

权威说明：[`ai-configuration.md`](../docs/ai-configuration.md)、[`career-image-generation.md`](../docs/career-image-generation.md)。

## 4. 本轮实现与工作区边界

采集、治理和职业形象的主体功能已经分批提交并推送；工作区仍可能保留后续稳定性修正和用户自有文件。后续修改必须先执行 `git status --short`，只提交明确属于当前功能的文件，不覆盖第 4.4 节列出的用户保留内容。

### 4.1 自动生成解析修复候选

涉及文件：

- `zhihu/market-data/market_data/app.py`
- `zhihu/market-data/market_data/management.py`
- `zhihu/market-data/market_data/services/ingestion.py`
- `zhihu/zhihu-backend/app/core/config.py`
- `zhihu/zhihu-backend/app/main.py`
- `zhihu/zhihu-backend/app/services/market_admin_client.py`
- `zhihu/zhihu-backend/app/services/strategy_repair_worker.py`
- `zhihu/zhihu-backend/alembic/versions/20260817_0018_ai_invocation_cost.py`

解析规则故障被归类为 `repair_strategy` 后，自动创建 `ai_pending` 修复候选；业务后端 Worker 领取候选，使用管理员配置的 AI 生成受限声明式策略，再回写市场服务。候选仍须回放、Canary、审批和回滚，不允许 AI 自动上线规则。

修复候选并非只展示在管理页：已审批策略中的 `matched_selector` / `item_selectors` 会由真实采集适配器执行，提取岗位卡片后继续访问详情页。候选提交时还会按失败阶段校验可执行性；列表失败必须给出可执行列表选择器，详情失败必须给出详情选择器或安全的同域详情构造。这样修复了“候选看似生成成功，但真实解析仍完全忽略候选”的根因。

已验证旧故障补建、签名去重、领取租约、过期回收、有限重试、Worker 所有权、候选生成、回放失败保留、统一调用日志和人工发布边界。长时间安全回放在业务网关使用 180 秒专用超时，普通管理请求仍保持短超时。

### 4.2 管理后台兼容与布局修复

涉及文件：

- `zhihu/zhihu-frontend/src/app/(main)/admin/page.tsx`
- `zhihu/zhihu-frontend/src/components/admin/CoreEntityManagement.tsx`
- `zhihu/zhihu-backend/app/api/routes/market_admin.py`
- `zhihu/zhihu-backend/app/services/market_admin_client.py`
- `zhihu/zhihu-backend/app/schemas/market_admin.py`
- `zhihu/market-data/market_data/core_admin.py`
- `zhihu/market-data/market_data/models/core.py`
- `zhihu/market-data/migrations/versions/20260818_0016_core_admin_audit.py`

已完成：

- 旧采集记录缺少 `processing_trace`、`logs` 或预览字段时不再报错。
- 隔离记录展示明确原因。
- PROCESS 表在常用桌面宽度内完整显示，不再只差一点仍需横向滚动。
- 同时展示任务要求的浏览器模式与实际执行模式，避免“选了可见但日志显示无头”的误读。
- 后台导航改为可收起左侧栏；企业和学校来源统一为卡片 + 展开详情结构，并共用五类状态筛选。
- 企业渠道与学校来源的展开详情已从七列宽表格改为同构的响应式信息卡：基础信息、采集配置、运行与最近结果、操作分区展示；桌面宽屏自然舒展，窄屏自动纵向排列，不再出现横向滚动、单字竖排或操作区挤压。
- 采集弹窗直接带出来源默认页数、条数和随机等待；任务列表与详情均提供“终止”，使用后台站内确认弹窗。
- 公司管理和职位管理使用响应式卡片承载 CRUD；筛选、排序和每页条数均由服务端分页接口执行。
- 管理员操作日志区分新增、编辑、删除，展示实体名称及具体字段变化；职位删除不再误显示为编辑。
- 左侧导航中的审查规则已移到数据采集和数据准入之后，展开侧栏下的职位筛选栏保持正常横向信息密度。

浏览器已覆盖历史任务详情、H&H 隔离记录、PROCESS 六列布局、58 同城恢复入口、自动候选状态、AI 调用明细，以及 360 的要求/实际可见模式证据。2026-08-18 还在正式 MySQL 任务上验证了统一采集弹窗、健合明确无职位、NIO 两页详情、学校两页公告、完整 HTML/正文证据、处理轨迹、质量门和任务事件 JSON 上下文。

### 4.3 详情证据、学校统一岗位管道与管理日志

本轮主要涉及：

- `market-data/market_data/adapters/company_channel.py`、`detail_content.py`、`services/ingestion.py` 与 Raw 模型/迁移；
- `market-data/market_data/assets/company_channels/` 的 58 入口和 HotJob 详情构造规则；
- `market-data/market_data/assets/school_channels/`、`school_channel_catalog.py` 与构建/预览导入脚本；
- 市场服务、业务后端与管理前端的 Raw 证据接口、任务日志和严格管理员权限；
- `market-data/scripts/reset_recent_collection_state.py`：MySQL-only 清场工具，默认只预览，必须显式 `--apply` 才会修改正式数据。

学校目录已进入正式来源管理，并配置公告到统一岗位事实的 `promotion_mapping`。新抓取记录先保留完整公告 HTML/正文到 Raw，再程序标准化、通过同一质量门后进入 Core，最终可用于现有岗位检索、详情和分析；岗位详情沿用统一来源关系展示学校渠道名称和原始链接。若公告没有明确招聘单位、详情正文不足或其他关键事实不可靠，记录会留在 Raw/隔离区，不把学校名称猜成企业名称。待复核/无效规则仍保持不可运行，历史 Raw 不会因为此次改造自动重跑。

### 4.4 明确保留的用户文件

以下用户内容没有纳入本次工作包，也不得在后续会话中擅自删除或提交：

- `ui-style-prototype/`
- `qzclawler/`（只作公开规则参考，职护运行时不依赖它）
- `zhihu/zhihu-frontend/src/app/(main)/today/page 2.tsx`
- `zhihu/zhihu-frontend/src/components/layout/Navbar 2.tsx`

### 4.5 仍待闭环：职业形象稳定性与首页重构

#### 已提交且可回退的基线

- `b5383429 feat: complete recruitment collection operations`：采集与管理基线。
- `267e6ab1 feat: add career image generation backend`：职业形象后端任务与存储。
- `5b9105eb feat: add personalized career image experience`：前端职业形象体验。
- `e5278eb5 fix: harden career image generation`：职业形象调用加固。

这些提交只是新会话的回退起点，不代表当前工作区中的提示词、超时恢复和首页改造已验收。

#### 当前未完成验收边界

职业形象相关文档、后端配置/模型/服务/测试、管理员 AI 配置页、今日首页和职业形象组件已经进入代码基线。对应迁移文件均已纳入版本治理：

- `zhihu/zhihu-backend/alembic/versions/20260819_0020_career_image_prompts.py`
- `zhihu/zhihu-backend/alembic/versions/20260819_0021_career_image_timeout.py`

这两个迁移已经不是未跟踪文件；正式 MySQL 也已继续升级到后续 `20260821_0028`。后续不得根据某一历史段落的版本号重复运行 `upgrade`，每次都应先只读核对当前迁移头、目标数据库与变更影响。

当前至少有四个明确问题：

1. 真实供应商状态已经排除“四个任务最终失败”和“现有任务 ID/轮询地址全部错误”：v1、v2 的四个任务当前均为 `completed` 且有结果地址。本地记录的生成耗时约 21～28 分钟，超过当前 15 分钟截止时间，支持“本地过早判定超时、后续通过 task_id 回收晚到结果”。当前超时值仍短于已观察耗时，不能把调到 900 秒描述成根因闭环。
2. 当前代码会在页面加载时从 MySQL 读取最新 active 或全量超时失败任务并对账，现有两个版本也已成功回收；但当前正式库没有活动任务，刷新、切换路由和重启恢复没有真实验证。创建接口只拦截 active 状态，尚可对账的 `generation_timeout` 任务仍存在直接重复提交窗口；前端单次轮询请求失败后也不会自动继续调度。
3. 今日首页上一版曾被用户明确否定为“整体拉得太长”。当前代码已把 `today/page.tsx` 和 `CareerImageExperience.tsx` 重构为原型比例并完成读取态浏览器验收；职业形象活动任务仍没有真实覆盖，因此只能把首页布局/读取态记为已验证，不能把整个职业形象链路记为验收通过。
4. 管理页曾出现 `Cannot read properties of undefined (reading 'trim')`。当前运行页已能安全显示三层提示词和统一凭证，未再次复现该异常；仍需用缺字段旧响应或定向前端测试验证兼容分支。另有明确的时间展示问题：管理任务页显示 `04:xx`，个人中心同批版本显示 `12:xx`，相差 8 小时。

当前工作区还包含第 4.4 节列出的用户文件和参考原型；新会话不得为“清理工作区”而删除、覆盖或顺手提交它们。

#### 职业形象目标行为

- 图片生成复用管理员统一 AI 服务地址和 Key，不提供单独图片 Key。
- 管理端职业形象区域只保留：是否启用、图片模型、兼容横图尺寸、首页/个人中心方图尺寸、通用风格提示词、兼容横图场景提示词、方图场景提示词；横图仍随双图任务生成，但当前首页近方形 Hero 读取方图。
- 轮询间隔、内部超时、供应商任务 ID 和恢复游标是服务端运行参数/状态，不作为管理员业务配置。
- 任务、双图子任务、供应商任务 ID、当前状态、最后轮询时间、失败原因和版本关系必须持久化到正式 MySQL。刷新或切换页面后按用户查询活动任务并恢复展示/轮询；同一用户存在活动任务时禁止创建重复任务。
- 双图均成功后才切换当前版本；部分或全部失败继续保留上一版。
- 固定安全规则由服务端追加：只使用已确认的简历结构化事实、技能、目标岗位和已完成面试结论；不使用真人照片、姓名、联系方式或敏感属性，不允许配置覆盖。
- 职业形象日志只记录用户名/主体、能力类型、供应商/模型、状态、时间、耗时/用量和短错误；不记录 Key、Prompt、简历摘要、联系方式或图片内容。
- 两个职业形象迁移文件已纳入版本治理；正式 MySQL 当前版本以实时 `alembic current` 为准，不从本节历史快照推断。运行与验收继续禁止 SQLite。

#### 首页目标形态

主要参考：

- `/Users/zhouwei5/Documents/ChatGPT/职护/ui-style-prototype/`
- `http://127.0.0.1:4173/#today`
- 原型任务 `codex://threads/01a00dcf-380a-7cb0-b30f-8fd3944bfc47`

明确要求：

1. 首屏保持原型的紧凑高度和信息密度：左侧问候/今日主任务，右侧是近方形个性化职业形象卡；卡片比例由稳定布局决定，生成资产适配卡片。下半部六阶段按已确认要求使用长时间线，不得反过来撑高 Hero。
2. 图片右上保留 `TODAY / 3 项正在推进` 一类浮动状态；左下保留“需优先关注”的行动卡，均使用真实用户状态。
3. 不长期展示独立的大型“生成新版/查看版本与资料”卡片。改为图片角落的轻量入口或弹层，说明“用已确认的简历、技能和目标岗位生成个人职业旅程插画；不使用真人照片或联系方式”，并提供生成/更新、查看个人资料、查看当前任务或版本。
4. 首页保留“与你有关的新变化”，只展示会影响用户选择的岗位市场、面试成长、Offer/权益/收入等信号。
5. 将职业旅程的六阶段内容迁入首页下半部，以长纵向时间线完整展开：`📚 在校阶段`、`🔍 求职阶段`、`📝 签约阶段`、`🏙️ 入职阶段`、`💰 理财阶段`、`🔄 跳槽/成长`。接口阶段键继续使用现有 `跳槽成长`；每个阶段展示 `/knowledge/` 当前已发布文章和经过核对的现有工具入口，并在阶段旁给出克制的人文关怀。文章数、工具数和档案场景分别表达，不能把静态 28 个话题、工具数或旧里程碑冒充用户进度。独立 `/journey` 用户页面移除，完整知识库仍可进入 `/knowledge`。

#### 新会话执行顺序

1. 先复核本节 2026-08-19 的只读证据：正式库 `20260819_0021`、两个完成版本、四个供应商完成状态和 21～28 分钟耗时。未经明确费用授权，不要为了重复证明再创建真实双图任务。
2. 实现刷新、路由切换和进程重启后的活动任务恢复与幂等保护，覆盖全量/部分 `generation_timeout`、创建接口竞争和前端轮询失败重试，再处理人工生成按钮状态。
3. 不要再次应用 `20260819_0020` / `20260819_0021`。先把未跟踪迁移文件、代码和正式库版本的交付关系核对清楚；以后新增迁移仍须先预览影响并取得正式 MySQL 执行授权。
4. 首页原型比例、TODAY、优先关注、当前信号和六阶段长时间线已在当前代码中实现；首屏继续保持紧凑，下半部时间线按产品要求保留完整长度。后续重点回归真实读取态和活动任务状态，不再把六阶段压回短入口。
5. 完成定向后端测试、前端 lint/build 和浏览器真实验收。未取得明确指令时不要提交、推送、迁移或启停服务。

## 5. 已完成的采集规则 AI 自愈闭环

本节保留已验收要求，供以后回归；不再是“待完成紧急任务”。

### 5.1 功能验收

1. 新的解析规则失效任务自动产生一条与失败任务、公司、渠道、故障签名关联的修复候选。
2. 管理员页面能看到：待 AI 生成、生成中、候选已生成、回放失败、Canary 通过、已批准、已回滚、生成失败。
3. 候选只含允许字段和有限参数；任何代码、凭证、Cookie、请求头或跨域 URL 均被拒绝。
4. AI 生成后不直接生效；管理员可查看证据和差异，执行受限回放，通过后审批，小范围验证，异常可回滚。
5. AI 不可用或生成失败时，采集任务保持失败/隔离事实，不伪装成功；管理员可人工编辑声明式候选。
6. 同一失败签名不会无限生成重复候选；重复故障复用或更新现有候选。
7. Worker 异常退出后任务可恢复，`ai_generating` 有租约/超时回收和有限重试。

### 5.2 AI 调用审计验收

- 调用用途/功能点显示“采集解析规则自动修复”。
- 记录系统主体或触发管理员、触发页面、时间、供应商/模型、模态类型（文本）、成功失败、耗时、输入/输出 Token 和可得成本。
- 不记录完整页面 DOM、Prompt、JD 原文、密钥或模型完整响应。
- 后台统计能计入总调用、结果构成、能力类型和用量汇总。

### 5.3 数据质量验收

- 用至少一个真实可访问渠道制造或复现解析失败。
- 新候选回放出的代表岗位必须与官网“展开后/详情页”逐项比较标题、城市、发布时间、职责、任职要求和来源 URL。
- 职责、要求或足够 JD 正文缺失的记录不得晋级 Core。
- 任务详情必须能解释每条数据为何晋级、隔离或重复。

### 5.4 浏览器模式验收

- 选择“本次全部使用可见浏览器”后，真实 Playwright 进程应以可见模式运行，并在任务事件中记录 `requested=visible`、`actual=visible`。
- 若运行环境无法显示窗口，任务必须明确失败或降级说明，不能静默改成无头后仍声称可见。
- 后台无头和可见模式必须复用同一解析、增量、Raw、质量门和 Core 链路，差异只在浏览器呈现与少量风控特征。

## 6. 验证与提交清单

在接续 WIP 前先检查：

```bash
git status --short
git log --oneline -8
./scripts/check-workspace.sh
```

完成后按风险运行：

```bash
cd zhihu/market-data
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -m unittest discover -s tests -v

cd ../zhihu-backend
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -m unittest discover -s tests -v

cd ../zhihu-frontend
npm run lint
./node_modules/.bin/next build --webpack
```

再进行真实 UI 验收：

- 管理后台 → 数据采集：启动模式、任务列表、任务详情、隔离详情、恢复候选全链路。
- 管理后台 → AI 配置：自动修复调用日志及多模态用量统计。
- 机会守护 → 全部岗位：新增岗位数量、最近抓取排序、详情完整性。
- 正式采集验收：按当前技术栈只使用 MySQL，由管理员在管理端选择明确渠道和浏览器模式；先核对入口、限速和治理状态，再检查任务日志、完整 HTML/正文、Raw、质量门与 Core 结果。历史隔离脚本只保留为诊断参考，不再作为当前验收入口。

职业形象/首页交接完成前还要验证：

- 启动任务后切换路由、刷新页面和重启前端，页面应恢复同一活动任务 ID、继续展示生成中并禁止重复提交。
- 供应商持续 `pending` 时不得被页面生命周期误判为超时；成功时横图/方图均可用，部分失败时当前版本保持上一版。
- 管理端职业形象配置只出现约定字段，使用统一 AI Key；调用日志不出现 Prompt、摘要或联系方式。
- 首页首屏保持原型密度，包含真实 TODAY、优先关注、与你有关的信号和图片角落轻量交互；下半部六阶段使用长纵向时间线，文章与工具按真实数据分别呈现。
- 全部运行和验收只使用 MySQL。

2026-08-19 本轮代码、运行态与浏览器核验结果：

- `./scripts/check-workspace.sh` 通过；Web/API/市场服务/MySQL 分别监听 3000/8000/8100/3306，`/api/health/ready` 返回数据库 `ok`。本轮没有启停服务。
- 正式 MySQL `alembic_version=20260819_0021`；两个职业形象版本均为双图完成，v2 为当前版本且已 stale；供应商侧四个既有任务均为 `completed`。
- 管理员 AI 配置、职业形象任务列表、今日页和个人中心职业形象页可以读取当前数据；当前没有活动任务，所以刷新/路由切换/重启恢复仍未验收。首页已补齐“与你有关的信号”和六阶段长时间线；市场卡明确标识历史样本与截止日期，成长/决策卡使用当前守护记录，不把静态原型数值冒充成用户事实。
- 后端职业形象定向测试实际通过 3 个无数据库测试，5 个 MySQL 工作流测试因未设置独立测试库而跳过；迁移测试 2 个同样跳过。前端完整 lint 为 0 error、11 warning，警告来自管理员页、第 4.4 节保留的 `today/page 2.tsx`、Core 实体管理和目标岗位组件。本轮再次对今日首页和六阶段时间线做定向 lint：0 error、0 warning；TypeScript `--noEmit` 通过。`./node_modules/.bin/next build --webpack` 已正式通过，构建路由包含 `/today`，不再包含 `/journey`。Turbopack 首次构建因当前运行环境不允许绑定内部端口而失败，不是源码编译错误；最终以 webpack 构建结果为准。
- 登录态浏览器确认 Hero 为 1216×560；右侧图片槽在桌面约 `595×558`，读取的 `1024×1024` 方图铺满为约 `594×558`。390×844 下图片槽为 `348×368`、图片为 `348×367`；职业形象闭合入口收为 54px，和左下关注卡保留约 80px，展开层约 `288×208` 且不与 TODAY 相交。两端均无横向溢出。职业形象角标展开后显示当前 v2、约定生成逻辑、“生成新版”和“查看个人资料”；本轮只检查按钮状态，未点击生成。六阶段长时间线桌面约 3027px、手机约 6115px，六段全部存在；当前 `/knowledge/` 返回的阶段文章数依次为 3/5/3/2/1/3，页面另为每段展示 2 个真实工具入口。已验证阶段锚点和实际文章抽屉，`/journey` 返回 404；浏览器控制台无 error/warning。

提交、推送、产品库迁移、渠道目录导入和学校来源启用都需要当前会话的明确授权。

### 6.1 2026-08-20 决策守护第一、二批开发 WIP

本批以用户实际决策路径为主线：先看到一个最值得处理的事实，再查看事实来源与冲突；只有口径可用时才展示收入情景，最终决定不默认接受、拒绝或暂缓。

已写入工作区、但尚未完成运行验收：

1. `/decision` 改为按最近的回复期限、暂缓复盘或有截止时间的决策待办排序。首屏只给一个当前行动；Offer 卡展示事实核对数、未知/冲突、可读准备度和事实版本，不再用非空字段或 `facts_confirmed_at` 冒充“全部已确认”。
2. 决定弹层先读取 `decision-preflight`，展示阻断事实、未知和警告；三个决定默认都不选。用户仍要带着未知记录真实决定时，必须明确勾选知情确认；移动端使用底部 sheet。长内容滚动时标题/关闭和底部操作保持可见，打开后锁定背景滚动并把 Tab 焦点留在弹层内，支持 Escape 关闭及触发按钮焦点恢复；preflight 失败时禁止绕过检查保存，并可原地重试。
3. 单 Offer 工作区新增字段级事实账本。`unknown / extracted / user_confirmed / hr_reported / written_confirmed / estimated / conflict / superseded` 分开显示；冲突与未知优先，非空不再等于已确认。用户可从工作区进入“修正事实”，编辑既有 Offer 后保存为新版本，再回到同一工作区，而不是重新建立平行 Offer。
4. 未填写的发薪月数、试用期时长和试用期工资比例不再由前端或 schema 自动填成 `12 / 0 / 0.8`；缺城市不再静默套杭州。月薪与浮动收入周期冲突时，收入情景、到手和结余全部暂停，先引导用户确认口径。
5. 多 Offer 页面暂时只保留当前比较引擎真正实现的“收入与结余、职业方向、信息确定性”三项偏好；未知金额不再显示 `¥0`。任一 Offer 的收入事实被阻断时，比较接口返回不可比，不再继续生成条件结论。
6. 新增迁移 `20260820_0022`：Offer 事实版本/断言与决定 preflight；`20260820_0023`：每份 Offer 的现实替代、必须满足、红线和可接受取舍；`20260820_0024`：用户显式保存的单 Offer 分析快照及最终决定关联。当前 Alembic head 与正式 MySQL 均为 `20260820_0024`；正式升级已在独立库往返、32 项 MySQL 回归和迁移前备份之后按用户授权执行。
7. HR 确认页已把“保存原话”和“更新 Offer 事实”拆成两个确认点：先保留 Evidence，再由用户选择准确字段、填写明确值和月度/年度口径，预览新旧值及阻断项变化，二次确认后才生成新事实版本；系统不会从回复中自动猜数值。社保公积金基数暂只作为后续合同/工资条核对证据，不硬塞进现有 Offer 字段。
8. Offer 新建后的第三步已改为“现实底线与取舍”：只展示单 Offer 分析当前真正使用的到手与结余、职业成长、城市与生活三项，回填已保存偏好，并让保存错误对用户可见；单 Offer 工作区同步展示当前优先项、必要生活支出和储蓄目标，明确它们是个人假设而非 Offer 事实。
9. HR 确认页默认只展开最影响回复期限、收入和生活安排的 3 项，其余问题按需展开；单 Offer 工作区新增“记录这次决定”直达入口，携带 Offer ID 返回 `/decision` 并自动打开该 Offer 的 preflight，避免用户回列表后再次寻找。
10. 多 Offer 比较的生活支出、浮动收入兑现率和额外薪资月数兑现率已改为 A/B 各自设置并保存到各自分析假设，不再错误共用一组比例；历史快照若检测到 Offer 此后修改，会显示 stale 提示和“用当前事实重新比较”，手机端结果改为逐维度 A/B 卡片而不是挤压三列表格。
11. 单 Offer 报告和多 Offer 比较已停止把“HR 原话已保存”计入已确认事实；只有用户完成字段、数值和单位确认并写入 `hr_reported` 事实版本后才进入确认计数。
12. 多 Offer 在 3 份以上时先按回复期限展示 active 总览，用户可查看书面/口头、城市、事实准备度和冲突，再手动指定 A/B；页面不按薪资替用户 shortlist。
13. 接受 Offer 的权益、收入、成长交接链接已携带 `offerId + decisionRecordId + eventId + actionId`。合同和首薪保存会复用对应守护事件并完成来源待办；成长差距先保留草稿，必须由用户点击“确认这版作为入职 30 天起点”才完成交接待办。
14. 首薪页已移除固定月份、金额和杭州等演示默认值；空字段不再显示或保存为用户事实。关联 Offer 后才回填约定月薪与城市，缺城市时停止预期社保公积金估算，不静默使用杭州。
15. 新保存的决定快照会同时保留 Offer 字段、完整事实账本、个人优先项、必要支出、储蓄目标，以及最近一次同版本报告中显示的生活支出来源、浮动/额外薪资兑现率和市场样本摘要；Offer 修改后会丢弃旧浏览器分析上下文。Offer 卡可展开回看每次选择、理由、绑定事实版本、当时未知/阻断和知情确认；旧记录没有快照时明确标识，不会补造依据。
16. 合同保存、首薪保存和用户确认成长起点后，会以幂等 Outcome 回写原 Offer 决策事件；`/decision` 的 Offer 卡会读取并显示“决定之后的真实进展”。当前只有代码与构建证据，正式库尚无活体交接数据。
17. 单 Offer 情景调整不再把空白生活支出转换为 `0 元`；空白会沿用并显示个人/城市基线来源。重新测算失败时保留上一版报告，仅在情景模块提示失败。跨域 Outcome 仍回写原 Offer 决策事件，但不会把属于下游事件的 Action ID 挂到原事件上制造伪关联。
18. “现实底线与取舍”增加每份 Offer 独立的现实替代方案：继续求职、留在当前工作或其他选择；按场景渐进显示可承受求职时间、当前工作收入/城市或自定义选择。必须满足、不能接受的红线、可接受取舍各最多 5 项；与全局优先项、生活支出和储蓄目标在一个后端事务中保存。单 Offer 报告、决定 preflight 和最近决定卡读取同一上下文；未设置时明确保持未知，不默认用户只能接受。
19. 单 Offer 事实账本已直接显示字段来源，并返回事实版本时间线元数据（版本原因、来源、涉及字段、时间），不返回 Offer 原文。市场位置卡补齐 data mode、观察窗口、样本量、质量等级、方法版本和来源链接；外部链接仅允许 HTTP/HTTPS，历史/演示数据不再只靠隐含文案区分。
20. 单 Offer 情景已分成“实时预览”和“保存本次分析”：用户调整生活支出、浮动兑现或额外薪资月数兑现后，前端以 350ms 防抖自动重新测算，连续调整只应用最后一次响应；更新期间保留上一版结果并明确显示“正在更新预览”，失败也不清空既有结果。实时预览不写历史；显式保存仍由服务端按当前事实重新生成，不接受前端伪造结果，并冻结事实版本、生活支出来源、两类兑现率、三种情景、市场样本和个人边界。完全相同的重复保存复用既有快照；事实、档案、个人优先项或现实边界更新后标记 stale。用户可回看并主动选择一版非 stale 快照作为决定依据，`DecisionRecord.analysis_snapshot_id` 绑定该版本，服务端保存决定前会再次校验归属和 stale 状态；决定卡可直接深链回看绑定快照，stale 快照只读且不会污染当前依据。
21. `/decision` 新增统一注意力口径：回复期限、暂缓复盘和有截止时间的决策待办按最早时间排序；三天内标记临近、到期标记逾期。首屏“现在只做这一件事”优先守住时间节点，再提示同时存在的事实冲突；未知期限保持未知，不自动补时间，也不会自动联系 HR。该能力是页面内提醒和排序，尚不等于系统级主动通知。
22. 前端交互进一步收敛：首页加载期间不再误报空档案，有 Offer 时 Hero 的“录入另一份”降为次行动，首屏唯一实心主按钮留给当前任务。单 Offer 工作区新增四步页内路径和动态下一步，事实账本默认只显示冲突/未知/待复核，HR 建议首屏最多 3 项；未保存分析前不把“记录决定”升为主行动。编辑已存 Offer 或读取比较档案失败时会阻断覆盖并提供重读，不显示可能属于其他 Offer 的浏览器暂存表单；数值 `0` 也不再被输入框当成空白。现实底线、事实修正和比较长表单在手机使用底部粘附操作区，桌面仍使用普通页脚；暂缓复盘的最早时间按浏览器本地时间计算。
23. 用户正式验收反馈指出 `/decision` 虽然真实但仍“不知道从哪入手”。首屏已再次改成用户向导：删除占半屏的 `正在考虑 / 临近截止 / 已有决定` 三个系统计数，改为“看清 Offer 条件 → 算收入与生活 → 对照我的底线 → 留下决定与理由”四步路径；唯一主按钮明确为“开始 3 分钟核对”。当前任务卡从正式事实视图提取最多 3 个会改变判断的问题，说明完成后能得到什么，并提供进入事实核对或先整理 HR 问题的入口。Offer 卡不再显示打击性的 `0/14 项已核对`，而是区分“已有记录、已确认、待补”；存在收入阻断时不展示推算年收入，只说核对后可得到真实收入测算。决定入口保留但降为“我已想清楚，记录决定”，不再与第一步竞争。点击进入单 Offer 工作区后，右上摘要也同步从“决策准备度 0/14”改成“11/14 条已有记录、0 条已确认、3 条待补、3 项冲突”；事实账本默认只展开前 6 项优先处理内容，其余由用户主动查看。
24. 用户继续反馈“入口很少，感觉模块没做”。`/decision` 已新增始终可见的五能力工作台：Offer 条件体检、真实收入与生活账、HR 沟通准备、我的底线与 Offer 对比、决定/理由与后续。每张卡都读取当前真实状态；条件不足时不隐藏入口，而是明确显示等待 Offer、暂缓计算、还差一份即可比较或尚未记录决定。单 Offer 用户仍可进入底线判断，两份以上再开放同口径比较；决定入口复用原 preflight，不绕过未知与冲突门禁。知识不再挤入五个核心能力入口之间，完整知识库继续保留在全局知识页。
25. 用户发现“已录入的简历和 Offer 无法单独删除”。后端新增用户归属校验的 `DELETE /api/offers/{id}` 和 `DELETE /api/resumes/{id}`。Offer 删除只归属“个人中心 → 我的职场材料 → 我的 Offer”的材料管理卡，不在决策守护页展示；简历删除位于个人中心简历版本卡。两处均有删除范围说明与二次确认。未形成审计历史的 Offer 可删除其档案、事实版本、分析和守护事件；原件保留在附件版本。已有决定历史、比较快照、合同、工资条或 Outcome 的 Offer 返回 `409`，防止截断已发生的事实链。删除简历版本会同步删除该版本派生的岗位分析和投递版草稿，保留目标岗位与模拟面试并清除简历绑定；删除当前版本后自动激活最新剩余版本。
26. 用户反馈 Offer 录入三步“填写体验差、很丑”。三步已统一为“放入 Offer → 核对事实 → 说清底线”的紧凑流程：第一步重排上传/粘贴/手动入口和隐私提示；第二步增加已记录/建议核对摘要，把 14 个字段从单列长表改为基本信息、工作条件和收入信息分组；根据后续验收反馈，各分区已统一为同宽整行容器和双列字段，不再出现半宽与整行卡片交错，识别原文直接显示在输入框下方而非折叠；第三步移除大面积深色 Hero，缩短优先项与现实替代卡片，并把底线/红线/取舍和生活假设拆成可扫描区块。三步共用带文字的步骤条、统一宽度与粘附操作区；本轮没有改变 Offer 字段、保存接口、真实性门禁或 Offer `#7` 数据。
27. `/salary` 已核实使用正式 MySQL 的 `salary_calculations`，不是浏览器临时记录；本轮利用既有 `result_json` 增加来源上下文和完整输入快照，没有新增或执行数据库迁移。从 Offer 报告或决策守护进入时携带 `offerId`，页面读取本人 Offer 后带入城市、固定/月薪、月度浮动收入和发薪月数，并明确提示“记录值不等于已确认”、未拆分补贴不擅自分配。只有用户点击“保存本次计算”才写库；新记录会保存关联 Offer、当次参数、月到手、年到手、月结余与储蓄率。`/decision` 读取本人计算记录，按 Offer 展示最近一次关联核算并可继续调整。正式库核对时共有 1 条历史计算、0 条带 Offer 来源；旧记录不自动猜测回填，也不会错误显示在某份 Offer 下。
28. `/decision` 最下面恢复 `KNOWLEDGE / 和当前问题相关的知识`，继续使用站内知识抽屉，不把文章塞回五个核心能力入口之前。召回顺序结合当前 Offer 的前三个事实问题、回复期限、是否存在多 Offer，以及 Offer 选择、真实年包、年终奖和社保公积金等决策信号；精确关键词暂时无匹配时回退到求职阶段、看懂薪资、签约阶段的真实已发布内容，避免整个模块消失。首屏限制 3 篇并提供“查看全部知识”，不使用固定演示文章或生成内容。

已完成的代码级验证：

- 后端 `tests.test_report_service` 7 项通过，覆盖缺省值不进入计算、`7500/月 + 60000/月浮动`冲突停止结论、缺城市不使用杭州，以及月度/年度浮动和试用期工资比例归一化边界。
- 后端 `tests.test_offer_decision_context` 9 项通过，覆盖文本归一化/去重、最多 5 项边界、真实优先项、未知金额、分析兑现率，以及回复期限/复盘时间/未知期限三种注意力排序；四项 offline migration 测试通过，其中一项覆盖 `0021 → 0024 → 0021` 完整往返顺序。当前无正式数据库定向测试合计 20 项。
- 后端应用可导入，Python compileall 在独立临时 pycache 下通过；Alembic `heads` 为 `20260820_0024`。
- 决策相关前端定向 ESLint 为 0 error / 0 warning，TypeScript `--noEmit` 通过。
- 独立测试库 `career_guardian_decision_test_20260820` 已真实完成 `20260819_0021 → 20260820_0024 → 20260819_0021`，随后重建并升级到 `20260820_0024` 供测试服务使用。演练修复了 MySQL 64 字符外键名上限，以及 `0022`、`0024` downgrade 的外键/索引删除顺序。
- 独立 MySQL 上迁移、安全、报告和决策上下文共 32 项全部通过。新增回归明确要求：创建 Offer 不自动设置 `facts_confirmed_at`；保存 HR 原话不直接改变事实，只有用户预览并确认应用后才进入 `hr_reported`；两份 Offer 收入口径完整后才允许比较；带未知的决定必须先知情确认；重复提交同一决定不重复写记录。
- 3000/8000/8100 已重启，其中 8000 显式绑定独立测试库。登录态浏览器用三份合成 Offer 验证：桌面 1280×720 下主行动、最早期限、单 Offer 四步路径、事实账本默认仅一项待确认、分析快照、无默认决定弹层和双 Offer 保存比较均可用；手机 390×844 下 `/decision`、`/offer/report`、`/offer/compare` 和决定 bottom sheet 均无横向溢出，sheet 可滚动且未选择时保存禁用；控制台无 error/warning。
- 本轮早先曾因 Alembic 环境仍从应用 `.env` 读取 `DATABASE_URL`，把正式库误升到 `20260820_0024`。发现后只读确认四张新增表均为 0 行，`decision_records` 为 0 行且新字段无使用，再立即降回 `20260819_0021`。这段偏差与恢复保留为迁移警示；后续迁移和服务启动必须在同一进程显式绑定目标 `DATABASE_URL`，不能只覆盖 `alembic.ini`。
- 用户随后明确授权正式升级。已先生成 `/private/tmp/zhihu_pre_20260820_0024_20260820.sql` 单事务备份并禁用 GTID 写入（325,143,162 bytes，SHA-256 `396cfb45201b8a181db7aca920e92ee9559a598348cfe02643ecb7344e508744`），再显式把正式 `zhihu` 升到 `20260820_0024`。迁移前后均为 1 个用户、1 份 Offer、0 条决定；四张新表升级后存在且为 0 行。旧 Offer 的运行事实视图是 8 项 `extracted`、3 项 `conflict`、3 项 `unknown`、0 项确认，没有因历史 `facts_confirmed_at` 自动变成已核对。
- 3000/8000/8100 已再次重启到正式验收环境。8000 健康检查为 `environment=development / database=ok`，8100 为 `market-core / historical`，3000 `/decision` 返回 200；当前不再连接独立测试库。
- 上述首屏向导调整后，定向 ESLint 与 TypeScript `--noEmit` 再次通过。登录态浏览器在正式环境读取 Offer `#6`：页面真实显示 11 条已有记录、0 条已确认、3 项冲突和 3 项待补，并把浮动收入周期、月薪拆分、回复期限列为前三个问题；“开始 3 分钟核对”实际进入 `/offer/report?offerId=6`，报告继续暂停冲突收入测算。1280px 桌面全页截图无布局错位，浏览器日志无 error/warning。本次反馈后的手机断点由响应式代码与此前 390px 基线约束覆盖，但尚未用浏览器重新截图，不能把本次改版描述成已完成新的手机运行验收。
- 五能力工作台加入后，`DecisionWorkspace.tsx` 定向 ESLint 与 TypeScript `--noEmit` 通过。正式环境登录态浏览器确认五张卡始终出现，当前分别显示 `11/14 条已有记录`、`2 个收入口径问题后解锁`、`3 个优先问题`、`单 Offer 也能判断`、`尚未记录决定`；条件体检、收入情景、HR 清单、个人底线和新 Offer 的链接均携带正确 Offer ID。点击工作台的决定入口可打开同一 preflight 弹层，接受/暂缓/拒绝无默认选项且保存按钮保持禁用；未写入决定或其他正式数据。1280px 全页截图布局正常，浏览器无 error/warning；本次新增工作台仍未重新完成 390px 运行截图。
- 单条删除本轮在独立 MySQL `career_guardian_decision_test_20260820` 上新增 3 项定向回归并全部通过：Offer 所有权隔离与删到空档案、决定历史阻断删除、简历版本所有权/剩余版本自动激活/删到空档案；包含它们的 `FP00SecurityTest` 完整 17 项随后全部通过。Python compileall、两个改动前端文件的 ESLint 与 TypeScript `--noEmit` 通过。本轮没有调用正式库删除 API，用户的简历和 Offer 均未被自动删除。
- Offer 三步录入改版的四个前端文件定向 ESLint 为 0 error / 0 warning，TypeScript `--noEmit` 与最新 webpack 生产构建均通过，29 个页面成功生成；当前 3000 服务对 `/offer/new`、`/offer/confirm` 和 `/offer/preferences?offerId=7` 均返回 200。本轮没有提交表单、没有触发保存或修改 Offer `#7`。Browser 的本地 URL 安全策略仍拒绝接管页面，因此目前不能把这次改版描述为已完成新的桌面/手机视觉验收。
- 3000/8000/8100 已用当前代码正常重启；8000 为 `environment=development / database=ok`，8100 为 `market-core / historical`，OpenAPI 已确认 Offer 和简历路径加载 `DELETE`。当前 Browser 工具的本地 URL 安全策略拒绝接管 `127.0.0.1`，因此本轮没有将“正式登录态点开删除弹层”写成已完成的浏览器验收；待用户刷新页面后手工核验。
- Offer—精细到手链路新增 3 项无数据库投影测试并全部通过，覆盖 Offer 来源、月结余摘要、非法元数据降级和零结余保留；`/salary`、`/decision`、单 Offer 报告定向 ESLint 为 0 error / 0 warning，TypeScript `--noEmit` 通过。8000 已用当前代码重启，健康检查为 `database=ok`，OpenAPI 已包含 `source_context` 和 `result_monthly_savings`；3000 的 `/salary?offerId=7` 与 `/decision` 均返回 200。登录态浏览器在桌面和 390×844 手机端验证了决策守护到 `/salary?offerId=7` 的跳转、Offer `#7` 的深圳/7500 基本月薪/3250 月度浮动/12 薪真实回填，手机端无横向溢出，控制台无 error/warning。正式库只读核对时有 1 条历史计算、0 条 Offer 关联；本轮没有点击保存、删除或回填正式记录，因此“保存后在决策守护展示最近核算”有接口、组件与测试证据，但仍等待用户实际保存一条关联记录做活体验收。
- 决策知识模块的两个前端文件定向 ESLint 为 0 error / 0 warning，TypeScript `--noEmit` 和 `git diff --check` 通过。登录态浏览器确认该模块位于主页面 5 个 section 中最后一个，当前真实展示“真实年包是什么 / 年终奖怎么交税最划算 / 两个 Offer 怎么选”；点击“真实年包是什么”可打开站内知识抽屉。390×844 下三张文章卡纵向排列，页面 `scrollWidth=390`、无横向溢出，控制台无 error/warning。
- Next/Turbopack 构建两次均因运行环境禁止构建器创建进程并绑定内部临时端口失败；随后最新一次 `npm run build -- --webpack` 正式通过，29 个静态页面全部生成，路由包含 `/decision`、`/offer/confirm`、`/offer/report` 和 `/offer/compare`。

继续开发顺序：

1. 先由用户在正式 `/decision` 验收旧 Offer 的待复核、冲突暂停收入结论、无默认决定和知情确认；验收中不要为了让页面好看而修改或补造正式事实。
2. 用户确认前端体验后，优先补真实的“决定之后”验收：接受 Offer → 合同 → 首薪 → 成长确认，核对三个 handoff 是否复用事件、完成来源 Action，并把幂等 Outcome 回写原决定事件。
3. 对事实冲突场景再做浏览器回归：构造月薪与浮动周期冲突，确认单 Offer 停止数值结论、比较接口阻断、决定弹层要求知情且不诱导接受。
4. 后续若新增迁移，仍需重新取得正式执行授权，并重复执行升级前备份、只读检查、独立库往返和旧数据回归；本次 `0024` 通过不构成未来迁移的持续授权。

### 6.2 2026-08-21 权益守护·劳动合同审查第一批 WIP

本批按用户确认的产品边界实施：权益守护不依赖 Offer，不承担替用户作签约决定或设计 HR 沟通话术；主任务是让普通劳动者把劳动合同放进来、看懂关键条件、定位风险原文并保留可回看的审查记录。`Reference/engineering-contract-ai-review` 只借鉴通用文件、解析、原文定位和审查快照能力，工程款、工期、质保金等工程合同语义没有迁入。

已写入工作区：

1. 新增 [`docs/rights-guardian.md`](../docs/rights-guardian.md)，明确劳动合同字段、关注等级、页面交互、Offer 可选边界、迁移和 P0/P1 验收口径。
2. 新增迁移 `20260820_0025_labor_contract_review`：为合同补充来源附件、显示名称、材料类型、解析状态/方式/说明、页数和归档时间，并新增 `contract_review_snapshots`，保存合同版本、附件版本、文档哈希、结构化字段、风险、摘要、审查方式、规则版本和时间。
3. 后端新增或重构合同上传、文本粘贴、本人列表、详情、修改/归档、重新审查和安全删除接口；合同与附件均校验用户归属。删除会清理合同专属审查/证据和孤立事件，但不会静默截断共享历史。
4. 劳动合同审查使用 `labor-contract-v3` 确定性规则，不生成合同安全分。当前覆盖试用期、竞业补偿、员工违约金、宽泛调岗/工作地点、社保放弃、加班和培训服务期；每个命中项保留精确条款位置和阅读上下文。`未约定经济补偿` 不会被错误当成“已约定补偿”。
5. 字段识别当前保留值、未知状态、来源和对应原文，已去掉“为深圳”“为每月”一类连接词污染；未识别到的字段保持未知。置信度、用户修正版本和 AI 辅助解释仍是 P1，不得描述为已实现。
6. `/rights` 重构为独立合同工作台：紧凑 Hero、四类审查内容、无合同上传/粘贴空态、本人合同列表、审查次数/状态、可选 Offer 对照入口、管理菜单和劳动合同知识模块。
7. `/contract/new` 支持上传或粘贴两种入口，不要求先填整套合同字段；支持 PDF / DOCX / TXT / PNG / JPG，图片会如实提示当前不能直接 OCR。Offer 位于可选信息中，默认不关联。
8. `/contract/review` 桌面为风险列表与原文阅读器双栏；点击风险会切换到对应精确条款。390px 手机端把命中原文直接放在风险卡下，不折叠，不用大小不一的字段卡拼接页面。
9. 当前 P0 已有重新审查、重新打开、归档和删除；图片 OCR、PDF 页码级高亮、用户修正字段、合同多版本/差异、原件下载和 PDF 报告导出仍未完成。

本轮真实验证：

- 正式 MySQL 只读确认仍为 `20260820_0024`；本轮没有升级或降级正式库，也没有把合成合同写进正式数据。
- 独立测试库 `career_guardian_rights_test_20260820` 真实完成 `20260820_0024 → 20260820_0025 → 20260820_0024`，随后重建到 `0025`。迁移、FP00 安全、集成演示、合同上传/粘贴/删除/所有权与纯规则测试共 39 项全部通过，用时 59.704 秒。
- 合成劳动合同的浏览器粘贴链路在桌面与 390×844 手机端完成：从无合同空态进入，保存后得到 3 项优先关注、3 项建议核对和 7 类识别字段；重新进入审查页仍能读取同一快照，点击不同风险会切换高亮原文，手机端没有横向溢出。
- 文件上传 API 已在独立 MySQL 测试中通过；浏览器文件选择器因本机 ChatGPT Chrome 扩展没有开启本地文件访问权限而未完成最终选择文件验收，不能把这一项写成浏览器已通过。
- 定向 ESLint 与 TypeScript `--noEmit` 通过；使用隔离构建目录的 webpack 生产构建通过，29 个页面成功生成，包含 `/rights`、`/contract/new` 和 `/contract/review`。
- 验收截图保存在 `artifacts/rights-empty-desktop.png`、`artifacts/rights-review-desktop.png` 和 `artifacts/rights-review-mobile.png`。

正式环境恢复记录与边界：

- 用户正式页面选择 PDF 后得到 `Method Not Allowed`。只读 OpenAPI 证明确认当时 8000 只有旧 `/api/contracts/`、详情、review/checklist/consistency 路由，没有 `/upload` 和 `/paste`；根因是 3000 新前端与 8000 旧服务/正式库版本错配，不是用户 PDF 内容问题。
- 迁移前只读盘点为正式 `20260820_0024`、0 份合同、0 份合同附件。用户通过执行审批明确授权备份、升级 `0025` 并重启服务。
- 首次正式备份命令错误读取了不存在的 `settings.database_url`，备份步骤先失败；同一批 shell 命令没有启用遇错即停，后续 Alembic 仍把正式库升级到了 `20260820_0025`。这是一次真实执行偏差，不能写成“先备份再迁移成功”。迁移为新增字段和新表，迁移前合同/附件均为 0；迁移后再次核对为 0 份合同、0 条审查快照，4 个抽查新增字段均存在。
- 迁移后立即生成完整单事务备份 `/private/tmp/zhihu_post_20260820_0025_20260821.sql`，大小 329,153,355 bytes，SHA-256 `0a3ee0d5fd3e2ea22537649c1f5397096aec3a0e92bb70bdb68030932ed17a59`。另有 2026-08-20 的迁移前完整备份 `/private/tmp/zhihu_pre_20260820_0024_20260820.sql`，但它不是本次 `0025` 的即时迁移前快照。
- 8000 已用当前代码重启，健康检查为数据库 `ok`；OpenAPI 已加载 `/api/contracts/upload` 和 `/api/contracts/paste`。不带用户数据的空 POST 经 8000 直连与 3000 代理均返回正常的 401 鉴权响应，不再是 405。
- 本轮没有代用户重新提交包含个人劳动信息的 PDF，也没有写入合成正式合同。真实文件选择、上传、解析、审查、跳转和重新打开仍须由用户本人重新点击“开始审查”完成最后验收。
- 合同录入页同期重构：首屏改为“审查后会看到关键条件、对应原文、关注顺序”，上传/粘贴表单紧跟首屏；完整说明工资社保、试用期、工时加班、岗位地点、竞业违约、解除离职六个审查角度。1440×900 和 390×844 登录态浏览器均无横向溢出，控制台无 error/warning。

下一步边界：用户验收前不要补造正式合同；请用户本人重新提交真实 PDF，确认上传后能进入 `/contract/review` 并在刷新后读取同一快照。P1 优先级按“真实 OCR/解析恢复 → 用户修正与重新审查 → 合同版本差异 → 法规来源治理/页码定位/报告导出”推进。

### 6.3 2026-08-21 权益守护·脱敏 AI 分段审查 WIP

本批按用户明确的隐私边界继续实现：不向模型发送 PDF/DOCX/图片原件、文件名、附件路径、完整合同正文、用户 ID 或未脱敏个人信息；只发送后端本地切分、筛选和脱敏后的必要劳动条款及最小审查上下文。当前代码与迁移仍是未提交 WIP，正式 MySQL 没有升级，不能把独立库结果描述成正式合同已重新审查。

已写入工作区：

1. 新增 `contract_ai_review_service.py`：合同先按原文位置形成稳定 `clause_id`，排除身份/签署段，并在本地脱敏主体名称、劳动者姓名、代表人、地址、出生日期、身份证号、手机号、邮箱和银行卡号；发送前还有直接标识符阻断。模型输入最多 24 段、14,000 字符，不包含文件对象或完整正文。
2. 模型按现有统一 AI 配置调用 OpenAI-compatible 文本接口。当前提示词 `labor-contract-clauses-v2` 明确禁止执行合同内指令、禁止无依据引用法规/法定上限、禁止猜测金额期限或替用户决定；模型“先看这里”最多 3 项。
3. 每条模型结论必须返回现有 `clause_id`，且 `evidence_quote` 必须逐字连续存在于对应脱敏条款；不满足、分类越界或字段不完整的结论直接丢弃。模型结果与 `labor-contract-v3` 本地规则合并，同条款同分类标为 `ai_model_and_rule`；模型不可用、失败或脱敏不完整时只保留规则结果并向用户说明。
4. 新迁移 `20260821_0026_contract_ai_review_metadata` 为审查快照增加条款分段、供应商/模型、提示词/脱敏版本、AI 状态、发送条款数及仅含计数的脱敏报告。调用审计沿用 `AIInvocationLog`，只记录功能点、配置、状态、耗时和可得 Token，不记录 Prompt、合同内容或模型完整响应。
5. `/contract/review` 重构为分段审查工作台：桌面左侧原文条款块、中间曲线连线、右侧审查卡，点击卡片同步高亮对应条款和连线；手机不画连线，也不重复整列原文，每张解读卡下直接展示对应原文，不折叠。首屏显示“原 PDF 未发送”、审查方式、供应商/模型和提示词/规则/脱敏版本。
6. `.next-*` 隔离构建目录加入前端 `.gitignore`。原因是 Tailwind v4 曾扫描旧构建产物中的损坏类名并生成非法 CSS。测试服务 3200/8201 已停止；随后为恢复正式前端，把 4 份旧 Next 缓存移到 `/private/tmp/zhihu-next-cache-recovery-20260821-1`（未删除，可恢复），再用干净缓存重启 3000。用户要求重启项目后，8000/8100 也已重启；8000 使用不执行自动迁移的直接启动方式，避免把重启动作暗含为正式 `0026` 升级。

本轮已验证：

- 独立测试库 `career_guardian_rights_ai_test_20260821` 真实完成 `20260820_0025 → 20260821_0026 → 20260820_0025 → 20260821_0026`。正式升级前盘点为 1 份合同、1 条审查快照；单事务完整备份 `/private/tmp/zhihu_pre_20260821_0026_20260821_1.sql` 为 340,162,253 bytes，SHA-256 `2f57c86ff61972598a376b1f83425eaa6f269be2565663ed646936fbdcb2c34a`，备份完成标记校验通过后才执行 `0025 → 0026`。
- 无数据库服务测试 12 项通过；独立 MySQL 下合同守护完整 14 项通过，用时 86.149 秒，覆盖上传/粘贴、私有附件、快照复用、删除、所有权隔离、本地脱敏、远端发送体、证据门禁、重点项上限和分段分类。`0026` 离线迁移往返测试再次通过。
- 使用完全合成劳动合同曾通过统一配置的 `SenseAudio / deepseek-v4-flash` 完成一次真实调用，返回 5 条可回指原文的模型结论；该成功快照使用提示词 v1。v2 的新增法规和关注级别门禁已通过单元/发送体测试，但隔离服务重启后因测试库中复制的加密配置与当前测试进程密钥不一致，没有把第二次外部调用写成成功。
- 登录态浏览器在独立库合成合同上验证：1440×900 桌面有 5 个原文块、5 条连线和 5 张审查卡，点击第二张卡后选中项切换；390×844 手机 `scrollWidth=390`、无可见 SVG、无重复原文列，5 张卡各自显示对应原文。浏览器读取的是 v1 已保存合成快照，不是用户真实合同。
- 正式迁移后 8 个新增字段全部存在，原有 1 份合同和 1 条快照均保留；3000/8000/8100 已恢复，正式 8000 的 `/api/health`、`/api/health/ready` 与 8100 的 `/api/health` 均返回 200。
- 正式旧快照首次读取暴露新增 JSON 字段为 `NULL` 时响应校验失败；`_review_detail` 已把迁移前的 `clause_segments` / `redaction_report` 规范化为空数组/空对象，并增加独立 MySQL 回归测试。登录态浏览器确认 `/api/contracts/` 返回 200、权益守护显示 1 份合同、`/contract/review?contractId=1` 可打开旧规则快照。
- 合同审查页面与类型文件定向 ESLint 为 0 error / 0 warning，TypeScript `--noEmit` 通过；最终交付前再次执行 `git diff --check`。

尚未完成与下一步边界：

- 正式 MySQL 已应用 `0026`，但用户此前保存的是迁移前本地规则快照。本轮没有点击“重新审查”，因此正式真实合同的脱敏模型请求、证据门禁、新快照写入与刷新回看仍由用户本人触发验收。
- v2 尚需在密钥一致的隔离环境完成一次真实合成合同调用；不得通过读取、输出或复制明文 API Key 来绕过测试密钥问题。
- 真实用户 PDF 仍由用户本人提交和触发；本批没有将正式 PDF、真实合同正文或个人信息发送给任何模型。
- 法规来源、适用地区和生效时间仍未治理，因此当前模型只能解释和提示核对，不能输出确定违法、有效、可签或具体法定上限结论。

### 6.4 2026-08-21 合同审查异步跳转、名称与多合同归组 WIP

用户正式验收发现：模型调用实际成功需要约 37～45 秒，但“开始审查”请求同步等待模型，浏览器可能先报“服务无法访问”，即使后台最终写入成功也无法进入结果页；个人中心用抽取出的 `employer` 当主标题，导致显示“乙方”；管理员日志暴露 `labor_contract_review`；页面也没有明确表达一份 Offer 可归组多份合同。

本批已写入工作区：

1. 上传/粘贴接口只保存私有原件、合同记录和 `processing` 状态后立即返回；前端拿到 `contractId` 即跳转 `/contract/review?contractId=...`，不再让浏览器等待模型。
2. 新增进程内 `ContractReviewWorker`。它从 MySQL 接管 `extracting / processing / reviewing` 任务，本地提取、持久化条款段和规则结果，再执行脱敏模型审查；进程重启后可以从持久化状态继续。模型失败时保留本地条款与规则结果，不把整个合同标成不可访问。
3. 新增 `/api/contracts/{id}/review-task` 作为可恢复的重新审查入口。审查页每 1.2 秒读取持久化状态：先显示本地解析，再让左侧真实条款依次出现；右侧等待完整模型结果，结果通过 `clause_id + evidence_quote` 门禁并落库后再逐项显示。当前不是 Token 流式输出，不得描述成实时生成正文。
4. 合同主标题统一优先使用用户填写/文件推导的 `display_name`，`employer` 只作为次级字段；管理员 AI 日志把功能名显示为“权益守护·劳动合同审查”。
5. 不新增迁移：现有 `contracts.linked_offer_id` 本来就是非唯一外键，已经支持一份 Offer 对应多份合同。本批 API 返回 Offer 摘要、同组合同数和当前序号；权益守护、个人中心、录入页与审查页明确显示“第 N / M 份合同材料”，每份合同仍保留独立原件与审查快照。

本轮实际验证：

- 独立 MySQL `zhihu_contract_async_test` 的 5 项 API 测试通过，用时 9.542 秒；覆盖上传/粘贴、所有权、私有原件删除、旧快照兼容、一 Offer 多合同，以及阻塞模型时请求先返回、`reviewing + running` 与已分段条款可读、随后正确收敛。
- 无数据库的 12 项合同分段、脱敏、模型发送边界、证据门禁和规则测试通过。前端定向 ESLint 为 0 error；管理员页保留 2 条既有 warning，TypeScript `--noEmit` 通过；`git diff --check` 通过。
- 正式 3000、8000、8100 已重启；8000 ready 返回数据库 `ok`，8100 已复核为 `provider=market-core`、`data_mode=historical`。重启中曾误用直接命令把 8100 启成 fixture，发现后立即停止并用统一脚本纠正，没有用 fixture 做验收结论。
- 登录态只读浏览器确认 `/rights` 显示 4 份合同，主标题为 `周玮的竞业限制协议V2.0` / `周玮的劳动合同`，同一 `商汤FDE` 下为第 1～4 份合同材料；个人中心“我的合同”一致。`/contract/review?contractId=4` 显示正确名称、归组关系、2 个原文块、2 张规则审查卡与 4 条 SVG 连线，1280px 无横向溢出。
- 当前浏览器账号无管理员权限，所以日志中文映射只完成代码、ESLint/TypeScript 验证，未冒充管理员页面运行验收。本轮未点击“重新审查”，没有再次调用模型或发送正式合同内容。

尚未完成：正式用户自行新建一份合同时的动态进度目视验收；真实外部模型成功后页面由等待态切换为结果态的正式环境验收；多进程/分布式 worker 租约；图片 OCR、PDF 页码定位和合同版本比较。

### 6.5 2026-08-21 合同材料删除入口与原文摘要

用户继续验收发现：个人中心“我的合同”只有“查看审查”，无法从材料管理位置删除合同；审查页左侧长条款默认完整展开，单个原文块会显著拉长页面。

本批已写入工作区：

1. 个人中心每张合同卡增加“删除”，位置与 Offer 材料管理保持一致；点击后显示站内二次确认，明确删除当前合同档案、审查快照、合同专属守护记录和私有原件，不删除关联 Offer，也不影响同一 Offer 下的其他合同。
2. 删除成功后只从当前合同列表移除该项；接口失败保留弹窗和原错误信息，不用前端假装删除成功。后端继续复用已有用户归属校验与合同专属数据清理接口。
3. 审查页桌面左侧原文改为固定高度摘要，长条款按需“展开完整原文 / 收起原文”；移动端审查卡下的对应原文采用同一规则。原文内容本身不截断、不改写，连线仍指向对应条款卡。

本轮实际验证：

- 两个前端文件的定向 ESLint 与 TypeScript `--noEmit` 通过；
- 独立 MySQL `zhihu_contract_async_test` 的合同上传后删除回归通过，确认合同、私有附件、孤立案例和守护事件均被清理，随后详情返回 404；该测试库名称带 `test`，未连接正式 `zhihu`；
- 登录态浏览器在“我的合同”看到删除入口和完整范围确认弹窗，点击“取消”后 5 份合同仍全部保留。本轮没有点击正式删除按钮，没有删除任何正式合同；
- 登录态既有审查页 `/contract/review?contractId=4` 已加载新交互：两张长原文卡默认高度均为 320px，而原文实际滚动高度分别为 672px 和 1,204px；第一张展开后高度为 816px，按钮变成“收起原文”，收起后恢复摘要。4 条 SVG 连线仍有有效路径。另一个新开页面一度因当前账号上下文读取到“合同不存在”，因此本轮只把上述既有可见页面作为运行证据，没有把所有账号和新建合同都写成已验收。

### 6.6 2026-08-21 个人中心薪资快照与分享卡片

用户验收发现：个人中心“薪资计算”只有四个数字和删除入口，无法查看保存时的完整计算结果；旧交互还要求把记录重新加载到计算器，与“保存结果档案”的心智不一致。历史参考项目曾用 `html2canvas` 导出薪资海报，但当前 Next 前端没有迁入该依赖或交互。

本批已写入工作区：

1. 个人中心的薪资记录改为结果卡，直接显示月到手、月结余、年到手、储蓄率、保存时间和是否来自 Offer 核算；不再把“加载回计算器”作为查看方式。
2. “查看结果”按记录 ID 读取既有 MySQL `salary_calculations` 详情，并在当前 `/profile` 弹层展示保存时的收入、扣款、年度结果与逐项生活支出。旧记录没有保存的字段显示“未记录”，不按当前计算器参数重新生成。
3. “分享卡片”在同一弹层预览，可复制文字摘要或下载本地生成的 SVG 卡片；分享内容默认不包含公司名、Offer 名称或个人信息，也不会把薪资数据发送到第三方。卡片保留城市、月/年到手、月结余、储蓄率和估算边界。
4. 本批没有新增迁移或复制一份结果表，继续读取已持久化的 `result_json + input_snapshot + source_context`；也没有删除或修改正式薪资记录。

本轮实际验证：

- `PersonalRecords.tsx` 定向 ESLint 和 TypeScript `--noEmit` 通过，`git diff --check` 通过；
- 登录态正式页面读取到 2 条既有薪资记录，列表分别显示真实保存的月结余 `¥5,311` 与 `¥6,271`；点击第一条“查看结果”后 URL 保持 `/profile`，弹层显示基本月薪、绩效/浮动、补贴、五险一金、个税、税前年收入、年结余、公积金双边积累和 7 项生活支出；
- 同一弹层切换“分享卡片”后显示脱敏预览及“复制文字摘要 / 下载分享卡片”入口。浏览器验收没有点击复制或下载，未改变剪贴板或生成正式下载文件；两条正式计算记录均未删除。

### 6.7 2026-08-21 合同原文模态框、单条款追问与模型一致性 WIP

用户继续验收提出：完整合同原文不应继续在双栏工作台内展开；右侧模型解读需要能够围绕当前条款追问；旧 Offer—合同一致性只靠字符串规则，合同侧会截到无关长句；管理员日志中劳动合同模型调用失败比例也偏高。

本批已写入工作区：

1. 合同工作台左侧卡片只显示原文摘要，“查看原文”改为独立模态框；打开原文只读取本地保存内容，不触发模型。移动端使用相同模态框，不再把长条款塞回审查卡。
2. 右侧审查卡增加“继续追问 / 让模型解释”模态框。新增 `POST /api/contracts/{id}/review-follow-up`，每次只发送当前一段本地脱敏条款、当前核对结论、用户问题和最多 6 条必要历史；不发送 PDF、完整合同、文件名、联系方式或用户 ID。模型返回的原文依据仍须逐字存在于该段脱敏条款。
   - 右侧不再重复提供“查看原文”：该入口只保留在左侧条款卡；右侧改为“查看解读详情”，独立弹层展示完整影响说明、下一步、脱敏依据和审查来源，并可继续进入条款级追问。手机端仍显示左侧条款卡及其原文入口。
3. Offer—合同一致性检查新增模型语义核对，当前提示词版本为 `offer-contract-consistency-v2`，输入限定为已确认 Offer 字段和必要的脱敏合同条款；本地规则只作兜底。页面展示核对方式、模型状态、每项来源和合同依据，模型失败时明确降级，不再把规则截取冒充模型结论。
4. 合同审查主请求从固定 45 秒改为连接 10 秒、读取 75 秒的分项超时，模型输出上限从 12 项收敛为最多 8 项，`max_tokens` 从 3000 收敛为 2200；JSON 解析兼容代码围栏和前后说明文字，失败日志细分为读取超时、连接超时、供应商 HTTP、无证据结论和无效结构，管理员功能名补充“权益守护·合同条款追问”和“权益守护·Offer 与合同核对”。

本轮实际验证：

- 正式 MySQL 只读核对最近 9 条 `labor_contract_review` 调用日志：4 条成功、5 条失败；其中 3 条约 45.1 秒的 `ReadTimeout`，2 条约 37～43 秒的 `ValueError`。这是修改前历史记录，只能说明旧链路主要受硬读取超时和模型结构/证据门禁影响，不能证明修改后真实模型已稳定。
- 无数据库合同服务测试 14 项通过；随后独立 MySQL `zhihu_contract_test_20260821` 的合同守护完整 19 项通过，用时 8.881 秒，覆盖现有 API 链路以及单条款追问只发送一段脱敏文本、模型一致性证据校验和规则补齐。测试启动早期 worker 在建表前产生一次“contracts 表不存在”的背景日志，但测试建表后全部用例通过；正式库未被测试清理或重建。
- 前端 TypeScript `--noEmit` 和定向 ESLint 已通过。管理员登录态浏览器只读打开正式合同 `#6`：左侧“查看原文”会在独立模态框显示本地条款全文及“不发送模型”说明；右侧“继续追问”会打开条款级对话模态框，明确单段脱敏发送边界，空问题时发送按钮禁用。验收只打开并关闭弹层，没有提交问题、重新审查或进入一致性页面，因此不得把真实供应商追问回答、模型一致性结果和修改后的成功率描述成已验收。
- 本轮进一步验收合同 `#6`：左侧当前 6 个“查看原文”入口，右侧“查看原文”为 0；右侧 7 个核对项均提供“查看解读详情”。详情弹层显示完整解释、下一步、脱敏依据、解读来源与追问入口；390×844 手机宽度下左侧原文入口及右侧详情入口均可见。本轮未提交追问或触发重新审查。

### 6.8 2026-08-21 PDF 解析质量、长文档覆盖与分批容错 WIP

用户对四份已上传材料的审查质量提出复核要求：模型只读取本地解析、分段和脱敏后的文本，因此 PDF 读取、材料类型判断、字段可信度和长合同覆盖必须先可靠，不能只优化右侧文案。

本批已写入工作区：

1. 新增迁移 `20260821_0027_contract_document_quality`：`contracts.raw_text` 从 MySQL `TEXT` 改为 `LONGTEXT`，补充解析错误码、文本页/OCR 页、页级解析质量；审查快照补充分批数量、完成批次和覆盖报告。该迁移先在独立测试库完成往返，后经用户授权完成正式库即时备份与升级。
2. PDF 改用 PyMuPDF 按页读取排序后的文字块，保留真实页数和页级字符偏移；重复页眉页脚只在本地清理，空白/低质量页明确计数。文本不足时尝试本地 OCR；当前开发机已安装 Tesseract 5.5.3 与 `chi_sim` 中文语言包，服务会自动发现 Homebrew 和常见 Linux 语言数据路径，并修复 OCR 把中文词和“第…条”标题误拆行的常见情况。OCR 仍无法读出时保留原件并停止生成虚假结论。
3. 条款分段支持章节、中文条款号和多级数字标题，过滤目录点线项；长段在自然句号/分号/换行处拆分，每段保留原文偏移与页码范围。
4. 新增本地材料类型判断：区分完整劳动合同、专项协议、员工手册/规章制度和其他用工材料。员工手册中命中的工资、试用期、地点等只能作为“材料线索”，不再回填成个人合同已确认事实；完整劳动合同和专项协议按各自边界提取。
5. 本地脱敏补充无显式“甲方：”标签的公司/组织名称；仍不记录或输出实体值。提示词升级为 `labor-contract-clauses-v4`，明确所有 `[……已脱敏]` 占位符都表示原文存在具体值，不能被判断成空白、缺失或待补填；单条款追问升级为 `labor-contract-follow-up-v2`，Offer 一致性提示词升级为 `offer-contract-consistency-v2`。解析层另行拦截模型把脱敏占位符误报为“未填写”的结果，但真正的下划线、空括号和“待填写”仍保留提示。单批最多 12 段、10,000 字符，最多 3 批；选择同时覆盖文档前中后、不同类别和最后条款，不再只看前 24 段。
6. 分批模型调用逐批记审计；某一批读取超时或返回无证据结构时，保留其他已完成批次并标为 `partial_success`，跨批去重后整体最多 18 项、其中“先看这里”最多 3 项。原文证据仍须逐字存在于对应脱敏条款。
7. 审查页新增本地材料类型与已读页数、模型分批完成数、发送边界和版本信息；员工手册不会在首屏伪装成完整劳动合同事实。

本轮真实验证：

- 无数据库合同服务测试 26 项通过，覆盖页眉页脚清理、空白页降级、页码映射、目录过滤、OCR 中文断行修复、长文档尾部覆盖、真实空白与脱敏占位符区分、员工手册降级、无标签公司名脱敏、模型发送边界、精确证据和三批中一批超时的部分成功。
- 独立测试库 `career_guardian_contract_quality_test` 真实完成 `20260821_0026 → 20260821_0027 → 20260821_0026 → 20260821_0027`；超过 65,535 UTF-8 bytes 的用工文档成功完整落库。测试库名称含 `test`，测试守卫拒绝正式 `zhihu`；验收完成后该独立测试库已清空。
- 对正式库最近四份私有附件做了只读、本地回归，未输出正文或字段值，也未调用外部模型：页数/可读页分别为 `8/8`、`6/6`、`3/3`、`80/80`；80 页材料本地读出 64,835 字符、162,173 UTF-8 bytes，形成 676 段，分批选择覆盖第 1 到第 676 段。材料类型判断为 1 份完整劳动合同、1 份专项协议、2 份员工手册；两份员工手册的字段均不再标为 `extracted`。
- 本地生成一页不含文本层的中文图片 PDF 做 OCR 冒烟，不使用私有合同正文：进入 `ocr` 模式，识别页数为 `1/1`，合同期限、工作地点、试用期工资和社会保险四组验收短语均能从结果中匹配。
- 合同审查页与类型文件定向 ESLint 通过。默认 Turbopack 构建因当前执行环境禁止内部进程绑定端口而失败；改用 Next 官方 webpack 构建路径后编译、TypeScript、29 个页面静态生成和构建跟踪全部通过。
- 正式升级前阶段的只读复核仍为 `20260821_0026`；其后用户已明确授权正式升级，最新状态见 6.9。扫描型 PDF 仍只有合成图片冒烟，不能据此宣称真实扫描合同 OCR 已验收。

下一步边界：仍应由用户对其余材料逐一重新审查，验证等待态、分批完成/部分完成、材料类型和最终结果。扫描件 OCR 目前只有合成图片 PDF 冒烟，仍需用真实、可授权的扫描样本验证版面、印章、表格和低清晰度下的中文识别质量，不能由四份原生文本 PDF 或合成样本推断已完成真实验收。

### 6.9 2026-08-21 正式 `0027`、新版审查与原文隐私视图

用户授权后，正式 MySQL 已在升级前完成完整备份并应用 `20260821_0027`。备份文件为 `/private/tmp/zhihu_before_20260821_0027_20260821_165005.sql`，大小 329,507,588 bytes，SHA-256 为 `a0ca46f97dba5b25a31997114706da17bba683182e9b8aa3a047ab5047646d4f`。独立质量测试库已在验收完成后删除；没有清理或覆盖工作区既有改动。

本轮开发侧运行验证：

- 正式合同 `#6` 重新审查收敛到第 6 版快照：`parse_status=ready`，PyMuPDF 本地读取 `8/8` 页，提示词 `labor-contract-clauses-v4`，脱敏版本 `labor-contract-local-redaction-v2`，模型状态成功，`2/2` 批完成并保存 17 个条款段。页面不再把脱敏占位符误判为“身份信息未填写”，明确否定竞业限制的材料也不再触发缺少竞业补偿告警。
- 对该合同仅做布尔和计数级隐私核验，不输出原文或标识符：本地原文存在身份证格式；进入模型发送体后身份证格式为 0，身份证替换计数为 1；发送的身份签署类条款为 0。这证明本次模型外发前的后端脱敏生效，但不意味着本地原件被改写。
- 用户指出本地“查看原文”弹窗仍直接展示完整身份证。前端已将其改为“原文隐私视图”，默认对身份证号、手机号、邮箱和 16～19 位账号再做一层只读遮罩；原件与数据库原始解析文本保持不变。登录态浏览器刷新后确认新标题和隐私提示可见，当前弹窗 DOM 中不存在完整身份证格式。
- 随后继续发现审查工作台左侧条款摘要仍绕过上述隐私视图、直接渲染本地原文。现已将同一只读遮罩统一应用到左侧摘要、解读详情的证据兜底和追问返回的证据片段；正式合同 `#13` 登录态刷新后，页面完整 18 位身份证格式命中为 0、遮罩占位符命中为 1，且未出现服务异常。本次仅改变前端展示，不改写数据库原文，也未重新触发模型审查。
- 后端身份证规则同步补强：除连续 18 位号码外，也覆盖旧式 15 位，以及 PDF/OCR 常见的数字间半角空格、制表符、全角空格、中点和连字符；同一模式同时用于替换和出站安全阻断，避免“遮罩规则认不出、安全门也放行”。
- 合同服务 30 项定向测试通过，其中新增 3 组身份证变体验证；前端该页面 ESLint 与 TypeScript `--noEmit` 通过。以上为开发侧验证，用户最终视觉与业务验收仍待完成。

### 6.10 2026-08-21 合同类型自动识别与本机服务复核

用户继续验收发现合同上传页仍像是必须先手动选择材料类型，同时机会守护出现过“职护服务暂时无法访问”，管理后台部分市场管理入口也曾显示未配置或 503。

本批已写入工作区并核对：

1. 合同上传/粘贴默认使用 `auto`。后端先根据本地读取的正文判断劳动合同、竞业协议、保密协议、培训协议、离职材料、补充协议和员工手册等类型；正文无法判断时才参考本地文件名，仍无法判断才在审查页要求用户手动确认。`parse_quality.document_kind_detection` 保存识别状态、结果和来源，但不把私有正文写入该元数据。
2. 新建页默认不再显示材料类型下拉框，只显示“上传后自动识别材料类型”和可选的“我想手动指定”；浏览器刷新实际确认默认页面没有下拉框，Offer 关联仍为可选。
3. 直接用 Uvicorn 启动后端时，市场管理内部令牌会从现有 JWT 密钥按固定域派生，与市场服务口径一致，不再依赖统一启动脚本额外注入。岗位管理列表单独使用 30 秒读取上限，避免包含长岗位原文和总量统计的有效慢请求被 10 秒代理阈值误报为 503。
4. 当前应用连接目标只读核对为 `localhost:3306/zhihu`，正式 Alembic 为 `20260821_0027 (head)`。自动识别属于业务逻辑更新，本批没有新增数据库迁移；独立测试库只用于隔离会清表的自动化测试，不代表功能尚未进入正式库。
5. 8000 后端已重启，`/api/health` 返回服务与数据库均 `ok`。机会守护刷新后不再抛运行时异常；当前页面仍显示“来源不可用”，这是市场 overview 返回的业务状态，不等同于 8000 服务不可访问。

本轮实际验证：合同审查与市场内部接口无数据库定向测试 34 项通过；Python 编译检查和 `git diff --check` 通过；市场岗位内部接口直接请求返回 200，实测约 4.4 秒；合同新建页登录态浏览器确认默认自动识别文案、隐藏手动类型选择和可选 Offer 关系。没有上传新的正式合同，也没有删除或重写正式合同数据。

### 6.11 2026-08-21 合同条款追问响应纠正与安全降级

用户实际追问“有没有更细致的解读”时，供应商约 4 秒返回后仍被页面判为失败。只读核对审计记录确认该次逻辑调用记为 `InvalidFollowUpPayload`，但隐私审计不保存模型回复正文，因此不能把失败原因进一步冒充为已确认的空回答或引用改写。代码核对发现旧逻辑会把“回答为空”和“`evidence_quote` 不是脱敏条款的连续逐字片段”合并成同一错误，并把正常问题误导为“请换一种问法”。

本批已写入工作区：

1. 单条款追问提示词升级为 `labor-contract-follow-up-v3`。宽泛但合理的问题也必须结合当前条款回答实际影响、下一步核对项和边界，不得要求用户改写问题。
2. 首次返回缺少正文、JSON 异常或引用不是连续逐字证据时，后端会用同一段已脱敏条款自动纠正一次；不会增加 PDF、完整合同、文件名、联系方式或其他条款，也不会把第一次模型原文写入日志。
3. 两次回答都有正文但引用仍未通过逐字校验时，系统删除不可靠引用、保留第二次解释，并明确标记为“单条款脱敏追问 · 一般解释”，同时提示该回答没有可逐字回指证据、具体事实以左侧原文为准；不再用伪证据，也不再把整次回答丢弃。
4. 两次都没有正文或格式连续异常时仍会失败，但接口会分别返回真实错误，并明确说明“你的问题没有问题”。同一用户动作最多记一条逻辑审计，重试的数值用量合并计算，不把内部纠正虚增成多次用户失败。

本轮实际验证：合同服务定向测试 34 项通过，新增覆盖“错误引用后二次成功”“连续错误引用后安全降级”和“连续空回答返回明确错误”；Python 编译检查及 `git diff --check` 通过。8000 后端已重启为进程 `62194`，`/api/health` 返回服务和数据库均 `ok`。本轮没有使用用户合同重新调用真实模型，因此新逻辑的真实供应商回答效果仍需用户再次提交同一问题验收，不能写成已完成真实模型验收。

### 6.12 2026-08-21 合同条款追问记录持久化与重新打开

用户关闭“继续问这段条款”弹层后再次打开时，页面原先只保留当前 React 内存中的消息；关闭弹层会清空状态，后端也没有追问记录实体，因此已经成功返回的追问无法再次查看。AI 调用审计按隐私边界不保存请求或回复正文，所以修复前产生的历史问答不能从日志可靠恢复，也不会据此伪造旧记录。

本批已写入工作区并部署到当前正式验收环境：

1. 新增 `contract_follow_up_turns`，以用户、合同、当前审查快照、条款和核对项保存成功的逐轮问答；同时记录模型、提示词、脱敏版本和审查方式。合同或审查快照删除时追问记录级联删除，不形成脱离原材料的孤立问答。
2. 新增私有历史读取接口。再次打开同一条款的同一核对项时，前端先读取当前审查版本的已保存问答，再允许继续追问；不同用户、不同合同、旧审查版本之间不会串记录。
3. 后续对话上下文由服务端读取最近三轮已持久化记录，不再信任前端传入的历史正文。用户新问题在入库前再次走本地脱敏，模型仍只接收当前已脱敏条款、当前核对结论、最近三轮已脱敏问答和本次问题，不发送 PDF 或整份合同。
4. 迁移 `20260821_0028` 已在独立 MySQL 测试库通过定向测试，覆盖两轮保存、关闭后读取、伪造客户端历史不生效、手机号脱敏、用户隔离和合同删除级联；测试库已清理。正式 `zhihu` 已从 `20260821_0027` 升级到 `20260821_0028 (head)`，8000 后端已重启并加载新的 GET/POST 追问接口。

本轮实际验证：后端合同服务测试 43 项通过（无数据库环境时 9 项按约定跳过），迁移测试 12 项通过（4 项 MySQL 测试按约定跳过），独立 MySQL 的追问历史定向测试通过；前端 `tsc --noEmit` 通过，Python 编译检查与 `git diff --check` 通过。Next 构建因当前受限环境不允许 Turbopack 绑定端口而未执行完成，不是代码编译错误。应用内浏览器当前登录的是测试账号，无法读取管理员名下合同，因此没有把管理员真实页面点击冒充为已验收；用户需用现有管理员会话发送一条新追问，再关闭并重新打开同一核对项完成最终交互验收。修复前旧追问正文因从未持久化，无法恢复。

### 6.13 2026-08-21 决策守护五项工具 Option Wheel

决策守护中部原先将五项能力同时展开为大卡网格，桌面首屏占用较高，也缺少清晰的当前焦点。本批只重构前端呈现与导航，没有修改 Offer、分析、薪资核算或决策数据口径，也没有新增数据库迁移。

1. 五项能力改为左侧圆弧 Option Wheel 与右侧单项内容区。左侧始终保留 5 个可见、可聚焦且带完整可读名称的选项；当前项在中间基准位置放大并提高对比度，右侧同步展示该项的真实状态、说明和原有业务入口。
2. 支持鼠标滚轮、触摸或手写笔上下拖动、方向键和 Home/End；首尾循环，滚轮与拖动停止后吸附到最近项目。点击任意圆弧项目也会直接切换，不要求用户理解手势。
3. `prefers-reduced-motion: reduce` 时不使用圆弧运动，自动退回普通纵向 `tablist`；语义结构为 `tablist / tab / tabpanel`，当前项使用 `aria-selected`，不会为了视觉效果丢失键盘与读屏入口。
4. 五项的状态和链接仍从当前 Offer、事实台账、精细薪资快照、HR 核对项及决定历史实时组装，没有改成原型固定数字；无 Offer 时仍保留完整五项入口与对应空态。
5. 针对首轮视觉验收中“标题看不清、不同项目大小不一、切换时布局抖动”的问题，桌面组件固定为 `34rem` 高度和稳定左右分栏；左侧当前项统一为固定宽度，右侧序号、标题、说明、状态、操作和分页分别使用固定或最小高度槽位。长标题最多两行，状态内容在固定区域内截断，不再重新撑开外框；右侧标题和正文改为明确的深色文本，非当前圆弧项目也提高了对比度。

本轮实际验证：目标文件 ESLint、TypeScript `--noEmit` 和 `git diff --check` 均通过。3000 前端启动后，登录态真实 `/decision` 页面确认 5 个 `tab` 均存在且右侧内容随点击切换；从第 5 项按向下键可循环回第 1 项。逐项切换 5 个选项时，桌面外框均为 `1216×544`，右侧面板均为约 `679.8×542`，标题高度均为 `45px`，标题计算颜色为 `rgb(37, 51, 47)`；未再观察到由内容高度变化造成的尺寸跳动。390×844 手机视口下五项仍全部可见并保持可聚焦；本次固定尺寸补丁没有重新执行手机视口运行验收。减少动态效果回退已做代码与语义检查，本轮未在真实操作系统级“减少动态效果”设置下做视觉验收，因此不把该项描述成浏览器端已完成验收。

### 6.14 2026-08-21 上下文压缩、文档校正与收支守护开发入口

目标任务 `codex://threads/01a019df-c031-74b3-8f45-373688bbc8b5` 的有效上下文已压缩为以下当前事实：

1. 决策守护已完成 Offer 事实版本、分析快照、决定 preflight、条件化比较、HR 证据应用、跨域交接和 Option Wheel；代码、测试与多轮浏览器验证不等同于用户最终业务验收。
2. 权益守护已完成劳动合同私有保存、自动类型识别、可恢复异步解析、长文档分批、后端脱敏、证据门禁、原文隐私遮罩、追问纠正/降级和按审查版本持久化追问。截至本节上下文压缩时，正式 MySQL 只读核验为 `20260821_0028 (head)`；此后已升级至 `20260822_0029`，见 6.15。真实扫描 OCR、合同版本比较、导出和正式用户追问重新打开仍有验收边界。
3. 职业形象与首页读取态已有代码和浏览器证据，但活动任务恢复、`generation_timeout` 幂等、长耗时和真实生成仍未闭环，不得因本轮切换重点而写成完成。
4. 当前用户明确把该领域改名为“收支守护”，收入和支出在用户体验上平级。`/Users/zhouwei5/Downloads/OneBill` 只提供账本、分类、预算统计、导入预览/去重/确认和候选确认交互参考，不形成职护运行时依赖。
5. 2026-08-22 起不再用产品版本或批次遗漏功能：工资核对、完整收支账本、预算、微信/支付宝/银行/通用表格导入、OCR、自然语言记账、AI 分类与月度解释均进入同一功能总图，只标记已实现、开发中、待开发或需外部配置。详细状态见 `zhihu/docs/income-guardian.md`。

本轮只读准备还确认：`./scripts/check-workspace.sh` 通过，Node、npm、Python、MySQL、后端虚拟环境和前端依赖均可用；工作区内的用户保留文件、OneBill 参考项目、合同 `page 2.tsx` 和验收截图不应纳入产品提交。

### 6.15 2026-08-22 收支守护统一账本开发中

1. 新增 `financial_categories` 和 `financial_transactions` 模型及迁移 `20260822_0029`。系统分类覆盖工资、奖金、兼职副业、经营收入、投资收益、报销、退款、补贴、住房、餐饮、交通、医疗、学习等；用户自定义分类与系统分类按所有权读取。
2. 新增 `/api/cashflow/categories`、`/api/cashflow/transactions` 和 `/api/cashflow/summary`。收入和支出必须使用同方向分类，转账不带收支分类且不进入合计；只有已确认流水参与收入、支出、分类和净结余，待确认和排除记录只计数量。
3. 流水支持手工新增、编辑、按月/方向/状态/分类/关键词筛选和软删除；列表、汇总、更新和删除均带当前用户所有权条件。外部导入唯一键已在表结构预留；本节记录的是 `0029` 完成时状态，后续导入批次、逐行候选和并发幂等实现见 6.16。
4. `/income` 已替换为专用“收支守护”页面：收入与支出等面积呈现，提供净结余、待确认、工资核对、支出性质、统一流水、记录/编辑/删除和转账入口。本节记录的是 `0029` 完成时状态；文件导入、OCR 和自然语言入口的后续实现见 6.16。
5. AI/OCR 复用职护现有服务端模型配置、文本模型能力、隐私边界和 `AIInvocationLog`；不移植 OneBill 的智谱、腾讯 OCR、COS、用户系统或密钥配置。当前 OCR 采用本地 Tesseract 后仅发送脱敏文字，详见 6.16。
6. 正式 MySQL 已获授权并应用迁移 `20260822_0029`。只读复核确认当前版本为 head、两张收支表存在、20 个系统分类已初始化；API `/api/health`、`/api/health/ready`、market `/api/health` 和前端 `/income` 运行检查通过。
7. 桌面登录态浏览器已验证收入、支出、转账新增，全部/收入/支出/转账/待确认筛选，待确认不进入汇总，待确认编辑为已确认后的重算，以及转账不影响收入、支出和净结余。过程中发现并修复只有单侧数据时另一侧被显示为 `0` 的问题：现在两侧分别判断是否有已确认数据，只有两侧都有确认记录后才展示净结余。
8. `390×844` 手机视口已验证页面无横向溢出、移动导航可横向滚动、收支卡片顺序正确、录入弹窗占满视口且内部可滚动；验收后已恢复 `1280×720`。浏览器控制台无 error 或 warning。页面持久化用户名曾与实际令牌身份不一致，已在导航挂载时通过 `/auth/me` 回源修复；4 条明确标记“验收”的合成流水实际归属正式库 `admin`。截至本节完成时尚未删除，后续已在获得授权后通过页面逐条软删除，见 6.16。
9. 代码复核后进一步补强：创建和更新金额使用 Decimal，在入库前拒绝零、负数、超过两位小数和超出 `DECIMAL(14,2)` 的值；前端同步限制金额格式并在浏览器验证 `0.001` 被阻止。月度 summary 现在用整月全部已确认支出计算五类支出性质，不受前端最近 200 笔展示上限影响；前端明确披露列表上限。月份读取使用请求序号避免旧响应覆盖新月份，可选工资条读取失败不再拖垮核心账本。后端完整测试 164 项中 91 项通过、73 项因未设置独立 MySQL 测试库按安全规则跳过；前端 TypeScript `--noEmit`、ESLint `--quiet` 和 Next 16 webpack 生产构建通过，`/income` 在 29 条构建路由中成功生成。本轮未调用真实 AI/OCR，也不等同于用户最终业务验收；截至本节软删除交互仍待浏览器验证，后续完成状态见 6.16。

### 6.16 2026-08-22 收支导入候选闭环开发中（正式部署前快照）

本节保留 `0030` 正式部署前的代码、隔离测试和运行边界，用于追溯当时为什么不能宣称正式可用；当前正式状态见 6.17。

1. 新增迁移 `20260822_0030_cashflow_import_candidates`、`financial_import_batches`、`financial_transaction_candidates` 和 `personal_attachment_cleanup_jobs`，并为用户增加 `business_data_epoch`。文件、自然语言和 OCR 共用用户私有批次与候选状态机；批次与候选均有乐观锁版本，候选通过复合外键强制与批次属于同一用户，数据纪元阻止清空前已启动的长耗时请求在清空后重新写回。
2. 新增 `/api/cashflow/imports` 文件上传、批次/候选读取、字段映射、候选编辑/排除/恢复/明确接受复核和批量确认接口。支持微信、支付宝、银行、通用 CSV/TSV/XLSX；旧 `.xls` 明确拒绝，文件上限 10MB、5000 条、80 列。解析器已升至 `cashflow-tabular-v3`：XLSX 总解压上限为 32MB，并限制成员、工作表/共享文字/元数据大小、XML 节点、单元格文本节点和 64 层深度；workbook、关系及 1900/1904 日期系统缺失或损坏时 fail-closed，不再猜测 `sheet1.xml`。Next 代理与 ASGI 接收器都按实际到达字节中断超限 multipart 和各类 JSON，不能只靠可伪造或缺失的 `Content-Length`。
3. 未确认行只进入候选表。精确外部键重复默认排除，常见占位、短序号、掩码和低熵流水号不会形成跨文件硬身份；疑似重复按方向、精确金额、日期和归一化商户/摘要复核。单桶最多精确扫描 100 条、一次最多预取 5000 条；超限时展示真实桶数量并把 `count + max_transaction_id` 水位绑定到接受证据，新增记录后确认会强制重新核对。只有 `ready` 候选能写正式流水。手工流水增删改、导入确认和清空数据共用每用户行锁；确认阶段再次校验版本、金额、方向、分类和精确/疑似重复。两个不同外部键批次并发提交相同疑似重复指纹时只能有一笔进入正式流水，另一批返回明确复核；软删除过的正式流水仍阻止同一外部键静默重导。
4. `/income` 新增统一导入工作台：文件、自然语言、票据 OCR 三个入口；字段映射、脱敏样例、状态汇总、桌面表格/手机卡片、逐行编辑、排除/恢复、重复显式接受、选择合计和二次确认已接入。前后端单次最多确认 500 条，前端串行衔接批次版本；部分分段成功后再失败会保留已成功报告并刷新批次、候选和正式账本，不冒充整批回滚。新增只读 `GET /cashflow/imports/capabilities`，先读批次表确认分支代码、导入数据表和数据库服务同时就绪，再分别报告文件接口、当前 AI 配置和本机 OCR 依赖；前端区分“入口可用”“依赖已配置”“待启用”，不把配置存在冒充远端模型已连通，未迁移、数据库不可读或旧后端也不会打开工作台。批次列表用 `unfinished_only=true` 在服务端先筛选，再返回最近 20 个未完成批次，避免较新的已完成批次挤掉更早待办。候选编辑只提交用户实际触碰字段，核对弹层只展示白名单内的脱敏来源字段、原文件行号、可定位证据短句与置信度；写成功而刷新失败会明确提示不要重复提交。
5. 自然语言和 OCR 复用职护现有 AI 配置和 `AIInvocationLog`，没有移植 OneBill 厂商配置。OCR 原图仅在本机 Tesseract 识别，连续或带空格/连字符的手机号、邮箱和长账号先本地脱敏，再把文字发给当前文本模型；原图片不出站。AI 配置与调用审计使用独立 Session，不会提前提交业务事务，日志不记录输入正文。图片在 OCR 前校验真实 PNG/JPEG/WebP 文件头和宽高，拒绝超过 16000 像素单边或 2500 万像素的压缩图。
6. `/api/auth/data` 与账号删除边界已补齐导入候选、批次、正式收支、自定义分类和私有附件文件；系统分类保留，AI 调用日志继续匿名化。文件删除目标先作为带预期 SHA-256 的持久清理任务与数据库删除事实同事务提交，物理删除失败可由原用户或管理员幂等重试；待处理任务优先于旧失败任务，成功后删除任务行，不永久保留用户/路径墓碑。私有目录和文件分别使用 `0700`、`0600`，旧权限在读取时修复且失败即关闭；重传会校验并原子修复缺失/损坏原件，字段映射遇到原件缺失返回可恢复的 409。私有附件下载补充 `private, no-store` 与 `nosniff`，提交结果不明确时用新 Session 复核，绝不删除可能已被数据库引用的原件。后台孤儿扫描已改为不跟随符号链接的有界分页续扫：默认每轮最多处理 200 个文件、检查 1600 个目录项，只查询本页路径的附件和任务记录，不再全量物化目录树或两张路径表。
7. 4 条旧合成验收流水已在授权后通过 `/income` 页面逐条软删除；正式库只读核对为活动记录 0 条、4 条均保留 `deleted_at`，因此操作可追溯且没有物理删除。
8. 迁移验证严格隔离：`0030` 离线 upgrade/downgrade SQL 通过；随机名称含 `test` 的临时 MySQL 空库升级到 head 通过；临时 schema 已完成 `0029 → 0030 → 0029 → 0030` 往返。自动化独立 MySQL 共 10 项通过：空库升 head、`0029` 往返、3 项基础收支 API、5 项导入 API/并发确认；API 覆盖 Alice/Bob 所有权隔离、预览不入正式账本、stale version 409、确认入账、跨文件精确去重、疑似重复串行化、能力状态与未完成批次服务端过滤，以及文本/OCR 端点的候选门禁、隐私和真实调用审计。所有临时 schema 均在 `finally` 中删除。正式库仍是 `20260822_0029`，低于当前分支 head `20260822_0030`。
9. 收支相关 Python 测试共 98 项，其中 90 项通过、8 项按默认进程的独立 MySQL 安全门跳过；这 8 项已全部在临时 MySQL 单独真实跑通。新增端点级组合用例验证自然语言候选在 `accept_review` 前后均不进入正式账本、只有 `confirm` 才写入，并由独立 Session 实际落 `AIInvocationLog`；OCR 未确认外部处理时本地 OCR/模型均不调用，确认后原图不进入模型消息且私有附件保持本人所有；能力用例验证文件、AI 配置与本机 OCR 分别报告，并验证完成批次不会挤掉待恢复批次。完整后端回归共 250 项，其中 171 项通过、79 项按相同安全门跳过。前端 TypeScript `--noEmit --incremental false` 与收支组件定向 ESLint 通过；完整 ESLint 为 0 错误、11 条其他既有页面警告；使用独立构建目录的 Next webpack 生产构建通过，29/29 页面成功生成。未触发真实外部 AI，也未完成正式迁移后的桌面/手机浏览器导入验收或用户最终业务验收。
10. 金额响应统一为两位小数字符串，前端用 `BigInt` 分为单位合计；日期范围、空币种/非 CNY、方向分列冲突和源状态错误均 fail-closed，源错误只有显式修改对应事实字段才能解除。原始账号、供应商流水号和带敏感信息的表头不复制到候选/API：公共表头先脱敏并保持唯一，字段映射只在本地解析时回到私有原件。
11. 额外完成一轮隔离 HTTP 运行验证：自定义表头 CSV 先进入 `mapping_required`，映射后得到 1 笔收入和 1 笔支出，确认前正式账本为空，确认后才写入；第二份重叠 CSV 把同日同额同商户收入标为 `possible_duplicate`，通过候选接口排除后仅确认新的支出。隔离账本最终为收入 `1288.50`、支出 `126.10`、净结余 `1162.40`。登录态桌面和 `390×844` 手机页面读取到相同结果，手机无横向溢出；隔离 schema、附件目录、构建缓存和合成文件均已清理。应用内浏览器的本地文件选择及加固后重载被 URL 安全策略拒绝，所以本轮没有冒充完成浏览器上传点击链或新门禁运行验收。正式环境只读复核仍为：4 条旧合成流水活动 0、软删除 4；`0030` 三张新表不存在；`GET /api/cashflow/imports` 为 404；健康检查和 `/income` 基础页面正常。未迁移、未重启、未创建正式验收数据。

### 6.17 2026-08-22 正式 `0030` 部署、真实 AI 与开发侧合成验收

1. 正式变更前先停止可能写库的服务，并在 `/private/tmp/zhihu-pre0030-20260822_202433` 生成 MySQL 与 uploads 配对备份。`zhihu-pre-20260822-0030.sql` 和 `uploads-pre-20260822-0030.tar.gz` 权限均为 `0600`；SHA-256 分别为 `48fdf38c4c21f50b60d1be49d8a968347cfd73fc8fd46cb940b04e42e3dff0b7` 和 `5985d74b2b5673886d8efd3718da789fef0cdbb4947c0db8503116f70018e29f`。SQL 已恢复至随机临时 schema，核对版本、51 张部署前表及关键行数后删除该 schema；这不是只检查文件存在。
2. 正式 MySQL 已显式执行 `20260822_0029 → 20260822_0030`，升级后共 54 张表。旧基线保持为用户 1、系统分类 20、流水 4（活动 0）、私有附件 4；`financial_import_batches`、`financial_transaction_candidates`、`personal_attachment_cleanup_jobs` 和 `users.business_data_epoch` 均存在。合成账号清理后，批次、候选和清理任务表均为 0。
3. market、后端和前端已经重启。8000 `/api/health`、8000 `/api/health/ready`、8100 `/api/health`、3000 根页面和 3000 `/income` 均返回 200；`0030` 能力门禁由重启后的正式后端加载。
4. 文件入口已通过正式 HTTP/API 验证，但浏览器原生文件选择仍未完成验收，不能写成浏览器上传、映射和确认点击链已通过。桌面登录态确认 OCR 支出 `36.50` 元、自然语言收入 `688.00` 元及重复候选排除；`390×844` 手机登录态确认文档无横向溢出，并实际验证未完成批次恢复和底部确认条。
5. 自然语言真实外部文本模型调用记录为 `AIInvocationLog #226`，状态 `success`。OCR 路径先使用本机 Tesseract，随后仅将脱敏文字发送给现有 `SenseAudio / deepseek-v4-flash`；原图片未出站，也没有外部 OCR 调用。首次 OCR 模型日志 `#227` 为 `ModelResponseInvalidJSON`；补齐 OCR 提示词 JSON schema 并重启服务后，重试日志 `#228` 为 `success`。失败与成功均保留审计，日志不保存输入正文。
6. 开发侧验收只使用合成账号和合成材料。验收后账号已经删除，相关交易、批次、候选、附件和清理任务均为 0，物理附件已经删除；AI 调用日志按账号删除规则匿名保留。正式用户、分类、既有软删除流水和私有附件基线恢复，`ai_invocation_logs` 共 191 条。以上证据不等同于用户已完成最终业务验收。
7. 正式部署前的远端功能分支基线为 `67b5999e`，已包含 `20447edb`、`ac7a12b9` 和 `67b5999e`；当时正式服务从 `codex/cashflow-import-guardian` 功能分支工作区重启。此后功能已经合并，本轮详细设计所基于的已合并代码基线为 `c2545768`。这只修正 Git 历史事实，不代表当前正式服务来源已在本次文档更新中重新验收。

### 6.18 2026-08-23 收支识别产物与原件一次性处理开发中

1. 产品与交互基线已通过 `9927cfa7 docs: establish cashflow guardian product baseline` 推送至 `main`；当前功能开发位于 `codex/cashflow-recognition-artifacts`。本节只记录功能分支代码和隔离验证，不改变 6.17 的正式运行证据。
2. 新增迁移 `20260823_0031_cashflow_recognition_artifacts` 和用户私有识别产物：表格保存解析清单与规范化行分块，普通图片 OCR 保存完整原文；产物带内容哈希、字节数、顺序和所有权约束，用于完整性校验和批次恢复。
3. 新上传的 CSV/TSV/XLSX 在内存解析后不再保存整张原文件，字段映射从规范化行产物恢复；普通 OCR 图片只在本次请求内用于本机 Tesseract，整张图片不写入个人附件，只保存完整 OCR 原文。历史需要映射的旧批次可以在下次操作时一次性转换，并把旧原件加入持久清理队列。
4. 现有 AI 体系只接收从完整 OCR 原文生成的有界脱敏派生文本；完整原文和原图不写入通用 AI 日志。OCR/AI 仍只生成候选，未经用户确认不进入正式账本。
5. 后端识别产物与 AI 入口定向测试 55 项通过；迁移测试 17 项通过、其中 6 项因当前进程未设置独立 MySQL 测试库按安全门跳过；前端定向 ESLint 和 TypeScript 检查通过。真实 MySQL `0031` 往返、正式升级、服务重启和正式浏览器验收均未在本节执行。
6. 长截图用户可见链路已进入功能分支：OCR 图片上限从 10MB 放宽至 30MB，支持宽 12000px、高 80000px、总计 6000 万像素；超过普通图片阈值后自动缩放并拆成最多 40 个、相邻重叠 320px 的片段。前端逐片触发本机 OCR 与现有 AI，展示完成/失败/待处理数量和片段级进度，失败片段可以单独重试，其余候选不会丢失。
7. 相邻片段里日期、金额、方向和交易文本完全一致的记录由程序合并为一个候选，同时保留两个来源片段和合并理由；只相似而不能确定的记录仍进入疑似重复核对。OCR 候选前端已按确定性校验和模型置信度显示绿色“可一键记录”、黄色“需要确认”和红色“重点核对”，用户确认前仍不会进入正式账本。
8. 本节新增长图服务定向测试 3 项通过，覆盖真实图片渲染切片、只保存派生切片、逐片进度、完整 OCR 原文、重叠自动合并、片段失败保留与重试；收支相关定向测试 82 项通过，迁移测试 17 项通过、其中 6 项按独立 MySQL 安全门跳过。前端定向 ESLint、TypeScript 和 Next 16 webpack 生产构建通过，29/29 页面成功生成。Turbopack 构建在当前执行环境因 CSS 子进程绑定临时端口被系统拒绝，webpack 构建不受影响。一张现有 `390×3878` 长图完成纯切片冒烟，生成 3 个片段；该图片不是微信/支付宝真实账单，没有调用真实 OCR/AI，也不能代替登录态浏览器和正式环境验收。跨多张连续截图和深度思考模型升级链仍待开发。
9. `/income` 功能分支已按详细设计重做统一工作台：不再增加页面内导航；收入与支出两个入口平级并直接连接工资条、长截图、文件和手工录入；已确认数据生成每日趋势、本月/上月对比、支出分类和商户排行；可信账本只展示 `confirmed`，导入批次和历史 `pending` 在列表之后提供轻量恢复入口。
10. 收支页面已复用现有 `KnowledgePreview`，根据当月已确认净结余、转账、可报销支出和工资条信号匹配“看懂薪资 / 入职阶段 / 理财阶段”内容。该能力是现有知识库的确定性相关性匹配，不是尚未开发的 AI 收支问答，也不代表知识引用元数据和具体异常证据关联已经闭环。
11. 本次主页面改造通过定向 ESLint 与 TypeScript 检查。本地服务可打开新入口结构，但现有本地 `admin` 凭据返回 401，因此没有创建合成账号、没有写入测试业务数据，也没有把这次检查表述为登录态桌面/手机验收；正式运行仍是 6.17 的历史证据。

### 6.19 2026-08-23 工资条文件与 OCR 候选录入开发中

1. 新增迁移 `20260823_0032_payslip_recognition_fields`：工资条补充发薪单位、实际发薪/到账日期、奖金、加班费、考勤扣款、餐费扣款、企业自定义项目、录入来源和识别置信度。原有工资所属月份继续保留，形成两个不同时间字段；约定发薪日和正式现金到账关系尚未接通。
2. `/payslips/recognize` 已支持 CSV、TSV、XLSX 与 PNG/JPG/WebP 工资条。表格由确定性字段别名解析；图片先在本机 Tesseract OCR，只把最多 8000 字的脱敏派生文字交给职护现有 AI，AI 不可用或输出无效时保留规则识别候选。图片和表格原文件都不入库，完整 OCR 原文可以在用户载入候选后随工资条证据保存。
3. 工资条候选按核心字段完整度、模型置信度、金额解析和算术线索分为绿色、黄色和红色；每份候选展示分级原因、未知字段和警告，必须由用户点击“载入表单核对”并再次保存。缺失字段保持 `null`，不自动写成 0；社保、公积金、个税、考勤、餐费和其他扣款未全部确认前，前后端均不计算伪造的扣款合计，也不生成“工资条数字异常”结论。
4. 工资条页已增加同一月份历史记录提醒，不静默覆盖已有工资条；企业自定义列可以载入、修改和保存。当前仍是一份一份人工确认，未实现批次恢复、批量保存、编辑删除、版本替代、Offer/合同多材料关联和实际到账匹配。
5. 本节 8 项工资识别/未知值/上传边界定向测试和 `0032` 离线迁移往返测试通过；前端工资条页、代理限额定向 ESLint 与 TypeScript 检查通过。未执行真实外部模型调用、正式 MySQL 升级或登录态浏览器验收。

### 6.20 2026-08-23 工资多材料与实际到账链路开发中

1. 新增 `0033` 多材料关联：一份工资条可同时关联多份 Offer 和多份劳动合同；页面明确询问关联 Offer、合同、两者或暂不关联。后端逐份计算可靠月薪与工资条应发的差异，不选定“哪份材料正确”；合同没有明确月薪数字时保留为未知。
2. 新增 `0034` 工资到账关系：工资条保存后，程序只从当前用户的已确认收入流水中按金额、日期和发薪单位找候选。工资条实发不会自动写成一笔现金收入。
3. 程序不能唯一判定的中/低匹配会自动复用职护现有 AI 做语义评议，只返回“较可能/较不可能/仍不确定”和理由，不改变程序置信层级，不自动建立正式关系。AI 失败时候选仍可人工处理。
4. 用户可选一笔或多笔到账，为每笔填写分配金额后确认；后端校验工资条累计分配不超过实发、单笔流水跨工资条分配不超过流水金额。关联可撤销，保留确认与撤销时间。
5. 当前是功能分支代码状态；工资多材料、到账规则和 AI 结构的定向测试，`0033`/`0034` 离线迁移和前端 TypeScript 检查已通过。真实外部 AI 调用、正式 MySQL 迁移和登录态业务验收尚未在本节执行。
6. 新增 `0035` 约定发薪日：工资所属月、工资条标注发薪日、约定发薪日和用户关联的真实流水到账日已分开。完整到账可形成“按时/晚到 N 天”守护发现；部分到账显示剩余待核金额；没有真实到账证据时即使约定日已过，也只标记“尚未核清”。工资条保存后还会与同发薪单位的上一份工资条逐项对比，只比较两期都已知的项目。

### 6.21 2026-08-23 收支守护 `0043` 正式部署与登录态验收

1. 当前功能分支连续完成并推送四个代码切片：`cdf4af8a` 统一账本修订，`f7802d7c` 月报/月结快照，`cb11ba8e` 事实关系版本与原子批量撤销，`544cf90e` 收支 AI 多会话和逐轮持久化。迁移头推进到 `20260823_0043`。
2. 正式变更前生成 MySQL 与 uploads 配对备份。可移植 SQL 为 `/private/tmp/zhihu-pre0043-20260823-Q0jTR8/zhihu-pre-20260823-0043.sql`，SHA-256 为 `d3aad1ae842056d3090f9ecaa9c47b9b9f6729516231f6763fd2acee62f12e6b`；uploads 归档 SHA-256 为 `a218c1ea68d53cd2ef39bebcf101aee0370aade191f2ea1bc9da8dec54ce0170`。SQL 已先恢复到独立 schema 核对 `0030` 基线，再在另一独立 schema 完成 `0030 → 0043` 升级；两处临时 schema 均在核对后删除。
3. 正式 MySQL 已完成 `20260822_0030 → 20260823_0043`，表数由 54 增至 68。升级后只读核对仍为用户 1、流水总数 4 且活动 0、导入批次 0；本次迁移没有创建或确认财务流水。
4. 后端、前端和市场服务已按现有启动脚本重启。管理员已有 Chrome 登录态可打开 `/income`：桌面宽度 1470px 和 `390×844` 手机宽度均无页面级横向溢出；页面只有产品全局五守护导航，收入与支出两个入口平级，其后依次展示已确认分析、周期支出、预算、月报/月结、工资证据、可信账本、待核对提醒、AI 和 KNOWLEDGE。
5. 收入入口已实际打开 `/payslip`，确认手工字段、CSV/TSV/XLSX/PDF/图片候选入口、Offer/合同多材料选择、未知值不补 0 和保存前人工核对文案存在；支出入口已打开统一导入模态框，确认长截图 30MB、重叠切片、候选确认门禁和本机 OCR 后脱敏文字才进入现有 AI 的隐私说明存在。
6. 正式管理员账号提交了不含真实财务明细的问题“当前账本没有已确认流水时，我应该如何开始整理收支？”。`AIInvocationLog #229` 记录 `SenseAudio / deepseek-v4-flash`、`success`、4308ms、990 tokens；页面标记“AI 基于程序结果解释”，固化账本 `r0`、数据范围和 0 笔已确认流水，刷新后同一会话和回答可恢复。验收后正式库为 1 个会话、1 轮问答，活动流水仍为 0，导入批次仍为 0。
7. 真实 OCR 文件上传尚未完成。Chrome 扩展拒绝 `fileChooser.setFiles`，需要用户在扩展详情中启用“允许访问文件网址”；本次失败发生在选择文件阶段，未上传图片、未创建识别批次、未调用 OCR/识别 AI，也未写入候选或正式账本。不能用既有合成 OCR 记录替代这次正式登录态上传验收。

### 6.22 2026-08-23 多张连续账单截图同批识别

1. 支出 OCR 入口已支持一次选择 2–10 张连续截图，在上传前调整顺序或移除图片；单张不超过 30MB，合计不超过 90MB。单张票据继续使用原入口，两张及以上进入新的 `POST /api/cashflow/imports/ocr/sequence`。
2. 服务端把排好顺序的多张图组成一个可恢复批次，每张图记录原图序号和局部片段序号，合计最多 80 个切片。即使是短截图也会生成两个重叠派生片段；上传的整张原图不入附件库，仅保留识别切片、完整 OCR 原文和结构化候选。
3. 同批次内完全相同的原图会跳过重复切片和识别，前端明确显示“第 N 张与第 M 张相同”。程序只对相邻截图交界处日期、金额、方向和交易文本完全一致的记录自动合并，合并候选保留所有来源切片和跨图理由。文本只是相似时自动调用职护现有 AI 评议；AI 只保存“较可能同笔/较可能不同/仍不确定”和理由，不改变程序重复状态，不自动合并或入账；AI 失败时直接保留人工核对。
4. 当前片段未识别出日期时，只有上一相邻片段存在唯一可用日期，程序才把该日期填入候选，同时保留来源片段并强制显示黄色“需要确认”理由。上一片存在多个日期、日期无效或片段不连续时不继承，保持日期未知并由人工处理。
5. 识别面板展示图片数、总切片进度、每张图的局部片段状态和失败重试入口。关闭模态框后已完成的切片、OCR 文字和候选不丢失，下次从未完成片段继续；任何 OCR/AI 结果仍不会静默进入正式账本。
6. 这是无迁移的功能分支代码，定向后端 14 项测试、前端 TypeScript 和定向 ESLint 已通过。测试覆盖唯一日期继承强制人工确认、多日期不猜测、跨图疑似同笔才调用 AI、AI 调用时不占用账本事务，以及 AI 失败回到人工核对。本轮没有重启正式服务、没有上传真实账单，因此真实微信/支付宝/银行多图的 OCR 质量、日期上下文接续和正式登录态交互仍待验收。

### 6.23 2026-08-23 OCR 旧批次兼容与拆分事实关系组合

1. OCR 进度面板不再假设历史批次一定包含 `duplicate_images` 和 `slices` 数组；归一化入口和面板渲染点都使用数组校验。用户报告的 `Cannot read properties of undefined (reading 'length')` 来自旧 Turbopack 模块，修复提交为 `98752a11`，已推送当前远端功能分支。冷加载后继续正式批次 #4 可正常显示 12/12、100% 和 105 条旧候选；这些候选仍未确认入账，也不代表识别准确率已验收。
2. 一条混合流水拆分出的每个经济事实现在可以独立生成并确认退款、报销和内部转账候选。接口以事实 ID 区分同一原流水中的不同子项；只传流水 ID 而该流水已有多个子事实时会明确要求用户选定一项，不会猜测。
3. 已确认关系会同时占用事实作为来源端或目标端的可分配金额，不能通过交换关系方向重复超分配。多个高置信候选争夺同一事实时自动降为黄色，程序无法唯一判断时才调用现有 AI，最终仍由用户确认。
4. 月报和图表重算按拆分事实应用影响：部分退款、报销只冲减选中的分类子项，内部转账只移除被选中的子项，其他拆分项继续正常计入。撤销关系恢复对应子项原事实类型和统计口径；关系仍存在时禁止替换或撤销拆分，避免破坏金额守恒。
5. 关系确认与撤销除保存关系修订外，也为来源、目标两个事实保存同一账本修订下的前后快照。CSV/XLSX 导出会通过 `split_component` 分配把拆分事实和关系回溯到真实来源流水，不再留下空流水 ID。
6. 这是无迁移的功能分支代码。经济事实和月报组合测试共 52 项通过，其中 11 项因当前进程未提供独立 MySQL 测试库按安全门跳过；前端定向 ESLint 和 Next 16 webpack 生产构建通过。当时工资到账仍以整条来源流水为端点，后续完成状态见 6.24。

### 6.24 2026-08-23 拆分收入事实级工资到账

1. 新增迁移 `20260823_0046_payslip_arrival_facts`：`payslip_arrival_links` 增加 `economic_fact_id` 和当前账本修订号，唯一约束从“工资条 + 原流水”改为“工资条 + 经济事实”；历史数据只有在来源流水唯一映射到一个有效事实时才确定性回填，已经拆成多个事实的历史关系不会由迁移猜测工资部分。
2. 工资到账候选按有效收入事实展开。一笔 10,000 元混合收入拆成 9,000 元工资和 1,000 元其他收入时，页面显示原流水总额、子事实金额、事实剩余可分配金额和事实 ID；程序以工资条剩余待匹配金额评分，中低置信才调用现有 AI，AI 返回也用事实 ID 区分同一原流水中的多个子项。
3. 旧客户端只传流水 ID 时，普通单一事实流水继续兼容；已经拆分成多个事实的流水返回 409 并要求明确选择，不会默认取第一项。确认仍由用户完成，工资条不会自动生成收入，也不会把候选静默写入正式关系。
4. 工资到账、退款、报销和内部转账现在共享同一经济事实的可分配容量。事实金额已经全部作为工资到账证据后，不能再把同一金额确认成退款或转账；反过来亦然。关系存在时，修改/删除来源流水、替换拆分或撤销拆分都会被阻止，必须先撤销对应关系。
5. 新增 `payslip_arrival_link_revisions`。批量确认共享一个账本修订；用户撤销、删除工资条或建立工资条修订版时，到账关系会撤销并保存前后快照、原因和修订号。恢复旧工资条不会自动恢复到账关系，必须重新确认新证据。
6. 工资条导出列现在同时包含经济事实 ID、来源流水 ID 和分配金额；现金事实视图中的工资条权益证据只匹配对应事实，不会把同一混合流水的其他子项误显示为工资到账。
7. 事实级工资到账、拆分保护、跨关系容量和撤销恢复组合测试已通过；`0046` 离线升级/降级 SQL 及完整 19 项离线迁移测试通过；工资条页定向 ESLint、TypeScript 和 Next 16 webpack 生产构建通过。正式 MySQL 仍是 `0043`，本节没有执行正式迁移、服务重启、真实外部 AI 或登录态真实工资数据验收。

### 6.25 2026-08-23 工资条候选批次恢复与确认门禁

1. 新增迁移 `20260823_0047_payslip_recognition_drafts`。工资条复用已有用户私有 `financial_import_batches` 和 `financial_recognition_artifacts` 保存批次元数据、规范化候选及完整本机 OCR 原文；新增候选草稿表保存逐份状态、结构化内容、乐观并发版本和最终工资条引用。整张 CSV/XLSX/PDF/图片仍只在当次请求内处理，不写入附件库。
2. 同一用户重新上传内容哈希和解析器版本都相同的工资条文件时，接口直接恢复原批次，不再次调用 OCR/AI，也不重复生成候选。页面进入时会读取未完成批次，展示待核对、已确认和已排除数量；用户可选择任一批次继续。
3. 候选可载入表单修改并单独暂存，退出页面后修改仍保留；“暂不处理”只把候选排除，不会生成工资条，并可撤销恢复。每次修改、排除、恢复和确认都校验候选版本，旧页面不能覆盖新状态。
4. 候选确认与正式工资条创建在同一数据库事务中完成。只有用户点击确认保存，候选才变为 `confirmed` 并记录正式工资条 ID；缺少候选版本、重复确认或已排除候选都会返回冲突。OCR、AI 和高置信候选仍不能静默入账。
5. 页面明确区分“暂存并稍后继续”和“确认并保存工资条”，恢复提示同时说明原文件未保存。批次所有候选确认或排除后才进入完成状态；已确认候选保留与正式工资条的可追溯关联。
6. 新增组合测试覆盖首次识别零正式工资条、重复上传只识别一次、批次恢复、编辑暂存、排除/恢复、完整 OCR 原文留存且无原附件、用户确认入库、正式工资条关联和已确认候选禁止再编辑；工资条、经济事实及全部离线迁移 64 项通过，更广收支相关回归 209 项通过，其中 22 项因未提供独立 MySQL 测试库按门禁跳过。前端定向 ESLint、TypeScript 和 Next 16 webpack 生产构建通过；默认 Turbopack 构建在受限运行环境中因内部 PostCSS 子进程尝试绑定端口被系统拒绝，不是业务编译错误。正式 MySQL 仍为 `0043`，本节未执行正式迁移、服务重启或真实文件登录态验收。

### 6.26 2026-08-23 绿色工资条候选原子批量确认

1. 工资条批次页面为仍待核对、置信层级为绿色且月份、应发和实发完整的候选增加勾选框；黄色、红色、核心字段缺失、已排除或已确认项没有批量入口，仍需逐份处理。
2. 用户勾选后可一次确认多份工资条，批量请求统一应用页面当前选择的 Offer、合同、预估月薪和城市。关联模式选了 Offer 或合同时，前端要求至少实际选择一份材料，不把“准备关联但未选择”静默降级为不关联。
3. 服务端在一个事务内锁定批次和候选，重新校验所有候选的所有权、版本、状态、置信层级和核心字段。批次内部同月份同发薪单位重复，或正式记录中已有同月份同单位工资条时，整批返回冲突并要求用户逐份判断补发、修订或重复导入。
4. 只有整批全部合法时才逐份建立工资条、收入事件、工资证据、材料差异和守护发现，再一次提交事务；任一项失败都会整体回滚，不留下部分工资条。候选和正式工资条仍保留一一引用，OCR 原文从服务端私有识别产物取得，客户端不能替换证据原文或伪造来源类型。
5. 组合测试已证明含一个黄色项时整批零写入，两份不同月份绿色候选可一次生成两份独立工资条并完成批次；工资条、经济事实和全部离线迁移 65 项通过，更广收支相关回归 210 项通过，其中 22 项因未提供独立 MySQL 测试库按门禁跳过。前端定向 ESLint、TypeScript 与 Next 16 webpack 生产构建通过。本节无新增迁移，正式 MySQL 仍为 `0043`，未执行正式服务重启或真实文件登录态批量验收。

### 6.27 2026-08-23 确认入账 500 修复与 `0049` 正式部署

1. 用户在正式批次 #4 勾选 17 条绿色候选后请求 `POST /api/cashflow/imports/4/confirm`，旧运行进程返回 500。根因不是 OCR 候选失效：06:07 启动的后端加载了 `544cf90e` 时的确认代码，该代码调用 `lock_financial_ledger_owner` 但未保存返回的 `owner`，第一条导入流水写修订记录时触发 `NameError`。异常发生在最终提交前，会话关闭后整体回滚；只读核对为批次版本 26、105 条候选、17 条 `ready`、0 条活动流水，没有部分入账。
2. `9684619e` 已在当前代码中正确保留账本所有者；本次又在候选确认回归中断言会生成同一用户的 `FinancialTransactionRevision` 和 `FinancialLedgerRevisionEvent`，防止“交易入库通过但修订链路未执行”的用例缺口。
3. 首次修复阶段没有直接拿真实 17 条候选做回滚实验：新建名称含 `test` 的空 MySQL schema，只放入 1 条完全合成的餐饮候选；首次往返演练依次暴露 `0049`、`0046`、`0045` 和 `0044` 在 MySQL 上删除外键支撑索引的顺序问题。修复为先删外键后删索引，整表删除则不单独删外键索引。最终 `0043 → 0049 → 0043 → 0049` 真实 MySQL 往返通过，合成确认精确生成 1 条流水、1 个经济事实和 1 条流水修订；临时 schema 随后删除。用户再次报告同一 500 后，本轮又把批次 #4 的同一组 17 条候选放入正式连接的外层事务中完整重放，服务返回 17 条确认结果且响应模型校验通过，随即回滚外层事务；独立连接前后活动流水均为 0。这是回滚复现证据，不是正式入账。
4. 离线迁移 24 项和导入服务 47 项全部通过。可回退修复与确认回归提交为 `85a0c9cb fix: make cashflow confirmation rollout reversible`，已推送到 `origin/codex/cashflow-recognition-artifacts`。未完成的图表筛选/按条件导出仍只保留为两个未提交 WIP 文件，未混入修复提交或运行服务。
5. 迁移前停止旧 8000 写入口，在 `/private/tmp/zhihu-pre0049-20260823-f6c9pxaq` 生成 0600 配对备份。SQL 大小 341,052,757 字节，SHA-256 为 `08eea10a0be5c9c668a71ce74087ac127dac50af45df2c548355bc6164dafe83`；uploads 归档大小 5,477,660 字节，SHA-256 为 `f57ec5cffb1dc81271daf499a3d28362cc367dc63180f2b8b1012ca224b1efae`。两份备份命令都成功退出且权限已核对；本轮没有把真实财务数据复制到测试 schema 做恢复演练，不将合成迁移往返写成真实备份恢复。
6. 正式 MySQL 已从 `20260823_0043` 升级到 `20260823_0049`，表数从 68 增至 71。前后均为流水总数 4、活动流水 0、导入批次 1、候选 105；迁移未生成或确认任何财务流水。后端已以已推送的 `85a0c9cb` 重启，`/api/health` 返回 `database=ok`。
7. 管理员 Chrome 登录态冷刷新 `/income` 后，工作台、图表、账本、AI 和 KNOWLEDGE 均正常读取；待核对提示仍为 1 个批次/105 条候选。打开批次 #4 后页面显示 17 条已勾选绿色候选（收入 200 元、支出 414.80 元、转账 5 笔）及可用的“确认 17 笔入账”按钮，不再显示旧 500。本轮没有代替用户点击最终确认，所以 17 条仍在候选层，不进列表、图表或 AI 计算。

### 6.28 2026-08-23 知识来源契约、AI 引用落库与 `0050` 正式部署

1. `c3da1577 feat: persist knowledge evidence in cashflow answers` 已推送 `origin/codex/cashflow-recognition-artifacts`。`knowledge_articles` 增加适用问题、适用地区、来源标题/链接、内容版本、生效/失效日期和复核时间；`cashflow_conversation_turns` 增加逐轮 `knowledge_references`。程序只把与当前问题匹配、已发布且未过期的有界知识放入模型上下文，模型返回的 slug 还必须在本次白名单中，伪造、重复或越界引用均被丢弃。
2. AI 回答中的流水、工资条和知识引用都随会话轮次固化，历史会话回读不依赖当前推荐结果。前端“回答依据”可以打开知识原文并显示来源、版本、适用地区和效力状态；KNOWLEDGE 卡片同时展示适用问题和推荐原因。新增回归覆盖“路由选择知识 → AI 采用 → 会话落库 → 历史会话读取”以及过期知识排除；本轮完整相关后端 79 项通过、11 项 MySQL 集成因未设置独立测试库按门禁跳过，前端 ESLint、TypeScript 和 Next 16 webpack 构建通过。
3. 迁移前停止 8000 写入口，在 `/private/tmp/zhihu-pre0050-20260823-o4mx7z6r` 生成 0600 配对备份。SQL 大小 341,060,341 字节，SHA-256 为 `f88245f0007a0fe26ca9734e30231d1f741552d8a6387f0b41464616fbad1bc1`；uploads 归档大小 5,477,914 字节，SHA-256 为 `06e936ea63907720cb3a533ad36f62fe3cd4790862be715effc414bfe31c6368`。SQL 已检查知识表、会话表和 `0049` 版本标记；uploads 归档无绝对路径或 `..` 成员。本轮没有把真实财务数据恢复到测试 schema，不把结构检查写成完整恢复演练。
4. 正式 MySQL 已显式绑定当前 `DATABASE_URL` 从 `20260823_0049` 升级到 `20260823_0050`。前后均为 71 张表、1 个用户、4 条历史软删除流水、0 条活动流水、1 个导入批次、105 条候选和 17 条 `ready`；31 篇知识元数据全部非空并为 JSON 数组，2 条历史会话知识引用回填为空数组。迁移没有确认候选或生成财务流水。
5. 后端已以已推送的 `c3da1577` 重启，8000 和用户实际访问的 `3000/api` 健康检查均返回 `database=ok`；OpenAPI 已包含 `CashflowKnowledgeReference` 全部字段。管理员原 Chrome 令牌已过期，本轮使用本机后端为现有 `user #1` 签发的短期令牌，经 `3000/api` 完成登录态接口验收；临时令牌文件和进程变量随后删除，没有改密码或新建账号。本轮没有新的桌面/手机视觉验收。
6. 正式账号提交了不含个人财务明细的问题“请引用相关知识说明工资里的社保和公积金应该如何核对；如果缺少我的工资条事实，请明确说明不能判断”。`AIInvocationLog #244` 记录 `SenseAudio / deepseek-v4-flash`、`success`、7934ms、4144 tokens。回答引用 `wuxianyijin`、`gongjijin-xiangjie`、`shebao-jishu` 三篇白名单知识，刷新读取会话后引用仍存在；回答明确标注当前没有工资条和已确认流水，不能判断个人是否少发、多扣。正式库因此新增 1 个会话和 1 轮问答，活动流水仍为 0，批次 #4 的 17 条绿色候选未动。
7. 现有 31 篇历史知识只能诚实显示为“职护知识库整理 / v1.0 / 全国通用 / 时效待核验”；这证明元数据和引用链路已工作，不证明每篇内容已完成法规来源、地区和时效核验。后续必须逐篇补充来源链接、适用地区、生效区间与复核时间。新的登录态桌面/手机视觉验收、真实工资条和真实微信/支付宝/银行账单质量仍未完成。

### 6.29 2026-08-23 跨月待结、年度对比、月末预测与可读报告

1. `2c9dafa2 feat: add cashflow outlook and readable reports` 已推送 `origin/codex/cashflow-recognition-artifacts`。月报现在用与主汇总相同的退款、报销、内部转账、拆分事实和辅助证据口径，分别计算所选年度截至当前月与上年同期的累计收入、支出和净结余；前端以双年条形对比展示，并明确“累计净结余不等于银行卡余额”。用户已明确不增加“我的账户”，因此系统没有臆造账户实时余额。
2. 新增退款/报销待结视图。程序遍历截至所选月份的已确认经济事实和已确认关系，计算每笔可报销支出的原金额、已结金额、剩余金额、账龄和是否跨月；疑似退款/报销但尚未关联原支出的已确认进账只显示为“进账待关联”，不自动改成退款、不冲销支出。隔离 MySQL 组合用例验证 200 元跨月垫付、已报销 80 元时只显示待报销 120 元，另有 50 元疑似退款时单独提醒而不混入普通结算关系。
3. 新增月末预测：历史月份直接展示实际值；当前月在至少有 3 天且存在已确认流水时，收入按当前确认值保守保留，支出按月内日均速度外推，给出预计支出、预计净结余和可选的总预算预计使用率；未来月份、空账本或数据不足时明确不预测。预测日期、金额和待结账龄属于时间性展示，不参与月结指纹，避免账本未变化却每天把历史月结标成过期。
4. 主工作台新增“导出可读月报”，经 `3000/api` 下载自包含、手机可读的 HTML 文件。报告包含当月已确认结果、年度同期、月末预测、退款/报销待结、程序提示、生成时间和账本修订号；不包含原件、OCR 原文、切片或未确认候选。正式管理员空账本接口返回 200、账本 r0、预测诚实降级；HTML 中确认边界存在。没有用空账本结果冒充真实财务数据验收。
5. 本轮新功能的两条隔离 MySQL 集成测试通过。更广的 58 项收支账本/经济事实回归首次为 57 项通过、1 项暴露既有修订快照把整数金额保存为 `900` 而非 `900.00`；已统一流水、事实、分配和关系快照为两位小数，失败项单独复跑通过。前端定向 ESLint、TypeScript 和 Next 16 webpack 生产构建通过；默认 Turbopack 仍因受限环境中 PostCSS 子进程绑定端口被拒绝而失败，不是业务编译错误。
6. 本轮无数据库迁移、未写正式财务流水、未改批次 #4 的 105 条候选和 17 条 `ready`。后端已重启到已推送的 `2c9dafa2`，8000 与用户入口 `3000/api` 健康检查均为 `database=ok`。新的登录态桌面/手机视觉验收、真实工资条和真实微信/支付宝/银行账单逐笔质量验收仍未完成。

### 6.30 2026-08-23 周趋势、历史月结与订阅续费守护正式部署

1. `b724f0d8 feat: expose cashflow history and weekly trends` 已推送：主图表新增月内周趋势，按 1–7 日、8–14 日等固定区间聚合已确认收入、支出和净额，点击任一周会把日期范围带入可信账本；历史月结增加版本选择与详情，能够查看当时固化的收支、净结余、未入账候选和程序提示；个人中心数据自主区域增加报告月份和“下载可读月报”，不再只有主工作台入口。
2. `b7c23f1c feat: guard subscription renewals` 已推送。`0051` 为用户已确认订阅增加续费周期、下次扣款日、自动续费和提前 0–30 天提醒；设置提醒必须先有扣款日，旧版本提交不能覆盖新版本，改回固定支出会清除订阅专属字段。前端新增独立“订阅续费守护”，按日期显示已过期、今天、7 天内或更远状态；对应月份的程序月报按具体计划日提示，不让相对天数造成历史月结每天变化。当前只做站内页面/月报提醒，不会代替用户向外部平台取消订阅，也没有冒充系统推送通知。
3. 订阅组合用例在隔离 MySQL 中验证了创建、修改、乐观并发冲突、月报提示和改为固定支出后清空字段；从空 MySQL schema 升级到 `0051` head 通过，27 项离线迁移/校验测试通过。个人中心、周趋势、历史详情和订阅前端的 ESLint、TypeScript 与 Next 16 webpack 生产构建通过。
4. 正式迁移前停止 8000 写入口。优先备份位于 `/private/tmp/zhihu-pre0051-20260823-hio1cdhx`，SQL 以 `--set-gtid-purged=OFF` 生成，大小 341,067,277 字节、0600、SHA-256 `5cebbc33e6931f2c811af93d3ed4673e55bd2f575f74a010e859b7ae2afaf22f`；uploads 归档大小 5,469,688 字节、0600、SHA-256 `4374d1fb72b2fa1d034f9b3b44484a00adbbed976edd72dff1fc2955ebe18d5b`，无绝对路径或 `..` 成员。本轮只做结构与归档安全检查，没有把它写成完整恢复演练。此前同轮另有一份带 mysqldump 默认 GTID 提示的备份，以上关闭 GTID 注入的第二份为优先恢复件。
5. 正式 MySQL 已显式绑定当前 `DATABASE_URL` 从 `20260823_0050` 升级到 `20260823_0051`。迁移前后均为 4 条历史软删除流水、0 条活动流水、0 条订阅结论和 17 条 `ready` 候选；迁移没有确认或改写任何业务数据。四个新字段及类型已只读核对。
6. 后端已以 `b7c23f1c` 重启。用户入口 `3000/api` 健康检查为 `database=ok`，现有管理员只读登录态请求可列出 0 条订阅并正常读取月报，OpenAPI 包含四个订阅计划字段。没有为了验收在正式账号制造虚假订阅。真实订阅、真实财务数据、登录态桌面/手机视觉和真实工资条/账单逐笔质量仍待用户数据验收。

### 6.31 2026-08-23 隔离登录态全链路回归与净额展示修正

1. 新建名称含 `test` 的独立 MySQL schema、独立上传目录、独立后端和前端，只用合成数据构造跨年/月/周收支、工资条与到账、退款、跨月报销、内部转账、预算、订阅和历史月结。正式 MySQL、正式批次 #4 的 105 条候选及 17 条绿色项均未改动。
2. 登录态 `/income` 在 `1440×1000` 桌面和 `390×844` 手机视口完成运行检查：页面只保留五守护全局导航，收入/支出双入口、分析、周期支出、订阅、预算、月报、年度/预测/待结、月结、账本历史、工资证据、可信账本、待处理、AI 和 KNOWLEDGE 顺序完整；浏览器控制台无运行时错误。
3. 实际上传一张 `1080×4600` 合成长截图，经本机 OCR 拆成 3 个重叠切片并全部完成，生成 26 条候选；进度展示程序确定与 AI 辅助数量，未复现 `duplicate_images.length` 崩溃。候选编辑、暂存退出、刷新后继续同一批次均通过。OCR 对少量商户和日期仍有误识别，三档门禁阻止其自动进入账本，因此本次不宣称真实账单准确率达标。
4. 修复导入弹窗关闭后主页面待办数据未刷新的问题：候选仍只留在核对工作区，但月报、月结和已确认列表后的轻提醒会立即显示 1 个批次 / 26 条候选并可恢复原批次。另修正跨月退款/报销可能形成负支出净额时的符号、周趋势、零轴、分类“跨月冲销”和月报文案，避免把冲销显示成正消费。
5. 复用现有外部 AI 完成一次合成账本问询：回答按账本 r64 解释退款 79 元、报销 850 元和待确认 299 元，并引用 5 条真实合成流水/事实；未确认 OCR 候选未进入问询。个人中心的条件导出、数据包和可读月报入口在手机登录态可见。
6. 为直接覆盖用户遇到的确认 500，在隔离库通过自然语言生成 1 条合成支出，人工核对成绿色后走同一最终确认接口成功入账；已确认流水从 13 增至 14，支出净额从 4090.30 元变为 4102.64 元，净结余同步从 16208.70 元变为 16196.36 元。该证据证明当前代码的确认和刷新闭环可用，不代表正式批次 #4 已确认。
7. 前端全量 ESLint 为 0 error、11 个既有无关 warning；Next 16 webpack 生产构建、TypeScript 和静态页面生成全部通过。正式环境仍保持 `0051`、0 条活动流水和批次 #4 的 17 条绿色候选；再次正式确认必须由用户针对该批次明确授权或亲自点击。

### 6.32 2026-08-24 正式批次确认、候选直连经济事实与手机趋势

1. 用户明确授权确认正式批次 #4 的 17 条绿色候选。确认后批次 #4 共 17 条候选进入正式账本，2026-08 统一口径为收入 200.00、支出 414.80、内部转账 4777.00、净结余 -214.80，账本修订号 17；批次中其余旧链路候选未入账。最终长截图批次 #12 仍保留 98 条待核候选、0 条确认，不能把候选数量写成真实交易数量或 OCR 准确率。
2. 本轮实现“导入候选直接归入已有经济事实”。疑似跨来源重复会展示全部正式匹配、证据来源、匹配理由、是否允许归入和目标剩余金额；用户可选择作为已有事实的辅助证据、明确作为新事实或暂不处理。同一来源重复不能借此绕过去重。已保存意图可以调整目标、金额和理由，也能改回新事实。
3. 选择辅助证据时仍需形成可追溯来源观察，因此方向、金额、日期和分类必须完整。真实页面验收发现自然语言候选缺分类时旧界面隐藏了字段，保存返回“候选数据不完整”；现已增加“补齐来源记录”，并在同一 PATCH 中显式提交用户填写字段。后端在同一锁和事务内先验证这组候选状态，再重算指纹与重复集合；不存在或方向不匹配的分类整体拒绝，不静默推断，也不留下字段或合并意图。
4. 最终确认在同一用户账本锁和数据库事务中保存来源观察流水、基础事实、`corroborating` 金额分配、流水/事实修订和统一账本修订；确认前重新校验所有权、候选指纹、用户看到的匹配集合、目标流水/事实快照、方向、币种、金额和跨来源条件。部分归入时只把未分配余额作为新事实；任一步失败整体回滚，重试幂等。所有金额边界按精确两位小数执行，多 0.01 元也会拒绝。
5. 本机登录态使用独立验收账号：先手工确认 12,000 元工资到账，再通过职护现有外部 AI 把同日同额自然语言描述生成候选。用户在页面选择工资分类、目标事实、12,000 元分配和理由后，最终确认显示“新增事实 0 笔、辅助证据 1 条”；账本由 r1 变为 r2，但收入、年度累计、月报和图表仍为 12,000 元。可信账本把第二条来源显示为“同一事实证据 · 不重复统计 / 实际计入 0.00”，关系框同时显示主记录、辅助证据和“撤销本次金额分配”。验收账号及全部数据随后通过删除账号链路精确清理。
6. 手机 `390×844` 视口下，每日趋势不再渲染桌面横向 SVG，而是按日期展示纵向收入/支出列表和明细入口；桌面继续使用完整趋势图。手机 DOM 已确认移动主导航、两个平级入口和纵向趋势列表，视口验收后恢复默认尺寸。
7. 后端导入与经济事实相关 92 项测试通过，覆盖完整/部分归入、同来源禁止、缺分类同请求补齐、无效分类拒绝、金额多 0.01 元拒绝、目标变化、原子回滚、幂等、统计不重复和撤销。前端目标 ESLint、TypeScript、全量 ESLint（0 error、11 个既有无关 warning）以及 Next 16 webpack 生产构建均通过，29 个页面全部生成。`git diff --check` 和最终提交边界在推送前再次执行；未跟踪 `zhihu/artifacts/` 不纳入提交。

### 6.33 2026-08-24 单批次删除与识别产物精确清理

1. 新增用户所有权隔离的 `DELETE /api/cashflow/imports/{batch_id}`，并以 `expected_version` 防止旧页面误删已变化批次。批次、候选、规范化行、完整 OCR 原文和派生切片在同一数据库事务中删除；已经确认的正式流水、经济事实及其账本口径继续保留。
2. 待删附件元数据先建立持久清理任务，事务提交后才物理删除。底层文件删除失败不会把已删批次复活，而是返回 `retry_pending` 供现有清理 worker 重试；被其他批次或产物引用的共享附件不会误删。数据库阶段失败则批次、候选、产物和清理任务全部回滚。
3. 当前批次头部和“继续未完成批次”列表均提供永久删除入口。二次确认逐项说明删除范围、不可恢复和已确认正式账本保留边界；成功后当前工作台、未完成列表和主页面待处理提醒同步刷新。个人中心清空业务数据与删除账号的确认文案同时补齐收支账本、识别批次、OCR 文字和切片范围；账号删除成功后立即跳转欢迎页。
4. 隔离登录态通过真实页面用职护现有外部 AI 生成自然语言候选批次 #14，从当前批次入口删除后账本仍为 0；相同内容再次提交生成新批次 #15，证明旧内容唯一键已经释放。刷新页面后从最近批次列表删除 #15，主页面不再显示待核提醒。`390×844` 下视口宽度和页面滚动宽度均为 390，没有横向溢出。验收账号随后通过删除账号接口精确清理，重新加载进入 `/welcome`；正式批次 #4 和 #12 未改动。
5. 新增 10 个后端删除专项用例，覆盖未确认与已确认批次、同内容重新上传、历史和派生附件、共享附件、非收支附件保护、工资条批次隔离、物理清理失败重试、版本/所有权以及数据库原子回滚。收支导入、经济事实和长图相关共 115 项测试通过；前端目标 ESLint、TypeScript 和 Next 16 webpack 构建通过，29 个页面全部生成。

### 6.34 2026-08-24 跨批次正式流水疑似重复 AI 解释

1. 候选被程序标为与历史已确认正式流水 `possible_duplicate` 后，批次进入或候选修改会自动请求现有 AI 补充评议。模型只能针对程序给定的候选—流水配对返回“较可能同笔 / 较可能不同 / 仍不确定”和一句可核对理由，不能改变候选状态、置信层级、合并目标、金额或正式账本。
2. 前端在每条历史匹配旁分别展示程序匹配理由、AI 倾向和 AI 理由；AI 不可用或输出无效时明确显示“AI 未能判断”。界面不根据 AI 自动选择单选项，用户仍须明确选择“归入已有经济事实”“作为新事实”或“暂不处理”。
3. 外部模型调用前释放数据库事务，只发送有长度上限的脱敏商户、摘要和程序对比特征。返回后重新取得用户账本锁，并重验账户数据清理版本、批次、候选状态、候选字段和目标流水完整快照；调用期间任何内容变化都会阻止旧结论落库。已经保存的结论也绑定上下文哈希，后续目标或候选变化时立即隐藏并等待重新评议。
4. 单次最多评议 30 个配对并可分批续跑；一个候选若同时关联超过上限的历史流水，不截断成误导性局部结论，而是保留给人工处理。当前只覆盖“未确认候选 ↔ 已确认历史正式流水”；未确认候选之间的一对多、多对一跨批次 AI 配对仍是后续缺口。
5. 新增两项服务测试，证明有效 AI 结果只保存解释且正式流水、经济事实和账本修订数不变，重复请求复用当前结果，目标变化后旧结论失效；无效模型输出保持 `possible_duplicate` 并降级人工。后端全量 406 项通过（90 项因未配置独立 MySQL 集成环境跳过）；前端全量 ESLint 0 error、11 个既有无关 warning，TypeScript 通过，Next 16 webpack 生产构建成功生成 29 个页面。默认 Turbopack 在当前受限环境仍因 PostCSS 子进程绑定端口被拒绝，非业务编译错误。本节未调用真实外部 AI、未重启正式服务、未改正式批次 #4 或 #12。

### 6.35 2026-08-24 未确认候选之间的跨批次重复核对

1. 候选响应新增结构化 `duplicate_candidate_matches`，不再只把对侧候选 ID 藏在证据 JSON 中。用户可以看到对侧批次、候选 ID、源文件行号、收支方向、金额、日期、来源、商户/摘要和程序匹配理由；同批编辑后新形成的疑似重复也会重新计算并保存精确对侧候选集合。
2. 批次进入和疑似重复候选更新会通过现有重复 AI 接口同时处理“候选 ↔ 正式流水”和“候选 ↔ 未确认候选”。后者只向现有 AI 发送程序筛出的最小脱敏配对，返回“倾向同一笔 / 倾向不同 / 仍不确定”和理由；不选择保留哪条、不排除、不确认、不合并、不写账。
3. 核对模态框把未确认对侧候选与正式流水分区展示。由于两边都尚未建立经济事实，界面不会提供虚假的直接合并；用户可以明确当前候选是另一笔并继续核对，暂不处理，或返回列表排除其中一条。只有用户确认后的候选才可能进入正式账本。
4. AI 结论绑定双方候选和批次来源的完整上下文哈希。模型调用前释放事务，返回后在用户账本锁内重验账户清理版本、双方候选字段、状态、版本和批次；对侧在调用期间变化时结果直接丢弃，之后任一侧变化也会隐藏旧结论。单个候选超过 30 个可能对侧时不做截断式局部判断，保留人工核对。
5. 新增和扩展测试覆盖跨批次候选结构化展示、AI 理由、零正式写入、对侧修改后结论失效、模型调用期间变化不落旧结论，以及同批编辑后精确对侧 ID 更新。后端全量 407 项通过（90 项因未配置独立 MySQL 集成环境跳过）；前端全量 ESLint 0 error、11 个既有无关 warning，TypeScript 与 Next 16 webpack 生产构建通过，29 个页面全部生成。真实外部 AI、登录态桌面/手机视觉和正式批次数据均未在本节操作。

### 6.36 2026-08-24 工资问询采用用户确认的材料适用语境

1. 收支 AI 的工资上下文不再只有合并后的“Offer/合同有几项不同”。每份当前工资条会同时带入关联 Offer、劳动合同和补充协议的材料类型、标题、用户选择的适用状态、排序、备注、总体比较状态和逐字段参考值/工资条值；全部文字继续经过长度限制和财务脱敏，不发送原文件或 OCR 原文。
2. `preferred` 只表示用户当前选择的优先核对参照，`reference` 是普通对照，`unresolved` 是适用性待确认。提示词明确禁止 AI 把这三种用户状态写成材料法律效力，禁止普通对照或待确认材料覆盖优先参照；存在多份优先参照或材料冲突时，AI 必须逐份说明而不能自行选一份正确材料。
3. 新增问询测试向上下文同时放入优先劳动合同和待确认补充协议，验证两份状态、备注和约束确实进入模型提示，并且模型返回的工资条引用仍只能来自当前有效上下文。后端全量 408 项通过（90 项因未配置独立 MySQL 集成环境跳过）；本轮无前端代码、无数据库迁移，也未调用真实外部 AI 或修改正式数据。

### 6.37 2026-08-24 复杂绩效、奖金和扣款证据边界

1. 合同薪资条款出现绩效、奖金、提成、年终、浮动或考核语义时，程序提取关键词附近的有限条款片段，并与工资条已列绩效、奖金并排展示；出现扣款、罚款、迟到、早退、旷工或请假扣款语义时，同样展示条款片段和工资条考勤、餐费、其他扣款。
2. 这些文本规则统一保持 `unknown`，不会因为工资条恰好有一个数字就推导公式或判断兑现。绩效/奖金至少还需适用周期、目标值、考核结果、触发条件、计算公式和本月兑现明细；扣款至少还需考勤事实、适用制度、计算基数、计算过程和工资条明细。
3. 守护摘要为上述证据缺口生成可直接询问 HR 的材料清单式问题，同时继续禁止使用“少发”或“多扣”作为未核清问题的既成结论。Offer 奖金只有明确为每月固定数字时才可与本月奖金直接比较；年终奖或按年度绩效、经营结果决定的奖金保持未知，不作为本月应发金额。
4. 新增两项测试覆盖复杂合同同时含绩效公式和考勤扣款、工资条实际项目并排展示、HR 问题生成和无少发/多扣结论，以及非月度 Offer 奖金不计入本月。后端全量 410 项通过（90 项因未配置独立 MySQL 集成环境跳过）；本轮无前端代码、无迁移、无真实外部 AI 和正式数据操作。

### 6.38 2026-08-24 知识展示与 AI 引用的严格分层

1. 知识文章的“可在页面展示”与“可作为 AI 通用知识依据”现在是两个独立门禁。旧文章仍可以作为明确标注的待核验参考，但只有同时具备 HTTP(S) 来源链接、非默认来源、适用地区、复核时间和当前有效期的文章才会进入收支 AI 上下文及引用白名单。
2. 已过期、尚未生效、无可访问来源、无复核时间或无效力期的文章全部降为 `reference_only`；后端响应同时返回具体阻断原因。这使现有 31 篇旧内容不会再因为“未过期”就静默变成模型的权威前提。
3. 收支守护的 KNOWLEDGE 卡片明示“来源已复核，可作为 AI 通用知识依据”或“待核验参考，不会作为 AI 判断依据”，避免用户把页面可读性误解为内容已核验。
4. 定向用例覆盖已复核、待核验、已过期和未生效四种状态。后端全量 410 项通过（90 项因未配置独立 MySQL 集成环境跳过）；前端 ESLint 为 0 error、11 个既有无关 warning，TypeScript 和 Next 16 webpack 生产构建通过，29 个页面全部生成。本轮无迁移、无真实外部 AI、无正式数据写入、无正式服务重启；新门禁仍待部署后的正式登录态验收，31 篇旧知识仍需逐篇核验和补全官方来源。

### 6.39 2026-08-24 导入候选精确匹配拆分经济事实

1. 导入查重不再只索引正式流水总额。程序现在沿已确认的 `primary` 和 `split_component` 金额分配索引每个经济事实，以资金方向、事实金额、事实日期和事实/原流水文本执行有界查重。例如 100 元混合流水拆成 35 元餐饮和 65 元其他支出后，新的 35 元候选会精确命中餐饮事实，不会把整笔 100 元当成合并目标。
2. 候选响应显式返回事实 ID、事实名称、说明、金额和是否为拆分事实。前端重复核对卡片分别展示原流水总额与当前拆分事实金额；单选标识同时绑定流水 ID 和事实 ID，同一流水下多个拆分项不会互相覆盖。
3. AI 疑难解释上下文升级为 v2，同时携带脱敏的事实标题、说明、金额、日期和拆分标记；AI 仍只给“倾向同笔/倾向不同/不确定”与理由，不选目标、不改金额、不写账。返回后依然在用户账本锁下重验候选、原流水、具体事实和数据清理版本。
4. 保存归入意图和最终确认都绑定具体事实 ID、当时快照和完整匹配集合。拆分事实已变更、撤销、不再属于该流水或新增匹配时都会阻止旧确认。专项用例验证 35 元微信证据归入 100 元混合流水的 35 元餐饮事实后，已确认支出仍为 100 元而不是 135 元。
5. 收支导入服务 81 项测试通过，后端全量 411 项通过（90 项因未配置独立 MySQL 集成环境跳过）；前端 ESLint 0 error、11 个既有无关 warning，TypeScript 和 Next 16 webpack 生产构建通过，29 个页面全部生成。本轮无迁移、无真实外部 AI、无正式数据写入、无正式服务重启。

### 6.40 2026-08-24 恢复批次前按当前可信账本重新查重

1. 未完成批次不再永久停留在首次 OCR/导入时的查重快照。用户点击“继续核对”后，前端会先请求 `POST /api/cashflow/imports/{batch_id}/duplicate-refresh`，后端在用户账本锁下以当前正式流水和已确认经济事实重新扫描。
2. 旧批次生成后才发生的混合流水拆分现在会被主动发现。新出现的事实匹配会撤销旧的“作为新事实”接受状态或合并意图，把候选退回 `possible_duplicate`；同来源且外部流水号已入账时则直接标为 `exact_duplicate`。两种情况都不会新建流水或经济事实。
3. 重新扫描结果与用户已看到且已确认的匹配集完全相同时保持幂等，不重复改版本、不要求重复确认。新增匹配会自动进入既有“程序筛选 → AI 辅助理由 → 用户确认”链路；预刷新暂时失败时，页面明确提示且最终入账锁内仍会强制重验。
4. 同一原流水下出现多个同额拆分事实时，后端不再根据排序猜测目标，必须收到明确的事实 ID 才保存归入意图。同时修复了仅经济事实查重桶超过有界扫描上限时可能引用错误水位对象导致 500 的边界；超限后现保持人工核对并展示原因。
5. 新增五类回归覆盖拆分事实精确归入、多个同额拆分事实必选具体目标、旧批次恢复后发现新拆分事实、后出现的同来源精确流水以及事实查重桶超限降级。收支导入服务 85 项通过；后端全量 415 项通过（90 项因未配置独立 MySQL 集成环境跳过）；前端定向 ESLint 和 TypeScript 通过，全量 ESLint 为 0 error、11 个既有无关 warning，Next 16 webpack 生产构建成功生成 29 个页面。本轮无迁移、无真实外部 AI、无正式数据写入、无正式服务重启。

### 6.41 2026-08-24 长截图核对减负、候选合并与商户识别门禁

1. 长截图识别进度和逐片诊断现在可收起、重新展开；识别结果不变，只减少完成后大段技术状态对核对列表的挤占。候选编辑新增“这是我自己的银行卡、微信或支付宝之间转钱”快捷入口，选择后按内部转账处理并明确不计收入、支出和净结余，不要求用户在消费分类中硬选一项。
2. 相邻切片把同一条交易切成两个待确认候选时，重复核对不再只有“归入已入账事实 / 明确不是同一笔 / 排除”三种容易歧义的替代操作。用户可明确选择另一个尚未入账候选并“合并为一笔候选”；后端只允许同用户、同批次、尚未处理且来源切片相邻或重叠的候选合并，保留两侧切片坐标和原始证据。合并不创建正式流水或经济事实，并可撤销恢复两条候选。
3. 程序商户门禁不再接受分类标题、版权符号或短拉丁噪声作为交易对方，例如 `餐饮`、`© te a` 不会再覆盖图片中实际的小字商户。现有 AI 只可在金额、日期和方向保持不变的前提下，用清晰商户名替换低质量程序结果；仍无法判断时保留人工核对。新增 `通讯` 支出分类和迁移 `20260824_0052`，手机话费、流量、宽带等不再被迫归入家庭或其他支出。
4. 本节最初评估过 PaddleOCR 主识别加云端兜底；用户随后明确取消 PaddleOCR，选择腾讯云 `GeneralAccurateOCR` 作为长截图目标主识别服务。本机只保留校验、切片、程序配行和故障降级。当前运行时仍是 Tesseract，腾讯云代码、密钥、真实调用、费用门禁和新隐私披露尚未落地，不能把方案变更写成已接入。
5. 相关后端 172 项测试全部通过，包含商户质量替换、通讯分类、相邻切片候选合并和撤销；迁移 head 为 `20260824_0052`，离线升级/降级专项通过。前端 ESLint 0 error、TypeScript 通过，Next 16 webpack 生产构建成功生成 29 个页面。代码未提交或推送，`0052` 未应用到正式 MySQL；本节未调用真实外部 AI/OCR、未修改正式批次和账本、未重启正式服务，仍待本机登录态和真实长截图逐笔验收。

### 6.42 2026-08-24 腾讯云 OCR 方案确认与配置文档

1. 用户明确取消 PaddleOCR，目标改为腾讯云 `GeneralAccurateOCR` 直接识别职护本机生成的长截图切片。OneBill 只提供腾讯 OCR 坐标配行、日期继承和重叠去重参考，不移植其 Key、配置或运行体系；现有 AI 仍只解释疑难结果，用户确认前不写正式账本。
2. 新增 `docs/tencent-cloud-ocr-configuration.md`，写明实名认证和服务开通、免费资源包、关闭后付费、专用 CAM 子用户、单接口权限、密钥安全、目标环境变量、图片出站、验收和停用。每月 1,000 次免费额度是当前云厂商政策且按 API 请求计算；一张拆成 14 片的长截图正常至少约消耗 14 次，不能写成“每月免费识别 1,000 张长截图”。
3. 隐私边界已纠正：当前 Tesseract 运行时仍是原图不出站；未来启用腾讯 OCR 后，整张原文件依然不长期保存，但派生识别切片会发送给腾讯云。发送图像前无法先靠 OCR 脱敏，前端必须重新披露处理方、发送内容、用途和留存边界。
4. 本节只确认方案并更新文档，没有安装腾讯 SDK、没有把环境变量接入代码、没有配置或读取 SecretId/SecretKey、没有开通腾讯云服务、没有调用外部 OCR，也没有修改数据库或正式数据。完成云端准备不等于运行接入；下一步须在代码中实现调用、免费额度保护、调用审计、失败降级和新隐私交互后再做合成图及真实长截图验收。

### 6.43 2026-08-24 腾讯云高精度 OCR 功能分支接入

1. 用户确认已完成腾讯云侧准备，并已在自己的安全位置保存 SecretId 和 SecretKey；本轮没有要求用户在聊天中发送密钥，也没有读取、打印或提交密钥。后端真实运行仍需由用户把密钥写入被 Git 忽略的 `zhihu/zhihu-backend/.env`。
2. 功能分支已使用腾讯云官方模块化 Python SDK 接入 `GeneralAccurateOCR`。普通图片 OCR 和长截图逐片 OCR 都会在开关启用时优先调用腾讯云，接收文字行、置信度和多边形坐标；长截图候选定位优先使用云端文字坐标，无法取得坐标时保留原有彩色图标近似定位。
3. 新增 `TENCENT_OCR_ENABLED`、区域、超时、单批云端切片上限、月度软上限和 Tesseract 降级开关。默认仍关闭腾讯云；默认单批最多 40 个云端切片、月度 900 次软停止、35 秒请求超时，云端失败时可明确记录降级原因并使用本机 Tesseract。调用审计只保存提供商、接口、状态、耗时和请求编号，不保存图片、OCR 正文或密钥。
4. 前端隐私说明和后端授权提示已根据实际运行能力动态展示：腾讯模式会明确告知派生图片或切片发送至腾讯云，整张原文件不长期保存；AI 仍只接收疑难的脱敏文本；任何 OCR 或 AI 结果仍只生成候选，用户确认前不进入正式账本。
5. 官方 SDK 已安装到当前后端虚拟环境。腾讯服务单元测试、普通图片接入和长截图接入共 61 项定向后端测试通过；收支导入前端 ESLint 和 TypeScript 检查通过。
6. 用户随后把密钥写入被 Git 忽略的后端 `.env`。当前功能分支后端已重启并通过就绪检查；能力接口实际返回“腾讯云高精度 OCR 已配置，本机 OCR 可在失败或额度不足时降级”。4 次预期服务级真实腾讯调用全部成功；首次在密钥开启状态运行回归时，测试夹具又把 43 个伪图片发送给腾讯并得到 `FailedOperation.ImageDecodeFailed`，实际腾讯请求和软额度计数均应按 47 次计算。失败审计作为真实调用证据保留，不删除、不改写。
7. 第一次真实裁剪调用返回 34 行文字、34 行坐标和腾讯 RequestId，平均置信度 99.15%。真实 `794×18578` 长截图按生产切片算法拆成 14 片，抽检第 1、8、14 片：腾讯与 Tesseract 的金额 token 数分别保持 11、13、4 一致；腾讯中段中文字符为 86、Tesseract 为 30，末段为 22 对 11，三片腾讯结果均完整返回逐行坐标。A/B 没有创建导入候选或正式流水。
8. 腾讯模式下旧 AI 内部提示“这是本地 OCR”已改为中性且准确的“这是 OCR 后并脱敏的文字”，避免提供商语境误导。普通图片、长截图和导入服务测试现会在每个测试夹具中默认关闭腾讯外呼，只有腾讯专项用例显式启用并 mock SDK；同一组 150 项测试重跑通过，审计仍为 47 条，证明没有继续触发云端请求。前端定向 ESLint 和 TypeScript 通过。
9. 尚未完成的边界是：完整 14 片尚未通过登录态界面生成候选，真实商户、日期继承、跨片去重和逐笔准确率尚未由用户验收；腾讯控制台用量尚未目视核对。浏览器只打开到未登录页面，没有猜测密码或绕过登录。本轮除新增腾讯调用审计外没有写正式业务数据，没有提交或推送代码；完成上述验收前不得标记为完整功能上线。

### 6.44 2026-08-24 管理后台统一服务配置与 OCR 审计

1. 管理后台导航和页面语义从“AI 配置”升级为“服务配置”。现有 AI 模型仍按原有数据库加密配置维护；腾讯云 OCR 增加只读运行卡片，分别展示是否启用、密钥是否完整、接口、区域、超时、单批调用上限、本月应用审计次数、1000 次免费资源额度、900 次应用保护阈值、100 次安全预留、最近调用状态和 Tesseract 降级开关，页面和接口均不返回 SecretId/SecretKey。
2. 服务调用明细统一纳入腾讯 OCR。后端为现有英文 feature key 返回中文功能名和中文筛选项；OCR 用量按“次请求”显示，系统发起的图片识别显示为“系统任务”。桌面表格压缩为时间/主体、能力/页面功能点、供应商/模型、结果、耗时/用量、成本/错误六列，状态与能力标签禁止窄列逐字断行；窄屏改为卡片。
3. 筛选区不再挤在标题右侧或让单个下拉框无边界拉伸。标题说明独占一行，能力类型、页面/功能点、调用状态在下一行按 `160px / 弹性宽列 / 160px` 排布；桌面保持同一行，手机按视口自然纵向排列。
4. 新增只读配置操作记录接口和页面，复用 `ai_configuration_audits` 展示操作时间、操作人、中文操作名称、服务/模型、启停及是否更新密钥。腾讯 OCR 由环境变量维护，不伪造网页修改日志；真实 OCR 调用只进入调用明细。
5. 修复前端热更新早于后端重启时，旧版 `/admin/ai/config` 缺少 `tencent_ocr` 导致读取 `display_name` 白屏的问题。页面在滚动升级短窗口使用明确的“等待后端刷新”占位，升级后自动显示真实值。
6. 新后端已在本机 8000 端口重启并通过 `database=ok` 就绪检查。使用现有管理员 `user #1` 的短期只读验收令牌直接核验三个接口：腾讯 OCR 为已启用并配置；管理页现分别展示 1000 次免费资源额度、900 次应用保护阈值和 100 次安全预留，不再把 900 伪装成腾讯总额度。调用日志中文功能名、`requests` 单位和配置操作记录均正确。临时令牌未输出并已删除。前端 ESLint 和 TypeScript 通过，仅保留同一管理文件中两条既有无关 warning。浏览器安全策略拒绝向新标签注入临时令牌，本轮没有绕过；因此管理页视觉结果仍需用户在现有管理员登录标签刷新后确认，不能写成浏览器 UI 已验收。
7. 本节没有触发新的腾讯 OCR 或其他外部 AI 调用，没有修改正式收支数据，没有提交或推送代码。后端定向 MySQL 集成用例因未配置独立测试数据库按既有安全门跳过；接口级正式库只读验收不能替代该集成测试。

### 6.45 2026-08-24 admin 收支数据清理与腾讯全图验收批次 #18

1. 用户明确授权清理 admin 账号的收支守护业务数据并重启项目。清理前确认目标为管理员 `user #1`；前端 3000 和后端 8000 的进程工作目录分别为当前仓库 `zhihu-frontend` 与 `zhihu-backend`，分支为 `codex/cashflow-recognition-artifacts`。项目通用 `/auth/data` 会同时删除其他守护数据，因此本轮没有使用该宽范围接口。
2. 在前后端停止且端口释放后，以 admin 账本锁和单一事务精确删除：1 个导入批次、28 个识别产物、105 个候选、42 条正式流水、42 个经济事实及其分配、42 条流水修订、42 条账本事件、1 个收支问答会话/回合、2 条薪资计算和 14 个收支派生切片附件。14 个附件清理任务全部完成、0 失败；工资条、预算、月结和固定收支当时均为 0。
3. 清理后再次核对收支主要表均为 0。admin 账号保留；1 个职业案例、1 个 Offer、4 份合同、206 条归属于 admin 的调用审计和 1 份服务配置数量保持不变。数据代次从 1 增为 2、账本修订从 60 墤为 61，用于阻止清理前开始的异步任务回写旧数据。
4. 项目随后从同一工作目录重新启动，前端代理和后端均返回 `database=ok`。用户立即上传真实 `794×18578` 长截图，形成新的批次 #18；这不是清理残留。14/14 个切片全部由腾讯云 `GeneralAccurateOCR` 完成、0 失败，批次进入 `review_ready`，生成 158 条候选：94 条待确认、8 条疑似重复、56 条格式或必要字段问题、0 条自动入账。调用审计由清理前历史 47 次增加 14 次至 61 次，正好对应本轮 14 个云端切片请求。
5. 批次 #18 的识别链路已经完成，但 158 条候选的逐笔正确率、跨片重复、日期继承、商户名称、三级颜色及批量核对体验仍由用户继续验收。不得把“14 片 API 成功”写成候选质量已经通过，也不得在用户核对期间再次清理该批次。

### 6.46 2026-08-24 切片详情、来源框选恢复与腾讯坐标配行修复

1. 用户在批次 #18 验收中确认腾讯 OCR 对商家名称的识别明显优于旧链路，同时指出三类绿色候选不可信：`尔海麦经典面片 -2157`、`美团平台商户 -520`、`黄大妈超级木桶饭 -200`。正式库只读核对证明腾讯原始文字和坐标本身正确，错误发生在职护前置配行：微信账单的金额与分类位于同一横行、商户位于下一行；旧程序在商户之后寻找金额，系统性拿到了下一笔金额。例如南北药行本应为 -87 却拿到 -23，尔海麦本应为 -23 却拿到下一笔转账 -2157。
2. 腾讯 OCR 结果现先按视觉纵坐标聚类：日期标题和同一横行的日收支汇总合并成上下文并排除；每条交易把同一横行的分类/方向/金额与紧邻下一行的时间/商户合并为一个逻辑行，再交给确定性规则和既有 AI。通用分类前缀从商户名中剥离，转账“来自/转给”前缀不会污染对方名称。专项回归证明 `尔海麦经典面片 ↔ -23` 保持支出，`gorgeous lady ↔ -2157` 保持内部转账，不再串行。
3. 候选来源证据的 500 已定位并修复：服务实际返回 `precision=ocr_text_line`，旧响应 schema 只允许 `approximate/slice_only`，导致 FastAPI 序列化失败并在前端显示 `Internal Server Error`。后端/前端契约现显式接受腾讯文字行精度，候选详情恢复派生切片和黄色框选，并说明框选依据腾讯文字坐标向上下扩展覆盖同一交易。
4. 长截图进度中每个切片增加“查看详情”。用户可浏览实际送入 OCR 的私有派生切片、原图纵向范围、供应商/模型、文字行数、平均置信度、候选数量、彩色图标行下限和按坐标重组后的 OCR 原文，并用上一片/下一片连续排查。新增用户私有详情与图片接口；整张原图仍不保存，接口不向其他用户暴露切片。
5. 依据腾讯官方高精度接口文档，图片 Base64 后须不超过 10MB。真实 `794×18578` 原图约 2.51MB、Base64 约 3.35MB，因此额外消耗 1 次免费调用完成整图 A/B：整图只识别 5 个日期标题，10 个抽样商家命中 4 个，平均置信度约 84.9%，确定性规则仅形成 24 个稳定候选且留下 96 个低置信锚点。当前继续使用 14 片质量优先方案；未来可独立评估约 7 个较大自适应切片，但必须保住日期、金额、商家和交易行召回。
6. 用户在修复加载前主动删除了错误批次 #18；删除请求成功，旧候选没有入账，也没有被后台静默改写。后端随后从当前 `zhihu-backend` 目录在 8000 端口重启，前端 3000 代理和后端均返回 `database=ok`。腾讯专项、AI OCR 规则和长图服务定向共 36 项通过，长图服务全量 28 项通过；前端收支导入定向 ESLint 与 TypeScript 通过。新坐标配行、切片详情和来源框选仍待用户重新上传同图完成登录态目视及逐笔准确率验收。

### 6.47 2026-08-24 通讯分类、分析图表与收支知识正式落地

1. 用户再次验收时发现“通讯”仍未出现在正式分类下拉框。代码和 `0052` 迁移此前已经存在，实际根因是正式 MySQL 仍停在 `20260823_0051`，不是前端把分类漏写死。执行前只读确认正式库有 52 条活动流水、1 个导入批次和 109 条候选；随后生成精确 SQL 备份 `/private/tmp/zhihu-pre0052-6stb69h3/zhihu.sql`，大小 331,015,823 字节、权限 0600、SHA-256 `71205a61bf1320b3f93e0987ef2618f9d83373967fd4abd2b864b886785fdbb7`，再显式绑定当前 `DATABASE_URL` 升级到 `20260824_0052`。
2. `0052` 正式执行后，系统支出分类“通讯”以 `id=21`、`sort_order=65`、启用状态进入分类表，顺序位于“家庭”和“购物”之间；前端仍从服务端分类目录读取，不再依赖静态列表。登录态浏览器已确认编辑下拉框出现“通讯”。迁移前后的 52 条活动流水、1 个批次和 109 条候选均未变化。
3. 收支主分析区已复用现有 `echarts` / `echarts-for-react` 重新设计：每日趋势改为支出柱形与收入平滑面积线，周内趋势改为收入/支出分组柱形与净结余折线，支出分类改为环形占比图和排名明细，商户排行改为横向金额条形图。退款和报销冲销使用蓝色语义且不混入消费占比；没有上月可比数据时显示“暂无上月基线”，不绘制误导性零值对比。原有日期、分类和商户下钻保持不变，登录态桌面页面已目视验证图表渲染以及“交通”分类下钻到可信账本；手机每日列表逻辑保留，但本轮没有重新完成手机端整页视觉验收。
4. `/income` 的 KNOWLEDGE 卡片已对齐其他守护模块：首页只展示阶段或分类、标题和摘要，保留文章详情抽屉及“查看全部知识 →”，不再显示“推荐依据”、来源、版本或审核元数据。底层适用问题、地区、来源、版本、时效和 AI 引用门禁仍保留，治理信息没有被删除。
5. 新增迁移 `20260824_0053_cashflow_knowledge_articles`，只幂等补充 6 篇缺失文章，回滚也只删除这 6 个 slug：账户互转不算普通收支、退款/报销与普通收入区分、信用卡还款避免重复记账、工资条应发/扣款/实发/到账口径、长截图查重核对、按已确认收支做预算。正式执行后知识总数由 31 增至 37。六篇内容属于内部产品和记账口径，没有伪造权威外部链接，保持 `reference_only`，不会进入 AI 权威知识上下文；登录态页面已打开其中一篇新文章并验证详情抽屉。
6. 正式 MySQL 当前迁移头为 `20260824_0053`。只读核对批次 #19 为 `review_ready`，109 条候选中 52 条 `confirmed`、49 条 `needs_review`、8 条 `possible_duplicate`；页面仍有 57 条待处理，系统没有借迁移或图表改造自动确认它们。当前页面显示 2026-08 已确认收入 700.10、支出 3583.87、内部转账 9795.00、净结余 -2883.77，账本修订号 113。
7. 6 篇知识目录/详情/引用边界与 `0053` 离线升级回滚共 4 项测试通过；前端改动文件 ESLint、TypeScript `--noEmit` 和 `git diff --check` 通过。整个过程没有新增腾讯 OCR 或其他外部 AI 调用，没有修改已确认业务流水，没有提交或推送代码，也没有启停当前服务。

### 6.48 2026-08-24 v13 完整行切片、v5 分层分类与正式登录态验收

1. 长截图切片标识升级为 `cashflow-long-image-v13`。程序不再只按固定高度落刀，而是在目标高度附近寻找安全空白间隙，尽量让每片以完整交易行结束；相邻片仍保留必要重叠，并用日期、金额、方向、时间和交易文本做确定性跨片合并，避免同一交易因重叠识别成两条。
2. 支出分类规则标识升级为 `cashflow-ocr-rules-v5`。分类采用可扩展的“来源标签 + 商户/说明语义”分层：与系统分类明确等价的账单标签直接归类；`服务 + 美团平台商户` 等非等价但有较强语义的模式只形成按建议分类聚合的批量确认组，不擅自变绿；程序仍无法稳定判断时调用现有 AI，AI 仍不确定再由人工逐笔处理。程序和 AI 都不能自行写入正式账本。
3. 真实腾讯 OCR 批次 #22 完成 14/14 个切片、0 失败，生成 99 条候选且 0 条缺日期；程序自动合并 9 组相邻片重叠记录，没有把这些确定性同笔记录继续交给用户逐条确认。
4. 分类减负结果在同一批次形成三个可批量确认组：餐饮 19 条、其他支出 5 条、交通 2 条；剩余真正缺商户、无法获得足够语义的例外为 4 条。上述数量是待用户确认的建议，不是已确认分类或正式账本数据。
5. 该 admin 环境保留旧正式账本，导致批次 #22 被标出 43 条疑似重复和 5 条已存在。当前绿色、黄色、红色数量混入了历史查重结果，不能作为清洁账本下识别质量或绿色率结论；后续正式验收清理 admin 收支业务数据后，应以同图重新导入的分布复验。
6. 已在正式 admin 登录态完成桌面与 `390×844` 手机验收：批次共同问题、餐饮/其他支出/交通三个分类组、逐片详情、候选来源截图定位、手机候选卡片布局均可操作，浏览器控制台 0 错误。本轮没有点击任何分类确认，也没有确认候选入账。
7. 数据边界保持不变：整张上传原件不保存；系统只保留续办、定位和去重所需的派生切片、OCR 原文和结构化候选。候选无论来自规则还是 AI，均须用户确认后才进入正式账本，未确认项不参与正式列表、图表或分析 AI。

### 6.49 2026-08-24 清洁账本重跑、同片误查重修复与批量未知商户确认

1. 用户明确授权清空 admin 的收支守护测试数据后重新验收，且说明这些业务数据均可变动。清理仍只作用于收支域，没有使用会连带删除其他守护数据的宽范围接口：删除 52 条流水、52 个经济事实及分配、52 条流水修订、52 条账本事件、319 条候选、84 个识别产物、3 个导入批次和 42 个收支附件；42 个物理清理任务全部完成、0 失败。admin 账号、1 个职业案例、1 个 Offer、4 份合同、389 条 AI/OCR 调用日志和 1 份服务配置保留，数据代次升至 3、账本修订号升至 114。
2. 清洁账本上的真实腾讯 OCR 批次 #23 首次完整跑出 99 条候选、0 条缺日期，但暴露出 4 条同日同额的 520 元交易被候选 sibling 查重误判。来源切片证明它们是同一图片、同一切片内四条独立可见交易行。通用查重现增加来源行门禁：同批次、同图片、同切片且 `ocr_line_index` 明确不同的候选不得互相标记为重复；相邻切片的同一业务行、共享同一 OCR 行、跨批次或缺定位证据的候选仍按保守查重处理。门禁同时覆盖初次生成、编辑重算、活跃 sibling 查询和确认前复核。
3. 批次共同问题新增“保留为未知交易对方”。只有日期、收支方向、正金额、CNY 币种和分类都完整，且交易对方与说明确实为空、剩余提示仅为商户缺失或相应 AI 核对提示的候选才进入该组；用户必须一次显式勾选确认，系统记录 `merchant_resolution=confirmed_unknown`，不伪造商户、不吞掉其他问题，也不写正式流水。若候选仍有分类、金额、方向、日期、币种或重复问题，后端会原子拒绝整个选择。
4. 为排除旧批次状态影响，用户界面又永久删除批次 #23 的 99 条候选和 28 份识别产物，0 条正式流水继续保留；随后从当前 `zhihu-backend` 重新启动后端并对同一 `794×18578` 长截图发起真实批次 #24。14/14 个腾讯切片成功、0 失败，生成 99 条候选、0 条缺日期、0 条疑似重复、0 条已存在、0 条格式错误，证明同片不同 OCR 行门禁在干净重跑中生效。
5. 正式 admin 登录态实际执行了三类批量核对，但没有点击底部入账：第一次一次确认 98 笔共同年份和 99 笔 CNY 后，候选变为 68 绿、31 黄；第二次一次确认 3 笔“保留未知交易对方”后变为 71 绿、28 黄；第三次确认餐饮 19 笔、其他支出 5 笔、交通 2 笔三个分类组后，最终为 97 绿、2 条待人工、0 疑似重复、0 已存在、0 格式错误、0 已入账。剩余行 13003 和 13007 的 OCR 行关联确实不完整，候选详情均能展示第 13/14 片及对应来源框选，应由用户核对而不是程序猜测。
6. 集中回归通过：收支解析、AI 识别、长图服务和导入服务共 204 项后端测试通过；前端 TypeScript、收支工作台定向 ESLint 与 `git diff --check` 通过。当前前端继续运行于 3000、后端运行于 8000，均来自当前仓库；代码尚未提交或推送。

### 6.50 2026-08-25 收支工作台重构、收入视角与流式问询

1. `/income` 已从堆叠全部功能的长页面重构为任务导向主页：两个录入入口位于首屏；可信账本以摘要和“查看全部”入口呈现；主要空间留给消费分析、收入守护、AI 问询和知识。完整流水进入 `/income/ledger`，预算、周期支出、订阅、月结、变更历史等低频能力进入 `/income/tools`，避免把二级工具永久铺在主页。
2. 分析区以支出高频场景为主，提供消费趋势、消费日历、分类构成、分类画像、支出性质、消费对象六个 Tab；折线、热力、环图、堆叠和横向排名按数据关系选型，不再全部使用同一种条形图。点击图表使用抽屉查看对应明细，不自动把页面滚到远处长列表。
3. 收入守护在主页保留独立板块，展示当前收入状态、工资材料入口与守护待办；收入分析支持按年查看各月收入变化，并在单月查看基本工资、绩效、奖金、补贴和其他组成。工资条缺失字段继续保留为“未知”，不会自动补成 0。
4. 收支问询新增流式接口 `POST /api/cashflow/ask/stream`。后端将供应商 SSE 转换为逐行 JSON，发送心跳并只在完整回答结束后持久化；前端 API 路由直接转发响应流，页面使用安全 Markdown 渲染并在中断时保留可重试状态。代码 head `0057` 又为每次问询增加 `request_id` 与请求指纹：相同请求重试返回已保存回答，同一标识对应不同内容时返回冲突，避免断线重试重复创建回合。该数据库保证仍待 `0057` 部署后生效；流式支持只覆盖收支问询，不应泛化成所有 AI 页面均已完成。
5. 收支知识卡只展示标题、摘要和标签，不展示内部“推荐依据”。`0054`～`0056` 将理财阶段内容改为用户可执行的科普和建议，并把账户互转、退款冲销、OCR 核对、内部预算口径等系统机制说明从公开知识列表隐藏；当前知识为 43 篇、39 篇发布。

### 6.51 2026-08-25 MySQL 唯一运行库、迁移与仓库安全审计

1. 当前代码 Alembic 唯一 head 为 `20260825_0057`，本机 MySQL `zhihu` 仍为 `20260825_0056`。`0057` 待部署且不新增表，只在 `cashflow_conversation_turns` 增加 `request_id`、`request_fingerprint` 和 `(user_id, request_id)` 唯一约束；本轮未经新授权不迁移本机或正式业务库。当前本机 `zhihu` 共有 71 张表、21 个系统/用户分类口径和 43 篇知识；这些数字只作为审计快照，不写成环境无关常量。`0030`、`0052`、`0053` 等旧章节继续保留为历史 rollout，不再与当前头混写。
2. 应用 `Settings` 会拒绝 SQLite 或其他非 MySQL 数据库 URL；CI 启动独立 MySQL 8.4 服务；`scripts/verify-fp00.sh` 必须显式接收名称含 `test` 的 MySQL schema，已移除 SQLite 临时库回退。少量直接创建隔离 SQLite engine 的纯单元测试只验证无数据库方言依赖的局部逻辑，不构成应用启动、迁移或数据库集成验收。
3. `alembic.ini` 中不再保留真实用户名、密码、主机和库名，只使用不可运行的占位地址；迁移运行时继续由 `app.core.config` 的 `DATABASE_URL` 覆盖。由于旧凭证已经进入 Git 历史，仍须在数据库侧轮换密码，普通增量提交不能消除历史泄露风险。
4. 腾讯云 OCR 的 SecretId/SecretKey 继续只从后端 `.env` 读取；`.env` 已由版本控制忽略。审计未发现本次工作区存在其他待提交真实密钥。`zhihu/artifacts/` 是本地历史验收截图，不属于运行资产，已明确忽略；`public/images/today-life-cycle-clock.png` 是页面运行所需静态图，必须随代码提交。

### 6.52 2026-08-25 今日首页生命时钟翻转卡片

1. `/today` 右侧主视觉改为可双面翻转卡片：正面继续展示职业场景，背面以生命阶段苹果/种子背景作为天然钟面，不再叠加第二个人工表盘。
2. 背面的十二个种子/叶片即十二个天然刻度。时针、分针和秒针围绕图片中心运动，长度约束在这圈刻度之内，并用叶片、枝条和果核的视觉语言保持艺术感；日期和数字时间仍作为辅助读数。
3. 翻转支持鼠标、键盘与明确按钮，不依赖悬停；动画遵守 `prefers-reduced-motion`。该功能进入代码基线后仍应以登录态桌面和手机实测作为最终体验验收，不能只凭组件测试或静态截图认定完成。

### 6.53 2026-08-25 仓库收尾门禁与成长守护目标

1. 收支问询断流恢复完成请求级幂等收口：前端为一次逻辑问询生成并在网络重试时复用 `request_id`，后端保存请求指纹，串行化同一用户同一请求，并在数据库已提交但浏览器未收到最终帧时返回已保存回答；相同标识携带不同内容会返回 409。旧客户端未传标识时由后端补充 UUID，但完整的跨请求持久化保证仍依赖 `0057`。
2. 迁移与审计边界同步加固：管理员连接测试日志使用当前操作者而不是配置最后更新者；`0052`、`0053`、`0055` 用明确的迁移创建标记区分“迁移新增”和“部署前已存在”，升级不覆盖已有业务数据，降级只回收迁移自身创建且仍符合所有权条件的数据。当前代码唯一 head 为 `20260825_0057`，本机 MySQL `zhihu` 仍为 `20260825_0056`；本轮没有迁移本机或正式业务库。
3. 后端全量单元与离线迁移回归共运行 493 项并全部通过，其中 92 项数据库写入集成测试因未提供通用隔离 MySQL URL 而按保护规则跳过。随后单独创建名称含 `test` 的临时 MySQL schema，验证空库升级到 `0057` 和管理员服务配置权限/密文/脱敏链路共 2 项通过，测试结束后已删除该临时 schema，未连接 `zhihu`、`market_raw` 或 `pin_legacy_staging`。
4. 前端全量 ESLint 为 0 error、11 个既有 warning，TypeScript `--noEmit` 通过，Next 16 webpack 生产构建成功生成 31 个路由。`git diff --check` 通过。构建结果证明代码可编译，不替代登录态桌面/手机业务验收，也不代表 `0057` 已部署。
5. 仓库安全审计确认当前提交候选不包含真实 `.env`、Tencent SecretId/SecretKey、当前数据库密码、SQL dump、上传原件、调试日志或本地验收截图；`today-life-cycle-clock.png` 是页面直接引用的运行资产，应随代码提交。历史 `alembic.ini` 中的数据库凭据仍可能有效，必须在数据库侧轮换；普通增量提交只能移除当前文件中的值，不能消除 Git 历史风险。
6. 下一主产品目标“成长守护”当时先形成了以 30/60/90 计划为主轴的初版设计；后续产品纠偏见 6.54。当前唯一有效口径以 `growth-guardian.md` 和 6.54 为准，不能再用本条初版闭环指导实现。

### 6.54 2026-08-25 数据库复核与成长守护产品纠偏

1. 使用后端实际 `.env` 只读连接 `localhost:3306/zhihu`，`alembic current -v` 与直接查询 `alembic_version` 均返回 `20260825_0056`；实际库为 71 张表，8000 健康接口返回 `database=ok`。代码迁移目录唯一 head 为 `20260825_0057`，实际 `cashflow_conversation_turns` 中 `request_id`、`request_fingerprint` 两列命中数为 0，证明 `0057` 尚未部署。本轮没有执行升级。
2. 市场数据迁移域另行核对：`zhihu.alembic_version_core`、`market_raw.alembic_version_raw`、`pin_legacy_staging.alembic_version_staging` 均为 `20260819_0018`。旧章节中的 `market_raw=20260818_0017` 只表示当时快照；`0018` 已增加学校主体、来源归属和学校管理员审计结构。市场域 `0018` 与业务库 `0056/0057` 是两条不同版本线。
3. 动态数据快照同步复核为：21 个系统收支分类、43 篇知识、39 篇已发布、99 条活动流水、账本最大修订号 213。它们只用于说明本次核对时的实际状态，不是固定产品数据。
4. 成长守护不再以“成长基线—30/60/90 计划”为产品主轴，改为“当下的事—过去的果—未来的路—新的当下行动”。30/60/90 只保留为未来目标拆解的可选模板。
5. `/today` 表示用户整个职业生涯阶段的跨守护总览；成长守护的“当下的事”只处理真实职场任务、沟通、汇报、情绪和高价值事件。两者不共享完整任务清单；`/today` 只读取聚合摘要和导航信号。
6. 下一开发入口是成长守护“当下工作最小闭环”：杂乱输入 → 情绪/事实/任务分离 → 用户确认 1—3 个破局点 → 记录结果 → 用户确认高价值事件 → STAR 维度或周报草稿。过去资产和未来方向在此后分阶段实现。当前没有成长守护专用表或迁移，不得把设计文档写成已实现。

### 6.55 2026-08-25 成长守护 Phase A 代码闭环

1. 新增业务迁移 `20260825_0058`，在 `0057` 之后定义 `growth_work_intakes`、`growth_work_items`、`growth_emotion_notes`、`growth_work_events`、`growth_weekly_reports` 和 `growth_audit_events`。工作输入使用用户级请求幂等键和 HMAC 指纹；所有业务对象带用户所有权、状态约束、版本或唯一键，清空业务数据时按依赖顺序删除。
2. `/api/growth` 新增工作区、候选整理、确认 1—3 项、任务状态与结果、工作事件确认、周报生成/复核/导出标记接口。原始工作输入不持久化；本地规则先隐藏直接标识符并把情绪从任务中分开；AI 路径必须由用户明确同意发送脱敏最小文本。情绪原文默认不保存，明确选择后才加密保存，且不进入工作区响应、周报和 `/today`。
3. 完成任务只创建 `structured` 工作事件候选，未提供的情境、行动和角色保留为 `evidence_gaps`；用户确认且设置为可汇报后才能进入周报。周报只使用同一周、本人、`confirmed`、可汇报事件，保留草稿/复核/导出标记状态，不自动发送。
4. `/growth` 已把“当下工作”调整为主入口，支持快速输入、候选编辑和 1—3 项门禁、任务推进与结果、事件确认和周报；原技能差距功能保留在“未来的路”折叠区，30/60/90 不作为主轴。`/today` 的成长状态只显示任务数、阻塞和待确认数量，不返回标题、结果、情绪或周报正文。
5. 后端全量运行 502 项测试全部通过，其中 94 项隔离 MySQL 写入测试因未设置 `CAREER_GUARDIAN_TEST_DATABASE_URL` 按保护规则跳过；成长新增的 6 项整理/隐私单测和 `0057 → 0058 → 0057` 离线迁移往返已通过，2 项成长 MySQL API 闭环测试被计入跳过。前端 TypeScript 与 Next.js 16 webpack 生产构建通过，生成 31 个路由。默认 Turbopack 在当前受限执行环境因内部进程绑定端口失败，不是源码编译错误。
6. 再次只读查询实际 `.env` 指向的 `zhihu`：`alembic current -v` 和直接查询均为 `20260825_0056`，`growth_%` 表数量为 0；代码唯一 head 为 `20260825_0058`。本轮没有迁移实际库、重启服务或调用真实外部模型，因此 Phase A 只能写成代码已实现、待隔离 MySQL 与真实登录态验收，不能写成已部署或完整成长守护已交付。

### 6.56 2026-08-25 成长守护 Phase B/C 代码闭环

1. 新增 `20260825_0059`：作品、成长证据、能力版本、能力—证据关联和反思 5 张表；新增 `20260825_0060`：未来目标、市场信号、差距快照和里程碑 4 张表。代码唯一 head 已只读确认是 `0060`，两段迁移离线升级/降级往返通过。
2. `/growth` 改为三个平级入口；`/growth/work` 保留 Phase A，`/growth/assets` 支持作品/证据候选、本人确认、能力分层与版本、单问题反思、职业筹码和确认资产 JSON 导出，`/growth/direction` 支持目标版本、市场信号来源/时间/样本/局限、差距候选、可选周期里程碑和行动倒推。
3. 作品、证据、目标、市场刷新、差距和里程碑都有数据库唯一键及请求指纹或语义版本；越权统一 404，状态与版本冲突 409。删除作品/证据先报告关联影响，明确解除时生成安全后继状态或能力版本。私人反思不进入导出。
4. 市场样本不足、过期或不可用时只保存弱信号并将差距列为“尚未核清”；方向趋势没有纵向数据时明确为未知。里程碑只生成 Phase A 工作输入候选，用户第二次确认后才创建正式工作项，且不会自动完成里程碑。
5. 506 项后端测试通过，96 项隔离 MySQL 写入测试因未配置 `CAREER_GUARDIAN_TEST_DATABASE_URL` 跳过；其中 Phase B/C 两条端到端 MySQL 用例只完成测试代码，未形成真实写入证据。TypeScript、成长页面 ESLint和 Next.js 16 webpack 生产构建通过，生成 34 个路由；全前端 lint 为 0 error、11 条既有非成长页面 warning。
6. 本轮重新只读核对实际 `zhihu`：仍为 `20260825_0056`、71 张表、`growth_%` 为 0。本轮没有升级实际库、启停服务或触发外部模型；Phase A—C 只能写成代码已进入基线，仍需 `0060` 隔离 MySQL 往返、真实登录态桌面/手机、断网/删除/所有权和跨守护验收。

### 6.57 2026-08-25 成长守护整合代码与实际库迁移

1. 用户再次确认产品目标为“当下的事—过去的果—未来的路”三段完整能力；Phase A—D 只是工程分段，不是替代产品目标的新口径。文档已按用户的任务/精力翻译、沟通军师、高价值事件、周报、作品/能力/情绪/反思、市场温差/筹码/目标倒推逐项记录实现和缺口。
2. 新增 `20260825_0061`，建立 `growth_communication_drafts`、`growth_handoffs`、`growth_inquiries` 3 张表。沟通/汇报草稿不可覆盖，支持修订、复核、导出和归档但不自动发送；整合表单支持浏览器本地草稿恢复。
3. 成长问询默认使用本地程序从用户勾选的已确认工作、资产、方向和市场信号组织回答，缺证据时说“尚未核清”；外部 AI 需二次同意，只发送选定的已确认最小上下文，支持流式、取消、重试、幂等、安全 Markdown、证据引用和不记问答正文的调用审计。本轮没有发起真实外部模型请求。
4. 跨守护交接只允许选择用户已确认的工作事件、作品、证据/能力和目标/差距/里程碑；确认前显示目标、内容、依据和影响，确认后写入共享收件箱，可撤销并留审计，不自动生成目标守护的正式结论。完整分类导出排除原始情绪、候选、私密反思和问询正文。
5. 回归结果为 512 项后端测试通过，97 项需隔离 MySQL 的测试因未配置 `CAREER_GUARDIAN_TEST_DATABASE_URL` 跳过；`0060→0061→0060` 离线迁移往返通过。TypeScript、全前端 lint 和 Next.js 16 webpack 生产构建通过，生成 35 个路由；lint 为 0 error、11 条既有非成长页面 warning。
6. 用户明确授权操作实际库后，已执行 `alembic upgrade head`。迁移后 `alembic current --verbose` 和直接 SQL 均确认 `zhihu=20260825_0061 (head)`，总表数 89、`growth_%` 表 18 张；表名已逐一只读核对。本轮未重启运行服务，未写入测试业务数据。
7. 按用户的完整产品目标，仍需补齐动态能力曲线/雷达图、真实作品内容与代码质量/复杂度分析、真实外部市场数据和技能温差时序，以及真实登录态桌面/手机验收。现状是“数据库已升级、主体代码已就绪、完整产品尚未验收”。

## 7. 文档事实优先级

发生冲突时按以下顺序判断：

1. 当前运行代码、数据库迁移和接口返回。
2. `zhihu/docs/data/` 下标为当前的架构与治理文档。
3. 本进度文档。
4. `zhihu/docs/acceptance/` 的历史验收快照。
5. `zhihu/docx/` 中的早期 PRD、蓝图和开发计划。
6. `Pin` 与 `Reference` 中的历史实现，仅作来源参考。

完整索引见 [`zhihu/docs/README.md`](../docs/README.md)。

## 8. 新会话建议开场指令

可直接把下面内容交给新会话：

> 阅读根目录 `README.md`、`zhihu/docs/README.md`、`zhihu/progress/PROGRESS.md` 和 `zhihu/docs/income-guardian.md`，再按需要阅读 `decision-guardian.md`、`rights-guardian.md`。先执行只读 `git status --short --branch`、`git log --oneline -8` 和 `./scripts/check-workspace.sh`；保留第 4.4 节用户文件，不清理、不覆盖、不顺手提交无关内容。
>
> 2026-08-25 的当前基线见本页顶部和 6.57：实际业务库与代码迁移 head 均为 `zhihu=20260825_0061`，实际库 89 张表、成长表 18 张；市场数据 `core/raw/staging` 三个版本表为 `20260819_0018`。admin 的流水、账本修订和知识数量是迁移前的同日历史快照，下一次使用前须重新只读查询。实际库结构已升级，但当前运行服务尚未重启，真实外部模型也未调用。
>
> 收支守护以 `zhihu/docs/income-guardian.md` 为产品基线：收入与支出平级；程序先算账，AI 只解释和建议，用户确认后才进入可信账本。不可删除工资条、预算、固定收支、退款/报销、OCR、AI、KNOWLEDGE 或导出能力，也不增加“我的账户”确认。当前主页、独立账本页、工具页、图表抽屉、收入分析和流式问询已经进入代码基线，真实工资条、更多银行/钱包样本、手机端细节和最终业务验收仍需继续。
>
> 当前主产品目标是用户明确的完整成长守护。以 `zhihu/docs/growth-guardian.md` 为唯一产品基线，按“当下的事—过去的果—未来的路—新的当下行动”推进；开发 Phase 只是工程分段。三段主体、沟通军师、问询、导出和确认式交接已进代码与实际库；下一步按最短可用路径补齐动态能力曲线/雷达图、真实作品分析和真实市场温差，然后直接做真实登录态桌面/手机验收。`/today` 只读取成长摘要，30/60/90 只是可选模板；AI 只能形成建议，不能自动确认能力或决定晋升/跳槽。

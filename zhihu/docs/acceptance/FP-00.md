# FP-00 验收记录：可复现与安全基线

- 功能点：FP-00
- 验证日期：2026-08-15
- 状态：`verified`
- 范围：仓库、依赖、启动、健康检查、用户资源所有权和基础 CI

## 已交付

- 根仓库忽略规则、开发文档、服务边界 ADR 和本地脚本。
- 后端显式配置与生产环境 JWT 安全校验。
- 健康检查 `/api/health` 和就绪检查 `/api/health/ready`。
- Offer、合同、报告和 Finding 的用户所有权校验。
- 创建合同时对 `case_id`、`linked_offer_id` 的归属与一致性校验。
- SQLite 测试数据库支持和空库 Alembic 迁移测试。
- GitHub Actions 前后端 CI 与 Dependabot 配置。
- Next.js 16.3.1 安全升级和 npm 漏洞修复。
- Pin 京东适配器中的登录态改为从 `JD_RECRUITMENT_COOKIE` 环境变量读取。

## 自动验收

命令：

```bash
./scripts/verify-fp00.sh
```

结果：

- 后端 `unittest`：6 项通过。
- 空数据库 `alembic upgrade head`：通过。
- 两个测试用户的 Offer、合同、报告、Finding 越权访问：全部返回 404。
- 前端 ESLint：0 error，31 warning。
- Next.js 生产构建：通过，25 个路由完成生成。
- `npm audit`：0 vulnerabilities。

## 运行验收

- `./scripts/run-backend.sh` 使用隔离 SQLite 数据库完成迁移并启动。
- `/api/health` 返回版本 `0.2.0`、环境和数据库 `ok`。
- `/api/health/ready` 返回 `ready`。
- `./scripts/run-frontend.sh` 启动 Next.js 16.3.1。
- 访问 `/` 返回 307 到 `/welcome`，最终页面返回 200。

## 仓库安全检查

以下内容已由根 `.gitignore` 排除并通过 `git check-ignore` 验证：

- 所有 `.env` 和本地环境覆盖文件。
- Python 虚拟环境、Node 依赖和 Next 构建目录。
- 用户上传材料、运行日志和临时测试数据库。
- `Pin/db/backup.sql`。
- Pin 抓取生成物、临时网页、代理池和企业级本地配置。
- 合并 monorepo 前保留的内层 Git 历史 `.legacy-git/`。

## 已知非阻塞项

- 31 个 ESLint 警告来自现有 PoC 页面，包括 Hooks 编译器建议、显式 `any` 和未使用变量；已保持可见，没有作为通过项隐藏。后续触达相应页面时逐项清理。
- 本验收使用隔离 SQLite 和脱敏固定数据，不代表本机 MySQL 业务数据、OCR、LLM 或真实招聘采集已经验收。
- FP-00 不包含五域首页和职业事件新模型，它们属于 FP-01。

## 结论

FP-00 的实现与本地验证已完成。GitHub 初次推送成功后，将状态从 `verified` 更新为 `accepted`，并解锁 FP-01 与 FP-02。


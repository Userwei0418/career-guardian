# 目标：仓库收尾与成长守护

- 建立日期：2026-08-25
- 更新日期：2026-08-25
- 目标性质：仓库收尾已经完成；成长守护三段主体与整合能力已进入代码和实际库，继续补齐能力可视化、真实作品分析、市场温差和用户验收
- 当前工作分支：`main`
- 当前仓库基线：文档收口、Phase A 和 Phase B/C 已分别推送；本轮将提交沟通军师、成长问询、分类导出和跨守护交接以及实际迁移文档
- 当前数据库基线：经用户明确授权，实际 `zhihu` 已从 `20260825_0056` 迁移到代码 head `20260825_0061`，只读复核为 89 张表、18 张成长专用表
- 目标主线：`仓库可信可重建 → 成长守护设计一致 → 当下工作最小闭环 → 过去资产 → 未来方向`

## 目标一：完成近期修改审计并合并到 main（已完成）

### 目标结果

仓库中的近期功能、迁移、文档和运行资产形成一个完整、可重建、无敏感信息、可验证的提交；功能分支推送后安全合并并推送到远程 `main`。

### 必须完成

- 审计工作区全部已跟踪和未跟踪变更，区分运行文件、测试、文档、本地证据和敏感文件；
- 纳入所有被运行代码直接依赖的模块、页面、迁移和正式图片资产；
- 忽略本地生成的截图、构建产物、缓存和环境密钥；
- 统一现行数据库口径为 MySQL + Alembic，不将历史 SQLite 验收记录误写成现行运行方案；
- 明确正式 MySQL 当前版本、代码迁移头和待部署迁移，不把不同环境写成同一个“当前”；
- 更新 README、文档索引、进度、数据库架构、收支守护、腾讯 OCR 和服务配置文档；
- 处理审计发现的接口兼容、流式幂等、管理员日志归因和迁移可逆性风险；
- 检查提交内容不包含 `.env`、腾讯密钥、数据库密码或其他个人凭据；
- 运行与风险相称的后端 MySQL 测试、迁移测试、前端 lint/type/build；
- 提交并推送功能分支；
- 将功能分支快进合并到本地 `main`，推送远程 `main`；
- 最后核对远程分支 SHA、工作树状态和代码迁移头。

### 完成定义

- 新 checkout 能得到完整迁移链和所有运行依赖；
- 正式数据库凭据不再出现在当前文件中；
- 发现于历史提交的凭据已明确要求轮换，未经额外授权不重写 Git 历史；
- 文档把 `0030`、`0053` 等历史 rollout 与当前运行事实分开；实际 `zhihu` 明确写为 `0056`，代码待部署 head 明确写为 `0057`；
- 目标分支和远程 `main` 指向同一已验证提交；
- 没有把测试通过误写为生产部署或用户最终验收。

## 目标二：把成长守护设计为最后一个完整守护模块

### 产品目标

成长守护是一套沿时间轴运转的个人职业外脑，不是课程商城、日程表、绩效工具或以 30 / 60 / 90 天计划为中心的打卡产品。它围绕三个连续工作区建立闭环：

- **当下的事**：处理真实职场任务、精力、沟通、汇报和情绪，把混乱翻译为用户确认的少量行动；
- **过去的果**：把用户确认的高价值事件沉淀为作品、证据、反思、能力事实和可带走的职业筹码；
- **未来的路**：用有来源、有时效和证据充分度的市场信号，对照用户目标与既有筹码，形成差距和待确认里程碑；
- **闭环**：未来里程碑只生成新的“当下的事”行动候选，不能绕过用户写入正式任务。

`/today` 是用户整个职业生涯阶段的跨守护总览，只读取成长域摘要和导航信号；它不是成长守护的日常工作台，不同步完整任务、原始情绪、沟通正文或周报正文。30 / 60 / 90 天只作为“未来的路”中的可选模板。

### 第一开发里程碑

先完成“当下的事”中一条可真实验收的最小闭环：

```text
输入一段杂乱的真实工作描述
  → 程序与 AI 分离任务、情绪和客观事实
  → 用户确认 1—3 个当前破局点
  → 完成工作并记录结果
  → 用户确认是否沉淀为高价值事件
  → 生成可编辑的 STAR 维度或周报草稿
```

### 必须遵守

- AI 只草拟和解释，不能确认能力、完成任务或决定晋升/跳槽；
- 原始情绪、客观事实、AI 候选和用户确认记录分层保存；
- 市场信号、AI 建议、用户自述和证据确认能力分层保存；
- 工作项、高价值事件、作品、证据、目标、里程碑和跨守护写入都可版本化、撤销和审计；
- 首页保持任务导向，低频功能进入独立页面、抽屉或模态框；
- 图表只统计已确认事实；
- 证据坚持最小必要、用户可控和所有权隔离；
- 所有跨守护写入都经过用户确认。

### 交付物

- 产品与交互基线：[成长守护详细设计、交互基线与实现状态](../growth-guardian.md)
- 数据模型与 Alembic 迁移；
- 后端所有权、状态机、版本、幂等和审计接口；
- `/growth` 当下工作台、过去资产库和未来仪表盘；
- 任务与精力翻译、情绪与事实分离、高价值事件确认和周报草稿；
- 作品、能力证据、职业筹码、目标差距和里程碑；
- `/today` 只读成长域摘要和导航入口；
- MySQL 集成、迁移往返、前端交互和真实登录态验收证据；
- 完成后同步更新本文、PROGRESS、数据库架构和模块文档。

### 当前实现边界

`0058`—`0061` 已在代码和实际 `zhihu` 建立 18 张成长表；`/growth`、`/growth/work`、`/growth/assets`、`/growth/direction`、`/growth/integration` 已覆盖任务/精力翻译、情绪脱敏、沟通汇报草稿、高价值事件、周报、作品/证据/反思/能力版本、目标/市场信号/差距/里程碑，以及可选 AI 问询、分类导出和确认式跨守护交接。512 项后端测试通过，97 项隔离 MySQL 用例因未配置测试库跳过；前端类型、lint 和 35 路由生产构建通过。admin 真实登录态的成长五页已在实际 `0061` 库完成桌面读取和 `390×844` 无横向溢出短验收，控制台 0 error；真实写入与外部 AI 仍未验收。按用户三段产品目标，仍未完整实现的核心功能是动态能力曲线/雷达图、真实作品内容与代码质量/复杂度分析、真实外部市场数据和技能温差时序。

## 执行顺序

1. 目标一已经完成；保留提交、测试与数据库版本边界，不重复迁移或重做仓库收尾；
2. 以 `growth-guardian.md` 为唯一产品基线，先同步 README、PROGRESS、目标和数据库文档；
3. 实际 `zhihu` 已迁移到 `0061`；不再重复把数据库部署当作前置讨论，直接用当前库做真实登录态验收；
4. 按最短可用路径补齐动态能力可视化、真实作品分析和真实市场温差，避免把简单产品功能过度拆分；
5. 完成真实登录态桌面/手机验收后再更新“已验收”状态。

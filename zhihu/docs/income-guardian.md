# 收支守护完整功能与实现状态

- 文档性质：持续维护的完整产品总图，不按版本拆分功能范围
- 更新日期：2026-08-22
- 用户入口：`/income`
- 既有工具：`/payslip`、`/salary`
- 内部兼容：职业事件类型暂继续使用 `income`，用户界面统一称“收支守护”
- 参考项目：`/Users/zhouwei5/Downloads/OneBill`，只参考领域和交互，不形成运行时依赖

## 1. 产品定义

收支守护平等回答四个问题：

1. 这个月收入多少，分别来自哪里；
2. 这个月支出多少，主要花在哪里；
3. 已确认收入减已确认支出后，当前净结余是多少；
4. 哪些收入、支出、转账、工资扣款或导入结果仍需确认。

统一原则：

- 收入和支出在首页、导航、详情、统计和报告中平级；
- 工资核对是收入侧的专业能力，不等于全部收入；
- 收入、支出和转账共用一套用户私有流水底账；
- 转账不进入收入、支出和净结余，退款、报销和信用卡还款必须避免重复计算；
- 缺失数据保持未知，不用 `0`、演示数据或 AI 推断填补；
- 确定性程序负责金额、统计、查重和关联，AI 负责理解、候选和解释；
- 导入、OCR 和自然语言识别先生成候选，用户确认后才能写入正式流水。

## 2. 用户体验

首页第一屏平等显示本月收入、本月支出、本月净结余和待确认数量。没有已确认记录时显示“尚无已确认记录”或“暂无法计算”，不展示四个零；已经录入但仍待确认的记录也不能被误写成“尚未记录”。

```text
本月收入           本月支出
¥13,226            ¥7,480

本月净结余         待确认
¥5,746             5 笔
```

收入侧包含工资、奖金、兼职副业、经营收入、投资收益、报销、退款、补贴和其他收入；支出侧包含固定、日常弹性、一次性、可报销和其他支出。用户可以在统一流水中切换“全部、收入、支出、转账、待确认”。

月度状态只描述数据整理状态：尚未开始、整理中、存在待确认。系统没有用户月结确认和完整账户覆盖证据时，不自动声称“已经核清”。

## 3. 完整功能总图

这里列出产品完整范围。实现进度只使用“已实现、开发中、待开发、需外部配置”，不再把未列入某一版本等同于不做。

| 能力 | 用户获得的结果 | 当前状态 |
| --- | --- | --- |
| 收支月度总览 | 收入、支出、净结余、待确认和分类结构平级呈现 | 已实现并完成正式迁移；桌面登录态已验证无数据、单侧未知和确认/待确认统计，手机登录态已验证响应式布局 |
| 手工流水 | 新增、编辑、软删除收入、支出和转账 | 已实现并完成正式迁移；浏览器已验证新增、编辑和筛选，软删除交互尚未执行运行验收 |
| 系统分类 | 收入和支出分别使用系统默认分类 | 正式 MySQL 已应用 `20260822_0029`，当前初始化 20 个系统分类 |
| 自定义分类 | 用户新增、停用和治理自己的分类 | 已有新增接口；停用、合并和占用处理待开发 |
| 工资条核对 | Offer/合同约定、应发、扣款、实发与实际到账分开核对 | 既有能力可用；未知值、详情和实际到账关联仍待完善 |
| 其他收入 | 奖金、兼职、副业、经营、投资、补贴等独立记录 | 手工流水已支持 |
| 支出性质 | 固定、日常弹性、一次性、可报销和其他支出 | 手工流水已支持；月度性质汇总由完整月度服务端流水计算 |
| 退款与报销关联 | 将退款冲减原支出，将报销与可报销支出关联 | 待开发 |
| 账户与转账 | 记录账户间移动并避免重复计入收支 | 当前支持转账方向，浏览器已验证不影响收入、支出和净结余；账户台账和成对转账待开发 |
| 搜索和筛选 | 按月份、方向、状态、分类、商户和备注检索 | 后端已支持主要条件；前端高级筛选待开发 |
| 月度趋势与比较 | 按日趋势、分类变化、环比和变化原因 | 后端已有当月按日结构；跨月比较待开发 |
| 预算 | 总预算、分类预算、使用进度和事实提醒 | 待开发 |
| 固定收支 | 识别工资、房租、订阅等周期性项目并提醒确认 | 待开发 |
| 文件导入 | 微信、支付宝、银行和通用 CSV/XLSX 先预览再确认 | 待开发 |
| 导入批次与幂等 | 原文件摘要、逐行状态、查重、重试和审计 | 待开发 |
| 票据与截图 OCR | 识别小票、账单截图、工资条和长图流水 | 待开发，复用职护现有视觉模型体系 |
| 自然语言记账 | “午饭 32 元”等输入生成可编辑候选 | 待开发，复用职护现有文本模型体系 |
| AI 分类建议 | 对未分类记录提供分类、支出性质和关联建议 | 待开发，必须由用户确认 |
| AI 月度解释 | 基于确定性统计解释主要变化、未知项和待办 | 待开发，不允许模型自行算金额 |
| AI 收支问答 | 回答来源、去向、变化和材料缺口 | 待开发，限制最小必要上下文 |
| 月度报告 | 收入、支出、结余、变化和待办的可追溯总结 | 待开发 |
| 月结确认 | 用户明确确认月份已核清或仍有缺口 | 待开发 |
| 证据与审计 | 每个数字回到来源、确认人、导入批次和修改记录 | 当前保留来源和确认状态；完整审计待开发 |
| 导出与备份 | 用户导出自己的流水和月度报告 | 待开发 |

## 4. 当前统一账本契约

当前代码新增：

- `financial_categories`：系统分类与用户自定义分类；
- `financial_transactions`：用户私有收入、支出和转账；
- `/api/cashflow/categories`：分类列表与自定义分类新增；
- `/api/cashflow/transactions`：流水列表、新增、编辑和软删除；
- `/api/cashflow/summary`：按月确定性汇总。

流水至少保存：当前用户、方向、金额、币种、发生日期、分类、商户或来源、备注、支出性质、来源类型、来源引用、外部幂等键、确认状态和软删除时间。

收入和支出必须选择同方向的系统或本人分类；其他用户的自定义分类不可读取或引用。转账不能带收支分类，也不进入合计。只有 `confirmed` 流水参与收入、支出、分类和净结余；`pending` 与 `excluded` 分别统计数量但不改变金额。

### 4.1 当前运行与验收边界

2026-08-22 正式 MySQL 版本已只读复核为 `20260822_0029 (head)`，`financial_categories` 和 `financial_transactions` 已创建，系统分类共 20 个。API `/api/health`、`/api/health/ready`、market `/api/health` 和前端 `/income` 运行检查通过。

桌面登录态浏览器已验证收入、支出和转账新增、方向筛选、待确认不进入汇总、待确认编辑为已确认后的重算、单侧缺失不显示伪造的 `0`，以及转账不影响收入、支出和净结余。`390×844` 手机视口已验证卡片顺序、无页面横向溢出、可横向滚动的五领域导航和可滚动录入弹窗，验收后已恢复 `1280×720` 视口。浏览器控制台没有 error 或 warning。

本轮还修复了持久化显示用户名与实际访问令牌可能不一致的问题：主导航挂载后会用 `/auth/me` 重新核对身份。运行验收使用 4 条名称明确包含“验收”的合成流水，实际归属正式库 `admin`；它们仍保留且不得当作业务数据，未在没有删除确认的情况下擅自软删除。软删除浏览器操作、真实 OCR/AI 调用和用户最终业务验收尚未完成，本轮没有调用外部 AI/OCR。

## 5. 工资与现金流边界

工资条的实发金额是工资证据，不自动等于银行实际到账。工资链和现金流链必须分开：

```text
Offer/合同 → 应发 → 扣款 → 工资条实发

实际到账收入 + 其他已确认收入 - 已确认支出 = 本月净结余
```

未来关联工资到账时，应创建或关联一条真实收入流水，并使用来源引用避免重复录入。同一月份存在多份工资条时，不能简单相加为到账收入。

工资条当前仍需解决：可选项目空值在分析阶段被转为 `0`、详情/编辑/删除、可扩展工资项目、字段级来源、实际到账关联、跨月对比和月结状态。

## 6. 导入、OCR 与 AI

### 6.1 文件导入

导入统一采用：

```text
上传私有文件
→ 创建导入批次
→ 程序解析与格式校验
→ 外部键和内容哈希查重
→ 收入/支出/转账候选
→ 用户编辑、排除和确认
→ 幂等写入正式流水
→ 生成导入报告
```

必须增加 `financial_import_batches` 和逐行候选表，数据库唯一约束至少覆盖 `(user_id, source_type, external_key)`。页面刷新、失败重试和重复上传不能重复记账。

### 6.2 OCR

票据、账单截图、工资条和长图 OCR 直接复用职护现有视觉模型/多模态能力、服务端配置和调用审计。不得移植 OneBill 的智谱、腾讯 OCR、COS 或独立密钥配置。

OCR 输出必须包含识别置信度、原始区域或可定位证据、模型与提示词版本、候选状态。低置信字段要求用户确认；图片原件遵守职护私有附件和最小出站原则。

### 6.3 AI

自然语言记账、分类建议、月度解释和问答统一接入职护已有 AI 配置：

- 使用当前服务端供应商、模型能力和管理员配置；
- 复用现有 `AIInvocationLog`，功能点区分 `cashflow_text_parse`、`cashflow_vision_parse`、`cashflow_classify`、`cashflow_month_explain`；
- 不在前端新增厂商 Key，不把 OneBill 的模型配置复制进职护；
- 只发送当前问题所需的最小、可脱敏上下文；
- 金额合计、查重、预算和趋势由程序计算；
- 模型只返回结构化候选或对既有统计的解释；
- AI 失败时基础账本、导入预览和确定性统计仍可工作。

## 7. OneBill 参考边界

OneBill 已有账本、分类、预算、月度统计、微信 Excel 预览/查重/确认、文本记账、图片识别和消费问答。职护借鉴这些领域和交互，但不复用其 Java/Vue/H2/JWT、用户系统、厂商配置或运行服务。

OneBill 将部分提现、充值等中性交易转换为收入或支出参与统计；职护不采用这一口径。OneBill 的导入查重主要依赖应用内集合检查，职护需要持久化批次和数据库唯一约束。OneBill 会把较多交易级明文提供给模型，职护必须沿用现有隐私和调用审计边界。

## 8. 验收清单

收支守护完整验收至少覆盖：

- 收入与支出在桌面和手机上平级，不把支出藏成收入附属图表；
- 无数据、部分数据、待确认、排除和负结余状态；
- 金额两位小数与跨月边界；
- 转账不影响收入、支出和净结余；
- 分类方向匹配、系统分类可见、用户自定义分类隔离；
- 用户 A 无法读取、修改或删除用户 B 的分类和流水；
- 软删除不会出现在列表和汇总中；
- 重复导入、并发确认和失败重试保持幂等；
- OCR/AI 候选未经确认不进入正式账本；
- AI 无配置或调用失败时，基础账本和确定性统计仍可用；
- 工资条空白字段保持未知，工资条实发不冒充实际到账；
- 每个金额可以追溯来源、确认状态、导入批次和修改历史；
- 正式迁移、浏览器运行、真实 OCR/AI 调用和用户最终验收分别记录。

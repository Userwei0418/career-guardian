# 成长守护详细设计、交互基线与实现状态

- 文档性质：成长守护唯一、持续维护的产品基线
- 更新日期：2026-08-27
- 本次修订：补齐项目总目标、跨周材料归属、项目/工作线增量影响、停滞提示、周/月回顾、成长数据清场与最终仓库边界
- 文档状态：目标设计与当前实现状态并存；未实现能力不得描述为已交付
- 数据库边界：经用户授权迁移和后续清场后，仓库业务迁移唯一 head 与 2026-08-27 复核的实际 `zhihu` 运行库均为 `20260826_0068`，共 101 张表、30 张 `growth_%` 表；清场完成当时为 0 行，随后重新操作已产生新数据。`0067` 保留工作线级目标与进展建议；`0068` 新增独立的用户确认项目档案和项目级进展建议，并用 `project_id` 稳定关联多条工作线与多份材料
- 主入口：`/growth`
- 核心对象：项目档案、长期工作线、会议/方案材料、进展节点、沟通草稿、情绪记录、高价值事件、作品资产、成长证据、能力事实、未来目标、差距、里程碑和周/月回顾草稿
- 上游：个人档案、真实工作记录、目标岗位、Offer 决策、作品链接、用户主动设定的职业目标和经过质量控制的市场数据
- 下游：职业档案、简历素材、机会匹配、决策守护、权益守护、收支守护，以及 `/today` 的摘要级状态

## 0. 产品结论

成长守护不是课程商城、日程表、绩效监控，也不是以 30 / 60 / 90 天计划为中心的打卡工具。它要围绕用户真实的职场生活，把“正在做的事”沉淀为“已经拥有的筹码”，再用这些筹码看清“未来还差什么”。

产品由三个平等且相互流动的部分组成：

| 时间视角 | 用户问题 | 核心定位 | 用户得到的结果 |
| --- | --- | --- | --- |
| 当下的事 | 今天的工作怎么理顺，情绪怎么放下，价值怎么留下 | 职场人的外脑与情绪回收站 | 少量清晰行动、可用沟通草稿、被脱敏的事实、高价值事件和周报草稿 |
| 过去的果 | 做过的事情最后留下了什么 | 个人数字黑匣子与资产盘点机 | 作品集、成长证据、能力事实、经验复盘和可带走的职业筹码 |
| 未来的路 | 市场在变化什么，我与目标还差什么 | 客观的职业仪表盘 | 有来源的市场信号、筹码账本、差距清单、用户确认的目标与里程碑 |

统一闭环为：

```text
当下真实工作
  → 程序整理任务、时间和状态
  → AI 辅助拆解、沟通、情绪脱敏和价值识别
  → 用户确认值得保留的事实与高价值事件
  → 沉淀为过去的作品、经验、证据和能力事实
  → 与未来目标及有效市场信号对照
  → 用户确认差距和里程碑
  → 反推新的职场当下行动
```

三个部分不是三个孤立工具，也不能被合并成一个超长页面。它们共享同一条成长证据链，但各自拥有明确的工作区、状态和确认边界。

30 / 60 / 90 天计划只是“未来的路”中可选的目标拆解方法之一。系统不得强迫所有用户建立这类计划，更不能把完成计划当作成长守护的唯一主线。

## 1. `/today` 与成长守护“当下的事”的边界

这是产品结构上的硬边界，不得在后续设计或开发中混淆。

### 1.1 `/today` 是职业生涯全局的“此刻”

`/today` 面向用户整个职业生涯阶段，回答的是：

- 我现在处于什么职业阶段；
- 五个守护当前分别有什么重要状态；
- 眼下最需要看清的跨领域问题是什么；
- 接下来应该进入哪个守护继续处理。

它是职业生涯的全局总览、状态入口和跨守护导航，不是工作任务管理器。

### 1.2 成长守护的“当下的事”是实际工作现场

成长守护中的“当下”只处理用户已经进入职场后，真实发生的工作：

- 今天堆在手上的任务；
- 工作优先级与精力分配；
- 和领导、同事、客户的沟通；
- 汇报、复盘和周报；
- 工作中的情绪、委屈和内耗；
- 值得沉淀为成果、作品或简历素材的事件。

它回答的是“这件工作现在怎么做、怎么说、怎么留下价值”，而不是“我的职业生涯现在走到哪里”。

### 1.3 两者允许的关系

| 方向 | 允许 | 禁止 |
| --- | --- | --- |
| 成长守护 → `/today` | 提供摘要级状态，例如“本周有 2 项重要工作待收口”“新增 1 条高价值事件”“未来目标有 1 个差距待确认” | 同步完整任务清单、原始情绪、沟通正文、周报正文或作品内容 |
| `/today` → 成长守护 | 提供职业阶段、当前主线和跳转入口 | 直接创建、完成、修改成长守护中的工作任务或能力事实 |

实现上必须遵循：

- 职场任务、高价值事件、情绪记录和周报归成长域所有；
- `/today` 只读取经过聚合的状态摘要和导航信号；
- `/today` 中最多出现一张紧凑的成长状态卡，不复制成长工作区；
- 不再设计“把成长守护的每项当前行动同步到 `/today`”；
- 成长守护不能替代 `/today` 的跨职业阶段总览。

## 2. 用户真正需要解决的问题

### 2.1 当下的事：生存与效率

- 面对一堆琐事，不知道先做哪一件；
- 精力不足时仍按任务数量排期，导致真正重要的事没有推进；
- 不知道如何向不同对象沟通、拒绝、追问或汇报；
- 工作中受到批评或委屈，情绪与客观事实混在一起；
- 做成了有价值的事，却没有及时保留背景、行动和结果；
- 到周五才回忆本周做了什么，周报低效且容易漏掉成果。

### 2.2 过去的果：底气与沉淀

- 项目、代码、设计、文章、客户反馈和上线链接散落各处；
- 做过很多事，却说不清自己真正解决过什么问题；
- 不知道一项能力有哪些可靠证据，哪些只是自我感受；
- 经历了同类问题很多次，却没有形成方法和复盘；
- 需要晋升、找工作或做选择时，临时整理材料成本很高。

### 2.3 未来的路：清醒与方向

- 在舒适区待久后，不知道市场需求是否已经变化；
- 面临晋升、转岗或跳槽时，不知道自己的客观筹码和缺口；
- 有远期目标，却无法把它倒推成可执行的近期行动；
- 容易把少数岗位描述、AI 判断或焦虑当作确定事实；
- 需要客观差距分析，但不希望系统替自己做职业决定。

## 3. 产品范围与不可破坏的原则

### 3.1 成长守护负责

| 守护层 | 主要能力 |
| --- | --- |
| 当下工作守护 | 任务与精力翻译、沟通与汇报辅助、情绪脱敏、高价值事件捕捉、周报草拟 |
| 过去资产守护 | 作品收纳、证据关联、经验复盘、能力事实确认、职业筹码盘点 |
| 未来方向守护 | 市场信号、目标对照、差距分析、筹码账本、里程碑与行动倒推 |
| AI 问询 | 基于用户允许的数据解释当下问题、过去资产和未来差距 |
| 知识守护 | 展示与工作方法、沟通、复盘、作品沉淀和职业成长相关的用户科普 |
| 数据自主 | 查询、编辑、导出、撤销和删除自己的工作、资产、目标与确认记录 |
| 跨守护交接 | 用户确认后，将必要的能力、证据或风险摘要回流其他守护 |

### 3.2 成长守护不负责

- 不替企业监控员工，不采集键盘、屏幕、会议或工作时长；
- 不自动发送消息、汇报、周报或其他对外内容；
- 不把情绪宣泄直接写入周报、作品集、简历或能力分析；
- 不因为完成任务、课程或打卡就认定用户掌握能力；
- 不用单一总分评价用户的职业价值；
- 不把 AI 生成的 STAR 草稿直接当作正式简历；
- 不依据少量或过期市场样本制造“技能正在淘汰”的结论；
- 不输出“你应该离职”“你应该接受 Offer”等替代用户决策的建议；
- 不自动修改个人技能、求职目标、简历或其他守护正式记录；
- 不把三个时间视角全部铺成一个无限向下的首页。

### 3.3 不可破坏的原则

- 程序负责状态、日期、版本、聚合、权限和审计；AI 负责解释与草拟；用户负责确认事实和外部行动；
- 原始情绪、客观事实、AI 解释和用户确认结论必须分层保存；
- 市场信号、AI 建议、用户自述和证据支撑的能力事实必须分层展示；
- 用户的一次输入可以产生多个候选，但未经确认不得静默写入正式资产；
- 所有跨守护写入都必须展示目标、内容、依据和影响，并支持撤销；
- AI 不得把推测补成事实；缺少证据时明确显示“尚未核清”；
- 删除、恢复、版本切换和正式确认均需可追溯。

## 4. 三个工作区的详细设计

### 4.1 当下的事

#### 4.1.1 任务与精力翻译器

用户可以输入一段杂乱文字、逐条录入或粘贴现有待办。系统先识别任务、截止时间、依赖、影响范围和大致工作量，把识别出的事项都放入可跟进清单；其中最多 3 项可标为“建议优先”，但这只是排序提示，不是保存或跟进上限。

排序依据需要可见，例如：

- 今天不处理会造成什么影响；
- 是否阻塞其他人或后续工作；
- 是否接近承诺时间；
- 当前精力是否适合完成；
- 用户主动置顶或暂缓。

四象限法可以作为解释工具，但不是唯一算法。用户可以改顺序、拆分、延期、暂缓或忽略建议。进入任务后只有一个自由上下文入口，进展、阻塞、反馈、疑问和结果都可追加；标记完成不以填写结果为前置条件，缺失事实保留为待补线索。误识别、重复或不再需要跟进的任务可单项或批量移出活动清单；默认先采用可撤销的“已收起”，恢复后继续使用原上下文和产物。用户确认不再需要后，也可在“已收起事项”中二次确认永久删除；活动事项不能直接删除。

#### 4.1.2 沟通与汇报军师

系统默认读取用户已经在该任务中留下的上下文并生成草稿；对象、场景、目标和语气只在用户愿意时调整，不要求重复录入。系统给出：

- 需要先确认的事实；
- 1—3 种沟通策略；
- 可编辑的话术或汇报框架；
- 可能的误解与边界；
- 结论先行、事实—影响—诉求等结构化提示。

系统只能生成草稿，不能自动发送。涉及劳动权益、承诺或争议时，应建议用户保存原始证据，并按需跳转权益守护。

#### 4.1.3 情绪与事实分离

用户可以用自然语言倾诉。系统先承接情绪，再将内容拆为：

- 用户的感受；
- 已知客观事实；
- 尚未核实的判断；
- 可控制的下一步；
- 是否存在值得保留的工作事件。

原始情绪默认私密，不参与能力分析、周报、作品集、简历或跨守护问询。只有用户主动确认的客观事实和行动，才能继续沉淀。

#### 4.1.4 高价值事件捕手

用户可以用大白话记录“今天搞定了什么”。AI 只把它整理成 STAR 维度候选：情境、任务、行动、结果、协作角色、可验证证据和可复用方法。

系统必须：

- 保留“不知道”“未量化”，不能虚构结果；
- 提醒用户补充能够证明结果的材料；
- 允许标记为普通工作记录、高价值事件或暂不判断；
- 由用户确认后才进入过去资产；
- 不直接生成并覆盖完整简历。

#### 4.1.5 一键生成周报

周报只汇总用户选定时间范围内可用于汇报的已确认记录：已完成事项、进展、结果、阻塞、下周计划和需要协助的事项。

原始情绪、私密备注、未确认候选和仅供个人复盘的内容默认排除。用户可以逐条选择纳入项、编辑草稿并复制导出；系统不负责自动发送。

### 4.2 过去的果

#### 4.2.1 全能作品收纳箱

支持录入或关联：GitHub 仓库、已上线项目、作品集链接、网盘链接、设计稿、文章、演讲、证书、客户或同事反馈，以及用户选择保留的本地附件。

系统保存最小必要摘要、来源、时间、隐私级别和可用状态。链接失效、附件删除或来源不可访问时要明确提示，不能继续把它作为完整证据使用。

#### 4.2.2 能力资产可视化

AI 可以从已确认作品和事件中提炼技能候选，程序负责聚合其证据数量、时间跨度、使用场景、复杂度和结果类型。用户确认后才形成能力事实。

可视化优先展示：

- 能力证据随时间的积累；
- 某能力覆盖的项目与场景；
- 最近一次使用时间；
- 证据成熟度和仍缺的证据类型；
- 能力组合而非单一总分。

雷达图若使用，只能表示可解释的证据维度，不能制造“职业能力 87 分”之类的伪精确结论。

#### 4.2.3 反思触发器

重要任务完成、项目更新或高价值事件确认后，系统一次只问一个有价值的问题，例如：

- 如果重做一次，你会先砍掉什么？
- 哪个判断最影响结果？
- 哪项方法可以在别的项目复用？
- 哪个结果仍缺少证据？

回答可以保持私密，也可以由用户确认后沉淀为经验或方法资产。

#### 4.2.4 职业筹码账本

筹码账本只列客观、可回溯的内容，例如完整项目、跨部门协作、带人经历、关键客户、业务结果、公开作品和被验证的技能。每项筹码都能回到证据，不把 AI 印象当成筹码。

### 4.3 未来的路

#### 4.3.1 市场价值温差信号

系统将用户已确认的能力与经过质量控制的外部岗位样本或市场资料对照，展示：

- 需求增减趋势；
- 样本来源、范围和更新时间；
- 用户对应能力的证据新鲜度；
- 可以补强的成果类型；
- 置信度与数据局限。

样本不足、地区不匹配或数据过期时，只能显示弱信号，不得输出确定预警。

#### 4.3.2 晋升或跳槽筹码对照

用户选择目标岗位、岗位族或级别后，系统对照“目标要求—已有筹码—证据缺口—待确认信息”，不直接给出离职、跳槽或晋升结论。

例如可以表达“目标样本普遍要求带人经历；你当前有 1 项协作主导证据，但没有已确认的直接带人证据”，不能表达“你不适合晋升”。

#### 4.3.3 目标倒推与里程碑

用户确认未来目标后，系统从过去资产出发生成差距候选，再拆成可验证里程碑。30 / 60 / 90 天、季度目标或自定义周期都只是可选模板。

里程碑由用户确认后，可以生成“当下的事”中的行动候选，但不得绕过用户直接写入任务，更不得把整套未来计划同步到 `/today`。

## 5. 前端信息架构与用户体验

### 5.1 `/growth` 主页面

主页面首先帮助用户在三个时间视角之间切换，不再默认展开完整计划。

建议结构：

1. **成长概览**
   - 本周最重要的工作状态；
   - 最近新增的高价值事件或资产；
   - 当前未来目标与最突出的一项差距；
   - 待用户确认的候选数量。
2. **三个平级入口**
   - `当下的事`：处理实际工作；
   - `过去的果`：查看作品、证据和筹码；
   - `未来的路`：查看目标、市场信号和里程碑。
3. **问一问成长助手**
   - 可以围绕当前工作、过去资产或未来目标发问；
   - 清楚显示本次使用的数据范围。
4. **KNOWLEDGE**
   - 三张与当前成长问题相关的用户科普卡；
   - 提供 `查看全部知识 →`；
   - 不展示内部推荐依据、模型规则或系统机制说明。

首页不承载完整任务列表、作品列表、市场样本、历史版本和长篇 AI 对话。卡片点击进入独立页面或模态框，避免页面无限变长。

### 5.2 推荐页面与载体

| 内容 | 推荐载体 | 说明 |
| --- | --- | --- |
| 当下工作台 | `/growth/work` | 高频使用，容纳任务、沟通、情绪、高价值事件和周报 |
| 过去资产库 | `/growth/assets` | 支持筛选、搜索、关联、导出和批量管理 |
| 未来仪表盘 | `/growth/direction` | 展示目标、筹码、差距、市场信号和里程碑 |
| 计划详情 | `/growth/plans/{id}` | 作为未来方向下的可选深度工具 |
| 单项任务或事件 | 右侧抽屉 | 保持当前工作上下文 |
| 沟通、周报草稿 | 大模态框或独立编辑页 | 避免误发送，方便逐条选择来源 |
| 图表明细 | 模态框 | 点击后不改变主页面滚动位置 |
| AI 对话 | 首页折叠区；移动端全屏 | 不挤压三个主要入口 |

### 5.3 当下工作台

默认只突出：

- 一个快速输入框；
- 当前 1—3 个破局点；
- 待处理沟通或汇报；
- 可沉淀的高价值事件提醒；
- 本周周报入口。

完整任务清单、已完成历史和私密情绪记录按需展开，不在首屏平铺。

### 5.4 过去资产库

首屏展示资产概览、最近沉淀、高价值事件待确认和能力证据趋势。作品、证据、经验和筹码可以分 Tab 浏览，单项详情展示来源、隐私、关联能力和使用记录。

### 5.5 未来仪表盘

首屏展示一个当前目标、已有筹码、关键差距和下一里程碑。市场趋势、目标样本、完整计划和历史对照按需进入二级页面，不制造信息压迫。

### 5.6 响应式行为

- 桌面端：概览与当前操作为主列，提醒和摘要为辅列；
- 手机端：快速输入和 1—3 个当前破局点置顶；
- 抽屉在手机端改为全屏模态页；
- 草稿可以自动保存，但只有用户确认才进入正式记录；
- 刷新或断网后保留草稿并提示状态，不得冒充已确认内容。

## 6. 状态与确认模型

### 6.1 工作项状态

`captured`、`planned`、`in_progress`、`blocked`、`completed`、`deferred`、`cancelled`。

AI 排序只改变建议顺序，不改变正式状态。完成工作项可以生成高价值事件候选，但不自动生成能力事实。

### 6.2 高价值事件状态

```text
captured → structured → confirmed → archived
                ├→ needs_more_evidence
                └→ discarded
```

- `captured`：用户原始记录；
- `structured`：程序或 AI 已整理为候选；
- `confirmed`：用户确认事实、可见范围和用途；
- `needs_more_evidence`：值得保留但仍缺结果或证据；
- `discarded`：用户明确不沉淀；
- `archived`：进入过去资产并保留来源关系。

### 6.3 作品与能力状态

作品可以是 `draft`、`active`、`unavailable`、`archived`。能力判断分为 `market_signal`、`ai_candidate`、`user_claimed`、`evidence_confirmed`，不得混为一个字段。

### 6.4 目标与里程碑状态

未来目标为 `draft`、`active`、`paused`、`completed`、`superseded`。里程碑为 `proposed`、`confirmed`、`in_progress`、`completed`、`cancelled`。

计划修改保留版本；恢复旧版本应形成新的当前版本，不能覆盖历史。

### 6.5 周报状态

周报为 `draft`、`reviewed`、`exported`、`archived`。系统只记录用户是否导出，不得声称已经发送。重新生成周报不得覆盖用户已经编辑的版本。

## 7. 数据模型基线

成长守护复用 `CareerEvent`、`Evidence`、`GuardianFinding`、`ActionItem`、`DecisionRecord` 和 `Outcome` 作为跨域事件图。下表中标注“已进代码”的对象由 `0058`—`0068` 定义，相应 30 张表以及增量字段已部署到实际 `zhihu`；未标注对象仍是目标设计：

| 建议对象 | 关键字段 | 作用 |
| --- | --- | --- |
| `growth_work_intakes`（Phase A 已进代码） | 用户、请求幂等键、HMAC 指纹、候选载荷、解析模式和状态 | 原始输入不入库的候选整理边界 |
| `growth_project_profiles`（0068 已进代码） | 用户、客户、项目名、总目标、成功标准、当前策略、关键约束、下次跟进、停滞阈值、版本和确认时间 | 由用户独立定义项目总目标，不用某条工作线的摘要代替项目目标 |
| `growth_work_items`（Phase A/0065/0067/0068 已进代码） | 用户、来源、标题、项目关联、事项级上下文、状态、资料、待确认问题和跟进规则 | 可跨多周跟进的长期父事项；可多条共属一个独立项目档案 |
| `growth_work_updates`（0064/0065 已进代码） | 用户、工作项、请求幂等键、会议/方案/进展原文、规则分类、建议、STAR 提示与节点建议 | 单事项追加式时间线，不覆盖历史内容 |
| `growth_work_nodes`（0065 已进代码） | 父事项、节点标题、状态、顺序、依赖、时间提示、来源、版本与确认时间 | 一件长期事项内的可视化推进节点 |
| `growth_work_node_evidence`（0065 已进代码） | 节点、原更新、关系类型、证据摘录、分析摘要、置信度、分析模式/规则版本、确认状态 | 点击节点时对照原纪要与 Agent 分析，不把建议当事实 |
| `growth_work_materials` / `growth_work_material_requests`（0066/0067/0068 已进代码） | 原始材料、内容哈希、来源、项目关联、用户填写的客户/项目、发生时间及精度、下次跟进、解析模式、模型/规则来源、请求幂等键和版本 | 一次保存会议纪要或零散材料；项目、客户和时间由用户提供，Agent 不猜测；日期未知时保持未知 |
| `growth_work_material_statements`（0066 已进代码） | 已确认事实、决定、提议、待确认问题、供应商说法、范围变化、行动或冲突，及证据摘录、候选状态和版本 | 将 Agent 提取结果保存为可逐条复核的候选，不直接升级为事实 |
| `growth_work_material_links` / `growth_work_material_relations`（0066 已进代码） | 材料到多个父事项/节点的建议或确认关联、逐目标证据、人工改派原因与材料间版本关系 | 同一份纪要可精确归入多条线索；各线索独立判断，不复制一个全局结论 |
| `growth_work_placement_events`（0066 已进代码） | 目标事项、优先级轴、进展健康度、象限、逐目标证据、规则版本、建议/确认状态和覆盖原因 | Agent 只建议象限；用户确认或调整后才改变正式看板位置，并保留历史 |
| `growth_work_progress_events`（0067 已进代码） | 目标工作线、材料、影响类型、一句结论、因果、前后状态、下一缺口、逐字证据、置信度、分析版本、人工复核和可汇报状态 | 持久化新材料对工作线目标的推进/受挫/转向/补充上下文/无变化；证据不足保留 `unknown` |
| `growth_project_progress_events`（0068 已进代码） | 项目、材料、影响类型、结论、因果、前后状态、下一缺口、证据、分析模式/版本、项目基线版本、建议/确认/驳回状态 | 独立持久化新材料对用户确认项目总目标的作用；AI 只生成 `suggested` |
| `growth_communication_drafts`（整合阶段已进代码） | 场景、对象、目标、事实、语气、草稿版本、确认状态 | 不可覆盖、不自动发送的沟通与汇报草稿 |
| `growth_emotion_notes`（Phase A 已进代码） | 加密内容、隐私级别、脱敏事实和删除状态 | 只在用户明确选择时保存，与正式事实隔离 |
| `growth_work_events`（Phase A 已进代码） | 情境、任务、行动、结果、角色、发生时间、确认状态 | 高价值事件及 STAR 维度 |
| `growth_weekly_reports`（Phase A 已进代码） | 周期、纳入项、正文版本、确认与导出状态 | 可编辑周报草稿 |
| `growth_audit_events`（Phase A 已进代码） | 用户、操作者、实体、动作、请求和前后摘要 | 不记录正文的成长域变更审计 |
| `growth_portfolio_items`（Phase B 已进代码） | 类型、链接或附件引用、摘要、可用状态、隐私级别、请求幂等键 | 作品与职业资产 |
| `growth_evidence_items`（Phase B 已进代码） | 类型、标题、时间、摘要、来源、隐私级别、请求幂等键 | 可回溯成长证据 |
| `growth_skill_assessments` / `growth_skill_evidence_links`（Phase B 已进代码） | 技能版本、来源层级、证据充分度、确认状态与证据关联 | 分离市场信号、AI 候选、自述和证据确认能力 |
| `growth_future_targets`（Phase C 已进代码） | 目标岗位或级别、来源、期限、状态、版本 | 用户确认的未来方向 |
| `growth_market_signals`（Phase C/0062 已进代码） | 来源、采集时间、两段 30 天窗口样本与占比、温差、方向、质量、方法、局限与弱信号状态 | 不冒充用户能力事实的市场快照；样本不足时方向为未知 |
| `growth_gap_snapshots`（Phase C 已进代码） | 目标版本、已有筹码、差距、未知项、样本信息、置信度 | 稳定的差距候选和确认版本 |
| `growth_milestones`（Phase C 已进代码） | 目标、完成条件、截止日、状态、版本 | 可验证里程碑及行动候选来源 |
| `growth_reflections`（Phase B 已进代码） | 触发来源、问题、回答、隐私和确认状态 | 把经历沉淀为经验 |
| `growth_handoffs`（整合阶段已进代码） | 来源、目标守护、内容、依据、影响、状态与版本 | 先预览、再确认，可撤销的跨守护交接 |
| `growth_inquiries`（整合阶段已进代码） | 请求幂等键、选定数据范围、问题、回答、引用、模式与模型元数据 | 默认本地程序回答，显式同意后才使用外部 AI |

工作项、高价值事件、作品、证据、能力、目标和里程碑之间均为可审计关联，不能靠复制文本保持关系。

所有表必须包含用户所有权，或能经父对象严格回溯用户。越权访问统一返回 404。唯一约束、版本号和幂等键必须在 MySQL 数据库层落地，不能只依赖前端。

## 8. 程序、AI 与用户的分工

### 8.1 程序负责

- 状态机、日期、逾期、版本、撤销、权限和幂等；
- 工作项的确定性聚合与用户规则；
- 对 AI 返回执行证据编号校验、结构归一、范围边界、近重复线索归并和冲突门禁；模型失败时可以保留材料并明确降级状态，但不能把失败结果伪装为 AI 成功；
- 将会议纪要原文、节点证据摘录和 AI 分析分层保存，并在节点详情中对照展示；
- 已确认记录的统计和趋势；
- 作品链接状态和证据关联；
- 质量合格市场样本的聚合、来源与更新时间；
- 周报纳入范围和跨守护写入审计。

### 8.2 AI 负责

当前“当下的事”材料合同为 `growth-work-material-ai-v5`。Agent 不是对某几份会议纪要分别摘要，而是先读取用户确认的独立项目总目标、成功标准、当前策略、关键约束和近期已确认历史，先判断材料对项目总目标的作用，再分别判断受影响工作线的增量。它只提交带证据的候选，程序与用户共同控制边界和写入。

- 从杂乱输入中提取工作项候选；
- 将会议纪要、方案和零散进展归入一条或多条既有工作线，必要时提议新线索，并给出原文证据；
- 对照项目目标判断新材料属于推进、受挫、转向、补充上下文或无变化，证据不足时返回 `unknown`；
- 用一句结论、因果、前后状态和下一缺口解释“这次对整个项目产生了什么作用”；
- 为每条线索提议推进节点、优先级轴和进展健康度，不直接移动正式象限；
- 提议 1—3 个当前破局点并解释原因；
- 草拟沟通策略、话术和汇报框架；
- 将情绪与事实分离，但不判断用户感受对错；
- 把已确认事实整理为 STAR 维度、周报或复盘草稿；
- 从作品和证据中提议能力候选；
- 解释市场信号、筹码和差距；
- 草拟多种里程碑与倒推路径。

### 8.3 用户必须确认

- 工作项、优先级和完成结果；
- 独立项目档案，包括客户/项目、总目标、成功标准、当前策略、关键约束和下次跟进；
- 工作线和材料的项目归属，以及事件发生时间；
- 每条进展影响的归类、证据和是否允许进入正式汇报；
- 哪些内容是事实，哪些原始情绪可以保留；
- 高价值事件是否进入过去资产；
- 作品归属、证据真实性和隐私级别；
- 能力事实、未来目标、差距和里程碑；
- 周报、沟通草稿和任何外发内容；
- 是否向其他守护写入摘要。

## 9. 能力、市场与证据规则

| 层级 | 示例 | 可否直接成为用户能力事实 |
| --- | --- | --- |
| 市场信号 | 合格岗位样本反复出现某技能 | 否 |
| AI 候选 | AI 从作品中推测用户使用过某技能 | 否 |
| 用户自述 | 用户表示自己会某技能 | 可以保存为自述，但不是证据确认 |
| 证据确认 | 用户确认并关联真实项目、结果或反馈 | 是 |

同一能力必须能查看：证据来源、发生时间、用户角色、结果、最近使用时间和仍缺的证据。课程与证书可以成为证据之一，但不能单独证明用户能够在真实工作中独立使用。

市场信号必须展示来源范围、采集时间、样本量和适用限制。对用户作品进行代码质量或复杂度分析前，应说明数据范围和授权，私有代码默认不发送外部模型。

## 10. AI 问询体验与安全边界

- 支持流式输出、安全 Markdown、取消、重试和断流后的可恢复状态；
- 同一请求使用幂等键，断流不得造成重复调用或重复写入；
- 每次回答显示使用了哪些数据域：当下工作、过去资产、未来目标或市场样本；
- 原始情绪默认不进入普通 AI 问询上下文；
- 私密附件、代码和沟通原文默认不发送外部模型；
- 对证据不足的问题回答“尚未核清”，并列出需要补充的信息；
- AI 不直接调用正式写接口，只返回结构化候选；
- AI 不自动发送沟通、周报，不直接改简历，不替用户决定离职、晋升或转岗；
- 管理页记录中文功能点、服务、模型、耗时、用量和结果，不保存请求或回答正文。

## 11. 跨守护数据闭环

| 来源/去向 | 允许流转的内容 | 禁止自动推断 |
| --- | --- | --- |
| 决策 → 成长 | 已接受 Offer、确认岗位、职责、入职日期和目标 | 已实际入职、承诺已兑现 |
| 机会 → 成长 | 目标岗位、合格样本和常见技能 | 用户一定缺少某能力 |
| 成长 → 机会 | 用户确认的能力和证据摘要 | 自动提高岗位匹配分 |
| 成长 → 简历 | 用户选定的高价值事件、项目结果和 STAR 草稿 | 自动覆盖正式简历 |
| 成长 → 决策 | 已确认筹码、差距和职业偏好变化 | 自动建议接受、拒绝或离职 |
| 成长 → 权益 | 用户确认的职责、承诺或工作边界变化 | 自动认定公司违法或违约 |
| 成长 → 收支 | 用户确认的成长投入、报销或收入变化线索 | 未确认费用、工资或报销事实 |
| 成长 → `/today` | 聚合状态、重要提醒和进入成长守护的链接 | 完整工作任务、原始情绪、沟通或周报正文 |

跨域写入统一采用“建议卡片 → 查看差异 → 用户确认 → 写入并可撤销”。

## 12. KNOWLEDGE

知识内容服务用户的真实职场成长，不解释系统内部机制。首批建议覆盖：

- 任务很多时如何找到真正的关键路径；
- 精力不足时怎样安排高认知和低认知任务；
- 怎样做结论先行的工作汇报；
- 如何提出异议、追问需求和拒绝不合理安排；
- 被批评后如何区分情绪、事实和可行动信息；
- 什么样的日常工作值得沉淀为高价值事件；
- 怎样用 STAR 复盘事实而不是包装经历；
- 一份有用的周报应该回答哪些问题；
- 如何给项目结果留下可信证据；
- 作品集不只是链接集合，还需要哪些背景和结果；
- 如何判断一项能力已经可以独立使用；
- 如何读懂招聘市场中的弱信号与强信号；
- 晋升或跳槽前怎样盘点自己的客观筹码；
- 如何把长期目标倒推成可验证的近期里程碑；
- 什么时候使用 30 / 60 / 90 天计划，什么时候不需要。

卡片显示标题、适用场景、摘要和标签；详情页展示完整科普、适用条件和更新时间。卡片不展示“推荐依据”“AI 判断规则”或内部版本说明。

## 13. 图表与指标

图表用于帮助用户看清事实，不做装饰性评分：

- 当下：工作完成与阻塞趋势、精力状态与任务类型分布；
- 过去：高价值事件和作品积累趋势、能力—证据覆盖、证据新鲜度；
- 未来：目标—筹码—差距矩阵、里程碑进度、市场技能温差趋势；
- 跨时间：从工作事件到作品、能力和未来里程碑的转化漏斗。

所有统计只使用已确认记录。原始情绪不进入绩效式图表。点击图表在模态框查看明细，不自动滚动页面。空样本说明原因和下一步，不绘制误导性比例。

## 14. 导出、删除与隐私

个人中心允许分类导出：

- 工作项与用户确认的结果；
- 长期父事项、推进节点、会议/方案/进展原文和节点证据；
- 高价值事件、反思和周报；
- 作品、证据和能力事实；
- 未来目标、差距快照和里程碑；
- 跨守护写入与撤销记录。

原始情绪记录默认不进入通用导出，用户需要单独选择。删除原始情绪不能影响已经由用户确认的客观事件，但系统必须说明两者的来源关系已被解除。

删除作品、证据、目标或已收起事项前展示影响范围。已收起事项的永久删除会清除其纪要/进展原文、节点、节点证据、规则分析以及直接引用该事项的沟通草稿和问询回答，并擦除来源候选中的对应正文；系统只保留无法反推正文的最小墓碑和审计计数。如果事项已经形成独立工作成果，则先阻止删除，避免破坏 STAR、周报、作品或跨守护引用。用户主动另存的私密情绪记录不是事项附件，不随单个事项静默删除。附件删除与结构化记录删除分开确认。

## 15. 当前实现状态（2026-08-26）

### 15.1 已实现或可复用

- `/growth` 已改为成长总览，`/growth/work`、`/growth/assets`、`/growth/direction` 分别对应“当下的事、过去的果、未来的路”三个平级工作区；成长路由不再叠加全站导航与页内胶囊导航，桌面使用同一行的成长上下文导航，手机使用“成长守护 · 当前页”单入口菜单。独立“整合”页不再作为产品入口，旧地址回到“当下的事”；
- “当下的事”主体已进入代码：用户从同一个材料入口提交混乱待办、会议纪要、方案、进展或零散记录，AI 先整理为可编辑的长期事项与节点候选；拆出的事项默认都可保留，前 3 项只作为优先级建议，不是任务容量；
- “当下的事”已按用户第二轮验收反馈重构为低约束任务线程：活动任务先以紧凑清单显示，仅展开当前跟进项；进展、卡点、反馈、疑问和结果共用一个自由输入入口，每次补充追加保存并返回建议。沟通草稿一键使用已有上下文，STAR、完成和周报均可跳过或以后补，不再要求按步骤填写多个字段；清单支持单项/批量移出、“已收起事项”恢复，以及带影响说明和二次确认的永久删除；
- “当下的事”进一步支持长期事项：一段原始输入优先保留顶层事项，再将具体行动放进父事项的推进节点；资料链接、待确认问题和“持续盯进度”跟进规则不再被拆成并列任务。用户原文的回归结果为 4 个父事项、9 个推进节点和 2 条资料链接；
- 页面提供统一材料入口，可粘贴会议纪要、方案、进展或零散记录，也可读取本地 `.txt` / `.md` 文件。原文只保存一次；Agent 可为同一材料提出多条父事项/节点关联，但每条线索分别给出证据与象限建议。未归属或待复核材料保留在待处理列表，可重新打开、手工改派或补充关联，不因离开页面而消失。主清单、象限看板和时间线默认不返回材料正文，只在用户点击查看时按需加载；
- 事件梳理入口本身由 AI 驱动，不再要求用户逐次勾选“是否使用 AI”。当前材料合同版本为 `growth-work-material-ai-v5`；Agent 以项目目标、成功标准、当前策略、关键约束和已确认历史为全局基线，判断新材料对结果的推进、受挫、转向、补充上下文或无变化。确定性程序继续负责证据编号、输出校验、范围边界、近重复工作线归并、版本与写入门禁；模型不直接把建议写成事实或移动事项；
- 用户可手工建立和确认独立项目档案，维护客户/项目、总目标、成功标准、当前策略、关键约束、下次跟进和停滞阈值，再将多条工作线与多份材料归到同一项目；材料发生时间和项目归属也以用户输入为准，Agent 不猜测写入；
- `growth_project_progress_events` 和 `growth_work_progress_events` 分别持久化项目总目标级和工作线级的影响类型、结论、因果、前后状态、下一缺口、证据与复核状态。两类 AI 输出都只能创建 `suggested`，不自动确认；停滞只由用户下次跟进、真实发生时间和已确认推进事件计算，AI 建议不重置时钟；周/月回顾按人工事件时间归档，无日期材料单独计数，正式汇报只使用已确认且允许汇报的事实；
- 完成任务后页面给出可选的 STAR 维度预览；缺项显示“以后再补”，用户也可不补直接收进本周素材。周报仅在已有确认素材或草稿时按需展开；
- 初次杂乱输入仍不保存整段原文；用户明确选定事项后的会议、方案和进展补充会作为 `growth_work_updates` 原文持久化，并与节点证据关联。情绪与任务候选分离，情绪原文默认不保存，用户明确选择后加密存储；
- 沟通与汇报军师已进代码：基于用户提供的事实、对象、目标和语气生成结论先行的本地规则草稿、沟通策略、确认问题和风险提示；修订生成不可覆盖的新版本，只能复核/导出/归档，不会自动发送；
- 完成任务只生成 `structured` 工作事件候选，缺失的情境、行动和角色保留为证据缺口，不虚构 STAR；
- 用户确认且标记为可汇报的事件才能进入可编辑、可复核和可标记导出的周报，系统不声称已发送；
- `/today` 的成长卡片只读取任务、阻塞、事件、资产、差距和里程碑的聚合数量，不返回标题、结果、情绪、反思或周报正文；
- `0058` 已定义工作输入、工作项、加密情绪记录、工作事件、周报和成长审计 6 张表，所有权、状态约束、唯一键和清数边界已进入代码；
- `0059` 已定义作品、成长证据、能力版本与证据关联、反思 5 张表；作品和证据使用请求幂等键及指纹，删除前检查关联影响；
- 作品与证据需本人确认，能力保留 AI 候选、用户自述和证据确认分层及历史版本；反思默认私人，只有本人确认后才生成方法证据；
- 公开 GitHub 作品可读取仓库元数据、语言、README、测试、CI、构建清单和复杂度线索，并可调用管理员已配置的文本模型形成工程、质量、复杂度和技能候选；分析快照保存在成长审计中，删除作品后不会继续展示，候选不会自动成为能力事实；
- 能力证据雷达、覆盖条和月度曲线已进入过去资产页；等级只由本人确认的能力和证据数量计算，明确表示“证据覆盖度”而不是能力高低或代码水平；
- 过去资产页已改为“作品 → 证据 → 能力 → 筹码”的紧凑资产库；新增作品/证据表单默认收起并互斥展开，空状态给出直接行动入口，不再让两个长表单占据首屏；
- 职业筹码仅聚合已确认作品、证据和能力；JSON 导出排除候选、失效项和私人反思；
- `0060` 已定义未来目标、市场信号、差距快照和里程碑 4 张表；目标、差距和里程碑确认均形成新版本而不覆盖历史；
- 市场信号保存来源、采集时间、样本量、质量等级、方法版本与局限；样本不足、过期或不可用时只生成弱信号和“尚未核清”，不形成确定差距；
- `0062` 在既有市场信号表增加最近/上一 30 天窗口的数量、占比、百分点温差和方向；数据来自现行招聘 Core，任一窗口少于 5 个样本时方向为未知，不以单窗口占比伪装趋势；
- 已确认里程碑只能生成 Phase A 工作输入候选，仍需用户第二次确认才创建正式工作项，工作完成也不会自动完成里程碑；
- 未来方向页已按“确认目标 → 市场温差 → 差距 → 里程碑”组织为职业仪表盘；目标与里程碑表单按需展开，市场窗口可视比较，差距分为已有筹码、待补差距和尚未核清，仍不替用户决定方向；
- 跨三个工作区的成长问询已进代码：默认用本地程序从用户勾选的已确认数据范围组织回答和证据引用，缺证据时明确说“尚未核清”；可选外部 AI 要求二次同意，支持流式、取消、同请求重试、幂等、安全 Markdown 和调用审计；
- 跨守护交接已进代码：只允许从用户确认的工作事件、有效作品、确认证据/能力、确认目标/差距/里程碑提议；写入前展示目标、内容、依据和影响，确认后进入共享收件箱，可撤销并留审计，不自动生成其他守护的正式结论；
- 完整分类 JSON 导出已进代码，工作域现包含独立项目档案、项目级进展建议、父事项、推进节点、追加更新、节点证据、材料、结构化陈述、多目标关联、材料关系、象限变更、工作线级进展建议、已确认工作事件和周报，同时包含资产/能力、方向/市场元数据、已复核沟通草稿和交接历史；排除原始情绪、未选定候选项、私密反思和问询正文；
- 沟通草稿继续围绕具体事项按需使用并支持浏览器本地草稿恢复；成长首页已接入入职阶段、跳槽成长、求职阶段知识卡，不暴露内部推荐逻辑；
- 接受 Offer 后已有幂等成长事件与入职待办链路可复用；
- 机会分析已有技能差距、学习路线、模拟面试和趋势信号，可作为未来方向的上游；
- `/today` 已读取成长域摘要，不回传沟通、情绪或其他正文。

### 15.2 已部署并完成本轮技术验收

- 实际 `zhihu` 曾由 `0056` 迁移至 `0063`；市场温差和任务快照字段已逐一存在；
- 第二轮“当下的事”用户验收反馈后，实际库曾迁移至 `0064 (head)`，形成 90 张表、19 张 `growth_%` 表的历史快照，`growth_work_updates` 已只读确认存在；
- 长期事项整改后，实际库曾到 `0065`，形成 92 张表、21 张 `growth_%` 表的历史快照；`growth_work_nodes` 和 `growth_work_node_evidence` 已存在；
- `0066` 迁移前已在仓库外备份实际 Alembic 版本表和成长工作项，文件大小、`0600` 权限和 SHA-256 均完成核对。实际 `zhihu` 随后由 `0065` 升级到 `0066`；当时只读复核为 98 张表、27 张 `growth_%` 表，6 张材料追踪表和长期事项的 5 个象限字段均存在，原有 32 个工作项数量未变；这只是 `0066` 历史快照，不代表 `0068` 当前数量；
- `0068` 迁移前仓库外完整备份为 353,045,232 字节，权限和 SHA-256 均已核对；成对盘点的 uploads 为 0 个文件。完整恢复及 `0067 → 0068 → 0067 → 0068` 往返通过，最终代码唯一 head 与实际 `zhihu` 均为 `20260826_0068 (head)`，实际库为 101 张表、30 张 `growth_%` 表；
- `0066`/v4 阶段的历史测试基线为后端全量 574 项 `OK`、100 项隔离 MySQL 写入测试因未配置 `CAREER_GUARDIAN_TEST_DATABASE_URL` 按安全门跳过，成长 AI、材料与整合服务定向 62 项通过；当时前端 ESLint、TypeScript `--noEmit` 与 Next.js 16 webpack 生产构建通过、35 个路由生成。这些数字仅作历史基线，不写成本轮 `0068` 全量重验；
- 实际 `0065` MySQL/API 使用一次性非管理员账号跑通活动删除 409、缺版本 422、旧版本 409、收起删除 200、重复删除 404，并核对节点、更新和证据同步清理、工作区隐藏；临时账号和数据随后删除，旧 token 返回 401；
- 实际临时账号 API 闭环确认用户原文解析为 4 个父事项、9 个节点、2 条资料；统一入口返回 1 个路由候选，规则建议为“问卷”节点 `in_progress`、“硬件”节点 `blocked`。人工确认后得到 2 条 `confirmed` 节点证据，原始纪要完整保留，完整导出包含新对象；临时账号随后删除；
- 实际 `0066` MySQL/API 另用一次性非管理员账号验证一份材料同时路由到两件工作：两个目标分别获得 `focus` 与 `maintain` 建议，确认前均保持 `unknown`，人工确认关联和象限后才进入各自看板位置；两条时间线均可回看同一原文及各自证据。待处理列表、未归属材料和人工补链同样跑通。临时账号随后通过账号自删清理，直接查询确认 6 张新表测试行均为 0、原有工作项仍为 32，未写入或改动 admin 业务数据；
- 最新 `growth_ai_service + growth_material_service + growth_integration_service` 成长核心定向回归 92 项全部 `OK`，失败 0，只有 3 条仓库已存在的 SQLAlchemy identity-map `SAWarning`；本轮未重跑后端完整全量套件。隔离 MySQL 完成 `0067 → 0068 → 0067 → 0068` 往返并删除临时 schema。前端 ESLint 为 0 个错误、11 个仓库已存在警告，TypeScript `--noEmit` 通过，Next.js `--webpack` 生产构建 35/35 路由通过；默认 Turbopack 只因当前环境无法绑定构建临时端口而失败，不记为代码或路由构建失败；
- admin 当前为 35 条工作项。人民日报独立项目的用户确认总目标为“持续澄清真实业务需求、技术与交付边界，推动已识别的 AI 合作线形成可落地、可验证、可追踪的方案或试点。”，停滞阈值为 14 天；3 条工作线与 7 份材料均已关联到该项目，材料正文和哈希保持不变；
- 经用户明确授权的真实外部回归中，7 份人民日报材料均由 `SenseAudio/qwen3.8-27b` 处理，`analysis_mode=ai`且无 fallback，产生 7 条项目级 AI 建议事件。所有 AI 输出仍是 `suggested`，工作进展事件的人工复核/确认数为 0；人工检查未见将 Demo、待确认或勘察写成已交付。该检查只验证了指定边界，不等于已人工确认模型的全部业务判断；当时只在仓库外保留不含原材料的紧凑报告，raw 报告未保留；
- 最新修复同步解决了三个真实回归缺口：`target_analyses` 优先于显式候选归属兜底，避免已判断的 `advanced/redirected` 被覆盖成通用 `context`；语音边界处理保留已正确路由的在线客服/热线数字化增量，只拆真正含混的合并目标；唯一已确认项目遗漏 `project_analyses` 时触发一次 `MaterialProjectCoverageMissing` 定向 repair，允许五类独立判断且禁止强制制造 `advanced`；
- `0066` 版本的登录态浏览器曾在桌面和 `390×844` 手机宽度验证 `/growth/work`：顶部只有“总览、当下的事、过去的果、未来的路”，不存在“整合”；当时的统一材料入口、待处理列表、四象限加独立“待判断”区和定高事项弹层均可操作，手机无横向溢出，控制台错误与警告为 0。该条仅保留为历史证据；
- 最新 v5/`0068` `/growth/work` 已在应用内浏览器真实登录态完成本地技术/视觉验收。默认 `783×837` 与移动 `390×844` 均无横向溢出；项目卡展示用户确认的精确总目标、3 条工作线、最新判断及 24 天/19 天停滞提醒；“项目全貌”展示 7 条项目事件，按月为 7 条，上一周 `2026/8/17—8/23` 为 2 条；热线工作线展示推进 2、受阻 1、补充 2，并可见 `before/change/now/next_gap`；
- 原始材料正文默认不在 DOM 中，只有点击“查看依据”才按需读取。桌面使用内部滚动弹窗，移动端使用全屏弹层。“整理与清理”弹层可见“已收起事项一键清空全部”和“清空待归位材料”，本次两类计数均为 0，且未执行任何删除。首次打开“项目全貌”时旧后端会话已失效；重启实际后端后复验通过，该现象是旧运行会话失效，不记为产品接口缺陷；
- 前后端和市场服务均已重启并通过健康检查。admin 真实登录态已在实际 `0062` 库验收过去资产和未来方向页：内嵌桌面宽度 705 与 `390×844` 手机宽度均无横向溢出；能力证据区和技能市场温差区可见；
- 重构后的过去资产、未来方向和成长上下文导航已在登录态 `1280×900` 与 `390×844` 复核：两个页面无横向溢出、无第二层页内导航，桌面模块切换、Esc 关闭、手机菜单展开与跨页后自动收起均通过，浏览器控制台错误和警告为 0；
- 使用公开 `fastapi/fastapi` 仓库完成一次真实 GitHub 读取、管理员已配置外部 AI 分析、数据库持久化和页面回读；验收专用作品、2 条成长审计及 1 条 AI 调用日志随后按精确 ID 删除并只读确认均为 0；
- 实际市场接口以“数据分析师”查询得到最近窗口 289 个样本、上一窗口 0 个样本，因此页面按规则显示“样本不足/趋势未知”。这证明温差降级逻辑使用真实数据工作，不证明当前数据已形成可判定趋势。
- 低约束任务线程使用隔离临时账号在实际 `0064` 库完成 7 项任务同时确认、追加自由上下文、读取规则建议、生成并修订任务关联沟通草稿、无结果完成和无完整 STAR 字段确认；清理能力另用隔离账号完成“活动列表 → 已收起 → 恢复跟进”并核对上下文仍在。两类临时账号均随后删除，开发验收不依赖 admin 的真实任务数据。
- 2026-08-26 用户明确要求清理成长守护旧测试数据并重新测试。清场前完整备份与临时库恢复演练通过；实际库事务精确删除 30 张成长表中的 362 行、11 条成长职业事件、4 条证据、2 条发现、3 条行动和 71 条成长 AI 调用日志，`business_data_epoch` 从 38 增至 39。清场完成当时 30 张成长表均为 0 行，`0068` 结构、admin 账号、AI Provider 配置、AI 配置审计和其他业务表保持不变；登录态页面复验“当下的事、过去的果、未来的路”均为首次使用空态。该段是清场验收快照；后续重新操作产生的新数据不应被误写成清场残留。

### 15.3 当前诚实边界与后续增强

- 深度自动分析当前只支持公开 GitHub 仓库；其他项目、网盘、设计稿和上线链接可收纳，但主要依赖用户摘要，私有代码和附件不会自动发给外部模型；
- 能力雷达和曲线展示确认证据覆盖度，不是客观熟练度评分。AI 生成的技能仍是候选，需用户补充证据并确认后才进入能力事实；
- 温差计算与展示已经可用，但当前实际历史数据的上一 30 天窗口为 0；需要后续补齐更早的合格招聘数据或自然积累，才能出现可判定的上升、稳定或下降；
- 作品/证据筛选、搜索、批量管理、目标/里程碑历史版本浏览，以及更完整的断网续办、通用撤销和越权浏览器用例仍属于后续增强；
- 当前事件梳理 Agent 的工程合同为 v5，已有本地人工审核 fixture 与本轮用户授权的 7 份人民日报真实外部 `qwen3.8-27b` 回归证据。该真实回归没有 fallback，但 AI 结果仍全是建议，不是用户已确认的工作事实。发送其他材料前仍只做手机号、邮箱、证件号和账号等有限规则脱敏，姓名、内部术语、IP、密钥等不保证识别；不应向模型提交密码、密钥或未经授权的敏感原文；
- Agent 不持有任意 SQL 或数据库直连能力，只能提交结构化候选并调用受控领域接口。材料到线索、线索到象限和陈述到事实都需要用户确认；程序负责版本、所有权、幂等与冲突门禁；
- 工作闭环已收敛为“人工定义项目目标与时间 → 单一材料入口 → 精确归入一条或多条工作线 → 全局 Agent 判断五类增量影响 → 人工复核 → 停滞/周月回顾 → 沟通草稿/STAR/正式汇报按需复用”。正式周报/月报仍只消费用户确认且允许汇报的事实，不会直接把未确认材料或模型判断写进正式汇报；
- admin 中已有事项没有被本轮开发自动合并或永久删除，不以数据迁移或开发验收擅自替用用户业务数据；
- 本轮完成的是可供用户直接验收的技术闭环，不代表产品已正式发布到未检查的远端环境，也不替代用户最终业务验收。

特别说明：2026-08-27 复核确认，实际库与代码头均为 `20260826_0068 (head)`，实际库为 101 张表、30 张成长表。用户确认的独立项目目标、项目与工作线/材料关联、项目级与工作线级五类增量建议、停滞提示、周/月回顾和原文按需查看已进入当前实现。当前同时有本地 fixture/mock 技术证据和用户授权的 7 份人民日报真实外部 Qwen 历史回归证据；后者的输出全部是 `suggested`，不是人工确认的工作事实。清场快照为 0 行且完整备份已完成恢复校验；用户随后重新操作，最终只读审计时 12 张成长表共有 241 行。数量是动态快照，当前事实须以运行库查询为准。该结论不代表未检查的远端或生产环境已经部署，也不替代用户最终业务验收。

## 16. 开发顺序

### 阶段 A：当下工作最小闭环

1. 建立工作项、高价值事件、情绪记录和周报的数据结构及 MySQL 迁移；
2. 完成所有权、状态机、幂等、撤销和审计；
3. 实现快速输入和任务候选；建议优先项不超过 3 个，但用户可确认并跟进全部有效任务；
4. 实现情绪与事实分离、高价值事件确认；
5. 从已确认记录生成可编辑周报草稿；
6. 明确 `/today` 只读取摘要，不同步工作任务。

### 阶段 B：过去资产闭环

1. 作品链接和附件引用；
2. 高价值事件归档、证据关联和反思触发器；
3. 能力候选、用户确认和证据可视化；
4. 职业筹码账本、筛选、查询与导出。

### 阶段 C：未来方向闭环

1. 目标岗位或级别与版本；
2. 有来源、时效和置信度的市场信号；
3. 目标—筹码—差距快照；
4. 可选的 30 / 60 / 90 或自定义里程碑；
5. 用户确认后反推“当下的事”行动候选。

### 阶段 D：整合体验

1. 三个工作区的首页概览和模态明细；
2. 流式 AI 问询、安全 Markdown、幂等和证据引用；
3. 用户视角成长知识；
4. 与机会、决策、权益、收支、简历和 `/today` 的确认式交接；
5. 桌面、手机、断网恢复、权限、删除和真实登录态验收。

## 17. 验收标准

1. `/today` 展示职业生涯全局状态，不出现成长守护完整工作任务列表；
2. `/growth` 的“当下的事”能够处理真实职场任务、沟通、情绪和事件；
3. 用户可以从杂乱输入得到有解释依据的 1—3 个破局点候选；
4. AI 排序不会绕过用户改变正式任务状态；
5. 沟通、汇报和周报只生成草稿，系统不会自动发送；
6. 原始情绪默认不进入周报、资产、能力分析和跨守护上下文；
7. 用户可以把工作记录确认为高价值事件，也可以丢弃或等待补证据；
8. AI 不虚构 STAR 中缺少的结果、数字和用户角色；
9. 周报只汇总用户确认且允许汇报的记录；
10. 用户可以保存、查询、编辑和删除作品与成长证据；
11. 能力候选必须由用户确认，并能回溯到证据；
12. 市场信号、AI 候选、用户自述和证据确认能力在数据与视觉上分层；
13. 市场信号显示来源、时间、样本范围和局限；
14. 样本不足或过期时不输出确定的淘汰、晋升或跳槽判断；
15. 用户可以查看客观筹码和差距，但系统不替用户决定职业去向；
16. 30 / 60 / 90 只是可选模板，用户可以使用季度或自定义里程碑；
17. 未来里程碑只能生成待确认的当下行动候选；
18. 行动完成不会自动标记能力、目标或里程碑完成；
19. 图表只统计已确认记录，点击明细不改变页面滚动位置；
20. AI 支持流式输出、安全 Markdown、取消、重试、幂等和证据引用；
21. 跨守护写入前展示目标、内容、依据和影响；
22. 所有跨守护写入可撤销并有审计；
23. 用户可以分类导出工作、资产、能力、目标和里程碑；
24. 越权访问统一返回 404，删除不会破坏其他用户或其他守护数据；
25. 客户/项目、事件日期和下次跟进由用户填写，Agent 不猜测为事实；
26. Agent 能对照总目标说明每份材料带来的推进、受挫、转向、补充上下文或无变化，并展示证据、因果、前后状态和下一缺口；
27. 未确认建议不重置停滞时钟；周/月回顾按用户填写的发生时间归档，无日期材料不冒充当周/当月进展；
28. 列表、看板和时间线不直接展开材料原文，原文只在用户主动查看时以定高方式按需读取；
29. 真实外部模型回归只能在得到明确授权并记录发送边界后执行，不以 fixture、mock 或连接测试代替；
30. 桌面和手机完成空状态、正常状态、断网恢复、AI 失败、撤销和删除场景的真实登录态验收。

## 18. 发布口径

只有完成以下真实闭环，才可以称为成长守护基础能力可用：

```text
真实工作输入
  → 用户确认当前破局点
  → 完成工作并确认高价值事件
  → 沉淀作品或成长证据
  → 用户确认能力或职业筹码
  → 对照未来目标形成客观差距
  → 用户确认里程碑
  → 反推新的当下工作行动
```

只完成计划页面、任务列表、作品上传、AI 对话、市场图表或 `/today` 摘要中的任意一项，都不能称为完整成长守护。代码测试通过不等于用户验收，静态页面和演示数据也不得写成正式业务能力。
